# 每日五圖 · Chart of the Day — 執行規格（AGENT_BRIEF）

> 這份文件與排程 prompt（`~/Documents/Claude/Scheduled/chart-of-the-day-daily/SKILL.md`）是**一組兩份**。
> 改任何一邊，另一邊必須同步。不同步是這類系統最常見、也最難察覺的故障。

---

## 0. 執行前提（不成立就停下來回報，不要硬跑）

1. **repo 資料夾已連線。** 開工前先用 Read 讀 `/Users/kenny/chart-of-the-day/AGENT_BRIEF.md`（也就是本檔）。讀不到代表資料夾沒掛上——**停下來回報「repo 資料夾未連線，今日無法產出」，不要嘗試其他路徑、也不要把檔案寫到別的地方。**
2. **發布靠 daemon，不靠執行者。** `com.kenny.dashpush` 每 180 秒自動推送本 repo。執行者**不跑任何 git 指令**，寫完檔後依第 6 節最後一步抓 Pages 驗證。

---

## 1. 這套系統是什麼

一句話：**每天挑五個題目，用公開資料自己畫五張圖，每張圖配一段自己的判讀；當天封存成一個獨立 JSON，圖與資料永久保留。**

三件事必須同時成立，缺一張圖就不算數：

1. **圖是自己重製的。** 選題可以取材自 Bloomberg、FT、Economist 的圖表專欄，但**絕不轉載他們的圖**。我們讀他們的論點，然後用 FRED／Yahoo 的公開資料自己畫、自己算、自己下結論。
2. **每張圖都要有「所以呢」。** 一張沒有投資意涵的漂亮圖，對本知識庫沒有價值。`takeaway`／`so_what`／`watch` 三個欄位是必填。
3. **資料留得住。** 每天的 JSON 內含該圖用到的完整序列，`data/series/` 另存 CSV 快取。任何一天的圖，一年後都要能被 `tools/render_day.py` 原樣重畫。

### 它在整個知識庫系統中的位置

```
advisory-dashboard-daily（07:30 起跑，完成時間 10:40→11:20 且持續往後漂，25 家來源、15 個子類別）
              │  提供當日題材
              ▼
     chart-of-the-day（每天 11:30，本系統）
              │  提供「已經畫好、可直接引用」的圖與判讀
              ├──────────────► convergence-weekly（每週日，主題匯流訊號報）
              └──────────────► House View 月報（pptx 直接取用 PNG）
```

**方塊圖裡的 07:30 是上游的「起跑」時間，不是完成時間。** 這個區別曾經害本系統每天退回用前一日題材——原本 09:00 起跑、只等 15 分鐘，而上游實測完成時間是 08-04 09:40、08-05 10:40、08-06 10:40，**近四個交易日有三天在 09:30 時根本還沒好**。2026-08-06 決議把本系統挪到 11:00。**寫上游時間時一律寫完成時間，起跑時間對下游沒有意義。**

**但上游完成時間還在往後漂**：08-06 10:40 → 08-08 **11:20**，已經晚於 11:00 的起跑點，那天是靠 15 分鐘等待規則才接住的。2026-08-08 再挪到 **11:30**。**每次挪動都要把當時的實測時間留在這裡**——這條時間線本身就是判斷「還要不要再挪」的依據，只寫最新值就看不出趨勢。若哪天 11:30 又接不住，先查是不是上游的來源家數又變了（實測它在 24／25／26 家之間變動過），那才是根因；**一直往後挪只是在追症狀**。

**下游會直接拿 `charts/<date>/*.png` 貼進簡報。** 所以圖的頁尾要自帶來源標註，離開網站也站得住。

---

## 2. 五個欄位（slot）——每天固定五張，順序固定

| # | slot | 要回答的問題 | 題材來源 |
|---|------|------------|---------|
| 1 | **當日主圖** | 今天最重要的一件事，用一張圖說清楚 | 投顧儀表板當日 headline 與深度卡 |
| 2 | **市場異動圖** | 今天市場哪裡最不尋常 | 程式化掃描：把當日變動放進歷史分布看分位 |
| 3 | **重製圖** | 國際媒體最近在講什麼、我們怎麼看得更準 | **任何以資料或圖表為主要論證的報導**（見下「取材定義」） |
| 4 | **主題深掘** | 儀表板 15 個子類別中，今天最值得補一張圖的 | 投顧儀表板子類別 |
| 5 | **軌道圖** | 五大長期軌道輪值，確保跨期序列不斷線 | 固定輪盤（見下） |

**軌道輪盤（週一至週五依星期輪值，週末改為週線複查）：**

| 星期 | 軌道 |
|------|------|
| 一 | 利率與匯率 |
| 二 | 台股與資金流 |
| 三 | AI 與半導體 |
| 四 | 原物料與能源 |
| 五 | 信用與風險偏好 |
| 六、日 | **不套輪盤，改為週線複查（見下）** |

軌道圖的價值在**跨期可比**：同一條軌道每週同一天出現，序列與基期盡量沿用上一次，讀者才看得出趨勢。**不要每次換算法或換基期。**

**週末（六、日）的處理——排程一週跑七天，每個週末都會遇到，不是例外狀況。**

五條軌道綁死週一到週五，是為了「同一條軌道每週固定同一天出現」。週末若接著往下數第六、第七條，軌道與星期就會脫鉤，一輪之後全部錯位，跨期可比性直接歸零。所以週末**不推進輪盤**，改做**週線複查**：

- 從當週週一至週五已經出現過的五條軌道中挑一條，用**週線或月線尺度**重看（不是把同一張日線圖再畫一次）。
- `slot` 寫成 `軌道圖｜<軌道名>（週線複查）`。開頭仍是「軌道圖」，檢查腳本照樣通過。
- **挑選原則**：選當週那次留下未解問題、或當週之後又出現新變化的那條。**週六與週日不得挑同一條。**
- **序列與基期沿用該軌道當週那一次**，複查才看得出前後差異。
- 「五張圖 `theme` 不得重複」對週末同樣適用。

### 美國三大月度數據發布日：`slot` 1 由該數據佔用

非農（NFP）、CPI、PCE **發布當日，`當日主圖` 必須是該數據的圖**。這三個數字是政策路徑的直接輸入，發布日它們本來就是當天最重要的事——2026-08-08 那輪執行者在沒有這條規則時也是自己這樣選的，這裡只是把它固定下來。

- **偵測方式不靠行事曆**：`tools/macro_release.py --check` 讀 FRED 序列的 `last_updated`，那個時刻就是官方發布時刻（實測 `PAYEMS` ＝ 2026-08-07 8:31 AM CDT，正是 BLS 的 8:30 ET）。**行事曆會過期而且過期時是靜默的，資料自己的更新時間不會。**
- **時序**：三者都在美東 08:30 公布 ＝ 台北 20:30，永遠落在次日 11:30 那輪之前約 13 小時，**不會漏接**。
- **`theme` 一律是「央行、利率與匯率」**，其餘四張圖照常避開該主題。
- **產圖**：`--build NFP|CPI|PCE` 會回傳備妥序列的 chart dict，**文字欄位刻意留空**——判讀要看當天的實際數字寫，工具不產罐頭句。它附的 `_hint` 有算好的關鍵數值供引用，**寫進 JSON 前要把 `_hint` 拿掉**。
- **每天都要把 `--check` 的結果寫進 `about.macro_release`**，不論有沒有發布。檢查腳本靠它守門：有 `fresh` 卻沒有對應的 slot 1 就是硬失敗。**沒寫等於沒查過，會被警示。**

三張圖的畫法：**NFP** 畫月增長條 ＋ 三個月移動平均（月增是水準值一階差分，**BLS 每期會修正前兩個月，圖註要寫明本圖為最新一版**）；**CPI 與 PCE** 都畫總體與核心年增率，並附 2% 目標線。

### 三條硬規則

- **五張圖不得有兩張落在同一個 `theme`。** 主題撞車就換掉排序較後的那張。
- **`slot` 1、2、4 必須是當天的事**，不可以拿舊題材充數。素材真的不足時，正確做法是把該 slot 改成「前瞻框架」並在 `about.run` 如實記錄，**不是硬湊**。
- **`slot` 3（重製圖）放寬到七天內**，但**我們的重畫必須用最新資料，話要接到今天**。原題可以是三天前的專題，我們的圖不行是三天前的圖。
- **`slot` 3 的 `provenance.inspired_by` 必填**（媒體、標題、URL、`published`），並在 `about.run` 寫出原文日期。這是我們與轉載的分界線，也是日後回頭檢查「我們當初看對了沒」的依據。

### 重製圖的取材定義（2026-08-11 改寫，原本是四個專欄的白名單）

**判準是「論證主體是不是資料或圖表」，不是「刊在哪個專欄」。** 一篇一般新聞若核心是一組數字的走勢，可以取；一篇 Graphics 專欄若通篇是敘事與訪談，反而不適合——**我們要的是可以自己重算的論點，不是漂亮的版面**。

- **優先掃描**（品質穩定，值得每天看一眼）：**Charts of the Day**（`chartsofthe.day`，每期約五則帶圖論點，最好抓）、Bloomberg Graphics、FT Visual & Data 與 LEX、Economist Graphic Detail、Reuters Graphics、**DB Research Chart of the Day**（Jim Reid，**2026-08-03 起停更至九月，見 §3.3**）。
- **彙整型來源要往上追一層**：`chartsofthe.day` 是策展型電子報，它的圖多半出自第三方（摩根大通、Bloomberg 等）。`inspired_by.outlet` 記彙整方，**但論點的原始出處看得見時要一併寫進 `title` 或 `note`**——不然日後回頭查「我們當初看對了沒」會查到轉手的那一層。
- **不要讓 slot 3 變成別人電子報的翻譯。** 這幾個來源本身就是「每日一圖」產品，天天照它的第一則做，我們就只是譯者。**取它的題目，但問題要自己重新提**——它問「歐股廣度是不是很強」，我們該問的可能是「這個廣度用我們算得出來的指標看，站在歷史的哪個位置」。
- **同樣合格**：各家的一般財經報導、專欄、研究機構與官方部落格，只要論證由資料撐起來。
- **一律不使用付費牆內的圖或數據**（見 §3.1）。取論點沒問題，把對方的數字當我們的資料來源不行。

**為什麼改**：原規格點名四個視覺專欄，但 2026-08-05 至 08-11 **連續七期沒有任何一期真的用到它們**，實際引用的是一般新聞、Lex、Yahoo 自製圖與 Investing.com。原因有兩層——Economist 與 Reuters 的 graphics 路徑在抓取層被封鎖、Bloomberg Graphics 是前端渲染要走瀏覽器（見 §3.3）；更根本的是**那些專欄是週到月頻的長篇專題，餵不動一個日頻的欄位**。**規格與實作連續七天不一致而沒有人擋，代表那條規格從一開始就不可執行**，該改的是規格。

---

## 3. 取數規範

### 3.1 允許的來源

| 來源 | 用途 | 取法 |
|------|------|------|
| **FRED** | 總經、利率、利差、信用、匯率、通膨預期 | `tools/fetch.py` 的 `fred()` |
| **FRED（三大月度數據）** | 非農 `PAYEMS`／CPI `CPIAUCSL`＋`CPILFESL`／PCE `PCEPI`＋`PCEPILFE` | `tools/macro_release.py`：`--check` 偵測今日是否剛發布（讀 `last_updated`，**不靠行事曆**）、`--build` 產出備妥序列的圖 |
| **Yahoo Finance** | 指數、個股、期貨、匯率日收盤 | `tools/fetch.py` 的 `yahoo()` |
| 世界銀行／IMF／EIA | 跨國、能源實體數據 | 官方 JSON／CSV 端點 |
| **台灣證交所、櫃買（日收盤）** | 加權指數、上市與上櫃個股日線 | `tools/fetch_tw_price.py`（**免金鑰、官方端點**）。`fetch.py` 會自動路由：`.TW`／`.TWO`／`^TWII`／`TAIEX` 一律走這裡，不再走 Yahoo |
| **Tiingo** | 美股個股與 ETF 日線（`GLD`／`XLE`／`MU`／`STX` 等） | `fetch.py` 的 `tiingo()`。**免費方案每日 1,000 次、每小時 50 次、歷史 30 年以上**。key 放 `TIINGO_API_KEY` 或 `~/.config/tiingo/api_key`，**不進版控**；`python3 tools/fetch.py --check-tiingo` 自我檢查 |
| 台灣證交所、櫃買、央行、主計總處 | 台股籌碼、台灣總經 | `tools/fetch_twse.py`，`bfi82u`（三大法人）與 `margin`（融資融券）**均已實測可用**。三大法人是**六列要聚合**；融資金額單位是**仟元**（→億元除 1e5）。**日期送錯不會報錯，會安靜回最近一個交易日**，務必核對回傳 `title` |
| 公司財報、法說簡報 | 個別公司數字 | 原始文件，不引用二手轉述 |

**一律不使用付費牆內的圖或數據。** 讀了 Bloomberg／FT 的文章拿到論點沒問題，把他們的數字當成我們的資料來源不行——因為我們無法驗證，也無權轉載。

**FRED 的兩條路徑**：`fetch.py` 會自動選——讀得到 API key 就走官方 API（`api.stlouisfed.org`，文件化、120 req/min），讀不到就退回繪圖端點（`fredgraph.csv`，免認證但非文件化 API）。key 從 `FRED_API_KEY` 環境變數或 `~/.config/fred/api_key` 讀取，**不寫進程式碼、不進版控**（`.fred_key` 與 `.env` 已列入 `.gitignore`）。所有對外輸出都經過 `_redact()`，key 不會出現在 log 或錯誤訊息裡。
用 `python3 tools/fetch.py --check-key` 可自我檢查。

### 3.2 三個實測過的限制（不要重踩）

0. **`fredgraph.csv` 在發布機上連不通（2026-08-08 `--check-key` 實測：`[Errno 60] Operation timed out`），而 `api.stlouisfed.org` 正常。** 上面把繪圖端點寫成「沒有 key 時的退路」，但**在這台機器上退路才是壞的那條**——所以 **FRED API key 不是「有比較好」，是 FRED 這條線的唯一活路**。同一輪也確認官方 API 的 `DGS10` 給 16,134 筆（回到 1962 年），遠多於繪圖端點。**若某輪回報「無 API key，走 fredgraph 退回端點」，先確認那輪跑在沙箱還是發布機**——沙箱讀不到家目錄的 key，兩者環境不同，別誤判成 key 掉了。
0.5 **CPI 永久缺 2025-10——政府關門那個月沒有調查，恢復後也不補。** 這不是取數失敗，是**資料本身就沒有**。實測對照：

   | 序列 | 2025-10 | 為什麼 |
   |---|---|---|
   | `CPIAUCSL`／`CPILFESL`（BLS） | **✗ 永久缺** | **價格必須當下去店裡採集**，事後補不回來 |
   | `PAYEMS`（BLS） | ✓ 158,408 | 就業來自薪資行政紀錄，事後可重建 |
   | `PCEPI`（BEA） | ✓ 127.871 | BEA 用其他來源推估。**但那個月的編製方式與其他月不同**，引用時值得一提——聯準會最看重的通膨指標裡有一個估算月 |

   **能不能事後補，取決於資料是「即時觀測」還是「行政紀錄」。** 同一次關門，只在必須當下觀測的那條留下永久的洞。

   **兩個會咬人的時點**：CPI 月增在 **2025-11** 已經沒有前月；CPI 年增在 **2026-10** 會沒有基期——**大約兩個月後就會遇到**。`build_series` 會讓這兩點斷開而不是硬接，**圖上出現斷點時，`note` 必須說明原因**，否則讀者只會覺得圖壞了。

1. **FRED 一次只能抓一條序列。** `fredgraph.csv?id=A,B` 會回傳 **zip**，不是 CSV。`fetch.py` 已對 `PK` 開頭做偵測並拋錯。
1.5 **同樣是 429，成因可能完全不同——處置也不同。** 2026-08-05 那次是**累積型限流**（連打數十次後才出現，持續逾半小時），規範因此訂為「等，不要繞」。但 2026-08-14 本機首跑是**第一個 Yahoo 請求就 429**，前面只打過三次 FRED——**沒有累積，等再久也不會好**。

   兩者的分辨方式：**看第一次請求就 429，還是打了一陣子才 429**。前者不是流量問題，重跑無效。

   不論哪一種，**都不可以換 host、改用瀏覽器去規避**。但也**不該對一個明確說「不」的站台連敲十幾次**——`prefetch.py` 已加熔斷：同一來源連續 3 次被限流就停止再試，其餘記為 `skipped`（未嘗試）而非 `failed`。**連敲十三次不是「等」，是敲。**

   **Yahoo 不通時的正當替代是 FRED 的官方 API，不是繞道。** 多數常用序列在 FRED 都有等價序列，而那是有文件、有認證的來源——brief §3.1 本來就偏好它：

   | Yahoo | FRED 等價 | | Yahoo | FRED 等價 |
   |---|---|---|---|---|
   | `^VIX` | `VIXCLS` | | `JPY=X` | `DEXJPUS` |
   | `^GSPC` | `SP500` | | `^N225` | `NIKKEI225` |
   | `^IXIC` | `NASDAQCOM` | | `BZ=F` | `DCOILBRENTEU` |
   | `^IRX` | `DTB3` | | `^TNX`／`^TYX` | `DGS10`／`DGS30` |

   **代價是第 3.2 節已記載的那條：FRED 日頻序列比 Yahoo 慢一個交易日。** 所以「講當日事件反應」的圖仍應優先用 Yahoo；等價序列是 Yahoo 不通時的底，**用了就要在 `note` 寫出基準日**。台股個股、`GLD`／`XLE`／`MU` 這類美股個股與 ETF **沒有 FRED 等價**，只能走 Yahoo 或證交所。

1.6 **台股官方端點的四個特性（2026-08-14 實測）。** 證交所與櫃買免金鑰、有文件，**優先於 Yahoo**：

   - **一次回一整個月，不是單日。** 要兩年就得打 24 次、每次間隔一秒。這其實比 Yahoo 省事，但別誤以為一次請求就有全部歷史。
   - **日期是民國年**（`115/08/14` ＝ 2026-08-14），要 +1911。**轉換錯了不會報錯，只會讓整條序列偏到 1911 年代。**
   - **櫃買的成交量欄位名稱會隨年代改變**：近年是「成交張數」，2016 年是「成交仟股」。**要讀回傳的 `flagField` 判斷單位，不能寫死欄位名或倍率**——這與融資餘額「仟元 vs 元」是同一類坑，差 1,000 倍而數字看起來都像個數字。
   - **櫃買對資料中心 IP 節流很兇**，被節流時**回空 body 而不是錯誤碼**。空不是「沒有資料」，是被擋；`fetch_tw_price._get()` 會重試並在最後誠實拋錯，**不回空序列**。

1.7 **指數沒有免費來源，只能用 ETF 代理，而且要標明。** 查證過的六家免費方案（Tiingo、Twelve Data、Alpha Vantage、Polygon、Finnhub、Nasdaq Data Link）**沒有任何一家提供 `^SOX`**——這是指數授權限制，不是技術問題。

   | 想要的指數 | 代理 | 為什麼 |
   |---|---|---|
   | `^SOX` 費城半導體 | **`SOXQ`** | 直接追蹤 PHLX Semiconductor Sector Index（`SOXX` 追的是另一個 ICE 指數，較不貼近） |
   | `^STOXX50E` | `FEZ` | 追蹤歐洲 Stoxx 50 |

   | `HG=F` 銅期貨 | `CPER` | 追蹤銅期貨（**ETF 有轉倉成本，與期貨本身不同**） |

   **用了代理就要在 `note` 寫明「這是 ETF 不是指數／期貨」**，比照既有的 XLE 註記。ETF 有追蹤誤差與費用率，**長期報酬會與原標的分岔**，講「指數漲了多少」時尤其要小心。

   **代理必須在 `series_spec` 明寫 ETF 代號，不由取數層自動替換。** 曾經寫成「`^SOX` 取不到就改抓 `SOXQ`」，那會把 ETF 的資料寫進 `^SOX` 的快取，**圖上標的是指數、數字卻是 ETF**——正是這條 `note` 規定要防的事。**替換要是一個有意識的決定，不是取數層的靜默行為。**

2. **ICE BofA 利差序列（`BAMLxxx`）只有近三年。** 這不是端點限制，是 FRED 的政策——序列頁面明載「Starting in April 2026, this series will only include 3 years of observations.」，資料本身受 ICE Data Indices 版權保護。**申請 FRED API key 不會解決這件事**，換 API 端點也不會。因此凡是用到 HY／IG OAS 的圖，分位數與中位數一律標明「近三年」，**不可以寫成「歷史低點」**。要更長的歷史只能改用其他來源（如 ICE 官方或 Bloomberg 授權資料）。

### 3.3 取數路徑：主路徑、預抓、與備援

**三條路徑，優先序由上而下。每天都要在 `about.data_path` 寫下實際走了哪一條**（`direct`／`prefetch`／`browser`）——單日降級不嚴重，**連續降級才是故障，而連續性只有被記錄下來才看得見**。

| 路徑 | 什麼時候用 | 前提 |
|---|---|---|
| **`direct`（主路徑）** | `tools/fetch.py` 直連 FRED／Yahoo／證交所 | 執行環境對外連線正常 |
| **`prefetch`（本機預抓）** | 讀 `data/series` 快取，由 Mac 上的 `com.kenny.chartfetch` 每日 11:00 刷新 | launchd 有在跑；**只涵蓋核心清單 ∪ 近 14 天用過的序列**，當天臨時想到的新序列抓不到。**用前先看 `data/_prefetch_status.json` 的 `failed` 與 `skipped`——某條在裡面就代表快取是舊的** |
| **`browser`（備援）** | 前兩條都拿不到時 | **需要有 Chrome 執行個體在線並經人工指定——無人值守輪次實際上不可用** |

**2026-08-06 起沙箱對外連線被出口代理擋掉**（FRED／Yahoo／證交所一律 Tunnel 403，只有 pypi 通），至 08-14 已連續九期靠 `browser` 取數。**08-14 11:30 那輪因為當下沒有 Chrome 在線而完全沒有產出**，13:44 有人在場重試才過。

**這件事真正的教訓不在那天的失敗，在前八天的成功。** 每天都「降級但成功」，於是一個持續九天的結構性故障從來沒有被升級成問題——**降級若每天都成功，就沒有人會把它當故障處理**。`check_day` 已加連續降級累計警示（`DEGRADED_STREAK_WARN`），連續三期未走主路徑就會大聲說出來。

**本機預抓的安裝（一次性，在 Mac 上做，不在沙箱）：**

```
mkdir -p ~/.chartfetch
cp ~/chart-of-the-day/tools/prefetch-launchd.sh ~/.chartfetch/prefetch.sh
chmod +x ~/.chartfetch/prefetch.sh
cp ~/chart-of-the-day/tools/com.kenny.chartfetch.plist ~/Library/LaunchAgents/
launchctl load ~/Library/LaunchAgents/com.kenny.chartfetch.plist
python3 ~/chart-of-the-day/tools/prefetch.py
```

最後一行是手動驗證，會印出每條序列的點數與末日，並寫出 `data/_prefetch_status.json`。
**沒有狀態檔＝預抓從未跑過**，`check_day` 會這樣報，不會假裝一切正常。
`launchctl list | grep chartfetch` 看有沒有載入；日誌在 `~/.chartfetch/prefetch.log`。

**先分清楚兩種「失敗」——處置完全相反：**

| 狀況 | 特徵 | 正確處置 |
|------|------|---------|
| **網路被擋** | 連線逾時、DNS 失敗、代理拒絕 | 走下面的瀏覽器同源備援 |
| **被限流（HTTP 429）** | 對方明確回 429 | **等，不要繞。** `_get()` 會自動退避 20／40 秒 |

**429 不是障礙，是對方在說「慢一點」。** 用瀏覽器、換 host、換來源去繞過速率限制，是把別人的營運規則當成技術問題處理——我們不做這種事。若 429 持續到執行時仍未解除，正確做法是**該圖降級或改題，並在 `about.run` 如實寫明「Yahoo 限流，某某序列未取得」**。寧可誠實少一張圖，也不要繞。

**圖表專欄的取用性（2026-08-11 實測，逐一測過）：**

| 來源 | 一般抓取 | 處置 |
|---|---|---|
| **Bloomberg Graphics**（`/graphics`） | **回空殼**（前端渲染，不是被擋） | **走瀏覽器**：`navigate` 後 `get_page_text`。索引頁免登入即可讀到圖題、一句話論點與日期，**這就是「只取圖題與論點」所需的全部** |
| Economist Graphic Detail | **抓取層封鎖** | 不要繞。改用該刊其他可取得的文章，或換來源 |
| Reuters Graphics | **抓取層封鎖** | 同上 |
| **Charts of the Day**（`chartsofthe.day`） | **可取，最好抓** | 首頁即封存索引（標題＋副標＋日期）；`/p/<slug>` 個別文章**免登入可讀全文**，每期約五則，各有標題、論點與圖。**實測是這批來源裡最省事的一個** |
| **DB Research CoTD**（`dbresearch.com`） | 可取，但**頁面很大**（實測約 58,600 字元） | 清單頁有標題、日期、分析師與導言片段，**通常夠用**，不必再抓個別 `.report`。**抓之前先想清楚要什麼**，整頁拉進來很貴 |
| FT Lex／一般財經報導 | 可取 | 照常 |

**兩個來源的更新頻率（2026-08-11 實測，與直覺不同，記下來免得誤判「今天沒更新＝壞掉」）：**

- **`chartsofthe.day` 不是每日。** 封存實際為 08-11、08-07、08-06、08-05、08-04、07-29、07-28、07-24、07-23、07-17…—**每週約三到四期，週間也會跳日**。當天沒有新的一期是常態，不是取數失敗。
- **DB Research CoTD 目前停更。** 最新一期 2026-08-03 的內文明寫「This will be the last CoTD until September」（作者 Jim Reid 休假撰寫年度長期資產報酬研究）。停更前是**週間近乎每日**（07-20、21、22、23、24、27、28），不是每週一次。
  **它現存最新的一期已經超過 slot 3 的七天上限**，所以在九月復刊之前，這個來源實際上取不到可用題材——**掃到沒有新東西是預期行為，不要當成故障排查**。**九月第一週回頭確認是否復刊**（MAINTENANCE §4 已列）。

**回空殼與被封鎖是兩件事，處置相反**：空殼代表頁面靠 JavaScript 產生，換瀏覽器就讀得到；封鎖代表不該繞，換來源。**把前者誤判成後者，等於白白放棄一個可用的來源——Bloomberg Graphics 就這樣被閒置了七天。**

公司網路或代理擋掉 Python 直連時，改由瀏覽器**同源** fetch：

- **FRED**：先 `navigate` 到 `https://fred.stlouisfed.org/series/<ID>`，再 `javascript_tool` 執行
  `await fetch('/graph/fredgraph.csv?id=<ID>&cosd=2015-01-01').then(r=>r.text())`
- **Yahoo**：先 `navigate` 到 `https://finance.yahoo.com/quote/<SYM>`，再 fetch
  `https://query1.finance.yahoo.com/v8/finance/chart/<SYM>?range=5y&interval=1d`
  （Yahoo 的 CORS 只允許 `*.yahoo.com` 來源，從 FRED 頁面抓會被擋。）
- **證交所**：先 `navigate` 到 `https://www.twse.com.tw/`，再同源 fetch `/rwd/zh/fund/BFI82U?dayDate=...&response=json`。
  **2026-08-11 實測同源可用，一次取得 57 個交易日。** 先前記載的「證交所 CORS 使瀏覽器備援無效」只對**跨源**成立（從 Yahoo 或 FRED 的頁面抓證交所會被擋），**同源沒有問題**——把限制寫得比實際寬，會讓人在真正需要備援時直接放棄一條可用的路。

**注意兩件實測到的事：**
- **不要把整包資料當成工具回傳值往外送**——回傳會被截斷（實測單一欄位約 1,000 字元）。要嘛在本機處理完，要嘛用 `a.download` 存成檔案再讀。
- **Chrome 的程式觸發下載配額是「每個分頁放行一次」，不是每個站台一次**（2026-08-08 修正實測）。同分頁第二次會被**靜默**擋掉，但**開新分頁重新導覽後配額重置**——所以要抓多條序列就一條開一個新分頁，不要在同一頁連抓。
- **`finance.yahoo.com` 的 CSP 一次都不放行**；但 `query1.finance.yahoo.com`／`query2.finance.yahoo.com` 可以。`fred.stlouisfed.org` 也可以。備援路徑只適合救急，正式流程請用本機直連。

### 3.4 資料品質檢查（強制）

`tools/render_day.py` 會對每條序列跑 `qa_series()`，抓出單日跳動超過 **5 個標準差**的點，寫進 `about.qa_flags` 並顯示在網站上。同一序列相鄰交易日的旗標會**合併成一筆事件**（記 `date_end` 與 `days`），因為 2025-04-03 與 04-04 是同一次關稅衝擊，拆成兩筆只會讓處置說明變流水帳。

**門檻為什麼是 5σ**：原本設 6σ，2026-08-05 覆核時發現它漏掉了 2026-01-30 黃金單日 **−11.37%**（z＝−5.59）。一根 11% 的棒子沒有任何機制看過一眼，就是門檻失職。改 5σ 後同一份資料由 2 筆增為 5 筆（已含合併），多出來的三筆都是已知真實事件。**多出來的不是雜訊，是本來就該被讀到的東西。**

**看到旗標不要直接忽略。** 它有三種可能，處置不同：

| 原因 | 特徵 | 處置 |
|------|------|------|
| 真實市場事件 | 有對應新聞（如 2025/04/03 關稅衝擊） | 保留，可在判讀中引用 |
| 期貨轉倉 | 出現在 `GC=F`／`BZ=F` 這類近月合約，**且幅度通常在 1% 以內** | 改用現貨或連續調整序列，或在圖註說明 |
| 來源錯價 | **隔日就跳回去**、無對應新聞 | 換來源重抓，**不要**照原樣出圖 |
| **衍生序列的窗口效應** | 滾動波動率、移動平均這類量；跳動呈**階梯狀**且與該日新聞無關 | 在 `series` 標 `"derived": true`。旗標會標記並改為**整條序列說明一次**，不必逐筆 |

**年增率（`yoy`）也算第四類，`build_series` 已自動標 `derived`（2026-08-13 加）。** 年增率是導出量，而 `qa_series` 抓的是相鄰觀測的百分比變動——2020-05 總體 CPI 年增只有 0.2%，次月 0.72% 就被算成 **+260%**（z＝7.4）。這與「不要餵原始報酬率給 `qa_series`」是同一個坑：**分母趨近零的導出序列**。序列本身正確、照常出圖，`about.run` 整條說明一次即可。

**第四類是 2026-08-06 那期逼出來的**：18 筆旗標裡有 13 筆全在「黃金 ETF 60 日波動率」這一條上。它既不是市場事件、不是轉倉、也不是錯價——是某一天的極端值滾出 60 日窗口，指標就跳一階。**原本的三分法沒有這一格，執行者只能硬塞進「真實事件」或逐筆寫罐頭說明。** 標了 `derived` 之後，檢查腳本只要求該序列在 `about.run` 被提到一次。**注意這不是豁免**：完全不提仍會警示，因為讀者需要知道那些階梯是什麼。

**判斷時先看幅度再看故事。** 黃金與原油的轉倉價差來自持有成本，**正常在 1% 以內、極少超過 2%**。所以「跌 11% 是轉倉」這種解釋在數量級上就不成立——2026-01-30 那筆的正確歸類是前兩類之一，而它之後停在 4,600–4,900 沒有跳回 5,300，**排除了「來源錯價」**（錯價的特徵是隔日還原）。**不要用轉倉當成不想查的藉口。**

### 3.5 序列新鮮度（拿兩天前的收盤當「今天」講）

第 7 節檢查腳本會列出每條序列的最後日期與距當期日期的天數：

- **落後 ≥ 2 天**：警示。**凡是以該序列算出的「今天」數字，`subtitle` 或 `note` 必須寫出實際基準日。**
- **落後 ≥ 5 天**：硬失敗，不得發布。

**日頻與月頻分開判（2026-08-13 加）。** 上面兩條門檻是為日收盤序列訂的。但第 2 節要求三大數據發布日 slot 1 必畫該數據，而 CPI／PCE／非農都是**月頻**——7 月的觀測值標記為 `2026-07-01`，在 8 月 13 日那輪一律落後 43 天，**照日頻門檻算是硬失敗，等於同一份規範自己禁止自己**。月頻改看**落後幾期**：≥ 2 期警示（`subtitle` 或 `note` 要寫出參照月份）、**≥ 3 期硬失敗**（那代表漏掉了一次以上的官方發布）。判準與 `build_series._is_monthly` 相同：日期全部以每月 1 號標記。`window.note` 的雙向比對仍涵蓋月頻末日，所以月頻的 `MM-DD`（如 `07-01`）也要寫進 note。
- **`window.note` 必須與實際末日相符。** 腳本會雙向比對——note 提到卻沒有序列以該日結束、或有序列末日卻沒寫進 note，都會警示。**metadata 寫錯比不寫更糟，它讓讀者以為資料是新的。**

這條規則來自 2026-08-05 的實例：`^SOX` 序列止於 08-03，卻用來算「自高點跌幅 −21.9%」並放在 08-05 那期，而 `window.note` 還寫著「行情序列最新至 2026-08-04／08-05」——**根本沒有任何序列以 08-04 結束**。當時圖註把與媒體數字的落差歸因為「盤中／不同基準日」，但真正的第一嫌疑是**我們的資料落後了兩個交易日**。

---

## 4. 內容規範

| 欄位 | 長度 | 要求 |
|------|------|------|
| `title` | 12–30 字 | **是一個判斷，不是一個標籤。**「台股補上了費半掉下來的那一段」而不是「台股與費半走勢比較」 |
| `subtitle` | 一行 | 畫的是什麼、期間、單位、基期 |
| `takeaway` | ≤ 70 字 | 一句話結論，帶數字 |
| `reading` | 200–620 字，分 3 段 | 第 1 段講圖上看到什麼（帶數字）；第 2 段講為什麼會這樣，**並給至少一個歷史錨點**（見下「歷史縱深」）；第 3 段講這裡面有什麼是市場還沒想清楚的 |
| `so_what` | 60–120 字 | 對投組的意涵。不准寫「須密切關注」這種空話 |
| `watch` | 1–3 條 | **可被驗證的觸發條件**，要有數字或事件名 |
| `tags` | 2–5 個 | 供跨期檢索 |

> 這張表是 `tools/check_day.py` 的散文版。**該檔是唯一可執行的權威副本**，這裡的數字必須與它逐字相符。
> 改任何一個長度數字，**本節、`tools/check_day.py`、排程 prompt 三處要同時改**——只改散文版不會有任何效果，只改腳本則會讓寫作者依錯的規範下筆。

**另外三條檢查的觸發條件（寫在這裡，就不必為了避開警示去讀腳本）：**

1. **`window.note` 的日期集合必須等於各序列末日的集合**，雙向比對——note 提到卻沒有序列以該日結束會警示，有序列末日卻沒寫進 note 也會警示。**比對用 `MM-DD`**，所以 note 裡寫「2026-08-04／08-05」這種簡寫也算數。
2. **判讀文字裡的 `M/D` 會與該圖「最短那條」序列的末日比對**（掃 `title`／`subtitle`／`takeaway`／`reading`／`so_what`，**不掃 `watch`**）。超過就警示——那是提示不是指控，不同序列末日本來就可能不同，**但你要在 `note` 說明各序列末日**。
3. **頁尾 `source` 與 `note` 合計超過 3 行是硬失敗**，每行以 120 視覺寬計（中文字算 2、拉丁字元算 1）。剛好 3 行會警示。

〔2026-08-09 補。`check_day.py` 外移的省量原本沒兌現，因為執行者為了避開警示只能去讀腳本——**規範沒把觸發條件寫清楚，等於把腳本變成必讀文件**。〕

### 歷史縱深：每張圖至少一個歷史錨點（2026-08-10 新增）

讀者要的不只是「現在是多少」，是**「這個位置在歷史上像什麼、上次走到這裡之後發生了什麼」**。範本是 2026-08-10 鴻海講稿的寫法——「上一次投資接手當主角，要回到 1990 年代中後期」接著馬上補「**但我這樣類比不是說現在等於 1999 年，關鍵差別是**當年靠高收益債融資、這一輪是現金流最強的公司花自己的錢」。

兩種錨點，紀律不同：

1. **數字型**（上次觸及這個水位、十年第幾分位、第 N 次出現）——**一律從自己的序列算出來，並寫出該次的日期**。序列就在 JSON 裡，`上一次 60 日波動率站上 93 分位是 2020-05` 這種話是算得出來的，**不要用記憶回想**。算出來的那個時點，**若落在圖的窗口內就加一個 `marker` 標在圖上**（如 08-06 黃金波動率那張的「疫情期高點 1.73」——那就是標準做法）。**但 `marker` 只有日期軸的圖型畫得出來**——`timeseries` 與 `range_area` 可以，`waterfall`／`grouped_bar`／`stacked_bar`／`pct_stacked_bar`／`heatmap`／`gauge` 的 x 軸是類別，在它們身上「某個日期畫一條垂直線」沒有意義，檢查腳本會擋下來。**用類別軸圖型時，錨點改寫進 `note`**，不要硬塞 `marker`。
2. **事件型**（類比某段歷史、某個機構的行為模式）——只能用**重大、可公開查證的事件，必須帶年份**；且**類比必須成對出現：相同點＋關鍵差異**。只說「現在很像 1999」是偷懶也危險；「像在哪、不像在哪」才是分析。**寫不出關鍵差異，就把這個類比刪掉。**

**三條禁令：**

- 禁止無出處的「歷史上通常」「經驗上」——**說不出是哪一次的，就不是歷史，是印象**。
- 禁止用類比替代機制：類比是輔助理解，第 2 段的主體仍然是「為什麼會這樣」的因果。
- **要求歷史深度最常換來的是編造的歷史。** 檢查腳本會做一個弱檢查：`reading` 出現「上一次／上次／歷史上／前例」卻整段找不到年份（19xx／20xx），就警示。這攔不住所有編造，但攔得住最懶的那種。

### 寫作要求

- 全程**繁體中文（台灣用語）**。
- **數字一律來自我們自己算出來的序列**，不是抄新聞。若我們算出的數字與媒體引用不一致，**兩個都寫，並說明計算基準**——這正是重製的價值。
- **不要引用本圖序列涵蓋不到的日期。** 第 7 節檢查會掃判讀文字裡的 `M/D`，與該圖**最短那條序列**的末日比對並點名。它只是提示不是指控——一張圖裡不同序列末日可能不同，日期落在較長那條的範圍內是合理的，**但你必須在 `note` 說明各序列的末日**。若那個數字其實是抄來的，刪掉它。（`watch` 不在掃描範圍，觸發條件本來就該指向未來。）
- 允許有觀點，但觀點要能被證偽。凡是寫「可能」「或許」的地方，問自己：**怎樣才算錯？** 寫不出來就刪掉那句。
- 不要用 emoji。不要用「驚人」「暴漲」這類形容詞去補數字說服力不足的地方。

### 圖型跟著問題走——每期至少一張非折線（硬性）

**先想這張圖要回答什麼，再挑圖型。** 2026-08-09 量測：前五期 25 張圖有 **24 張是折線**，53 條序列裡 52 條是線。而 `bar` 早就支援，**五天一次都沒被用過**。根因不是圖型不夠，是**選題角度單一**——25 個標題幾乎都在問「A 和 B 怎麼分歧了」，而兩條線疊起來確實是回答那個問題最直接的形式。

| 這張圖要回答 | 用 `kind` | 資料欄位 |
|---|---|---|
| 兩三個量隨時間怎麼走、怎麼分歧 | `timeseries` | `series` |
| **這個變化是誰貢獻的** | **`waterfall`** | `cats`＋`vals`＋`total_label` |
| **同一組對象在兩個口徑下的對比** | **`grouped_bar`** | `cats`＋`groups` |
| **組成隨時間怎麼變** | **`stacked_bar`**（佔比用 `pct_stacked_bar`） | `cats`＋`groups` |
| **現在落在歷史區間的哪裡** | **`range_area`** | `band`＋`band_label`＋`series` |
| **一整面的關係，不是兩兩** | **`heatmap`** | `cats`＋`rows`＋`matrix` |
| **單一數字距離門檻多遠** | **`gauge`** | `gauge{value,lo,hi,ref,ref_label}` |
| 兩個變數的分布與離群 | `scatter` | `pts`＋`hi_pts` |

**檢查腳本會硬性擋下「五張全折線」。** 但**若當天真的每個題目都是兩條線的關係，那要換的是選題角度，不是硬換圖型**——為了滿足規則把不適合的資料塞進雷達圖，比五張折線更糟。

**四角色調色盤仍然適用**：`stacked_bar` 與 `grouped_bar` 最多四個分項，第五個會繞回主角紅。**分項超過四個就把小的併成「其他」**，那本來也是比較好讀的圖。唯一的例外是 `heatmap`——它的顏色編碼的是量值大小而非論證角色，用四個離散角色表達連續量會讀不出來，所以走藍↔紅發散或單色斜坡。

**`waterfall` 刻意不用紅綠分正負**：增減方向已經由長條的位置與資料標籤表達，紅色留給最後那根合計（`takeaway` 講的就是它），增用對照藍、減用背景灰。這與下面「正負值不分色」是同一條規則。

### 圖表配色：四個角色，不是六個編號

**顏色回答的是「你在這張圖的論證裡是什麼角色」，不是「你是第幾條序列」。** 每張圖都有 `takeaway` 與 `so_what`，也就是每張圖都在講一句話——顏色的工作是讓人一眼看出那句話講的是哪一條線。

| 角色 | 色 | 白底對比 | 用在哪 |
|---|---|---|---|
| **主角** | **#D70C18** 品牌紅 | 5.3:1 | `takeaway` 在講的那條線／今日點。**一張圖只有一個** |
| **對照** | **#1F4E79** 深藍 | 8.7:1 | 前期、對手、共識 |
| **溢出** | **#8A8F95** 中灰 | 3.3:1 | 第三條。用到就代表該考慮拆圖 |
| **背景** | **#B8BBBE** 淺灰 | 1.9:1 | 其餘所有東西。**低對比是刻意的**，它的工作是退到後面 |

**`series[].color` 預設留空**，`chartkit` 會照上表順序給（`PALETTE[i % len]`）。**只有要指定誰是主角時才寫死**——例如 `jpy-vs-ust10` 那張刻意讓第二條（美 10 年期殖利率）當主角、第一條當對照，那就要寫。

2026-08-05 那期原本九條序列 100% 手寫了 color，其中**七條的值等於程式本來就會給的預設**（白工，且讓換配色從改一行變成逐條手改），只有 `jpy-vs-ust10` 那兩條是有語意的指定。已依此規則清空七條、保留兩條。

**序列最多四條。** 第 5 條會 `PALETTE[4 % 4]` 繞回主角紅，一張圖出現兩個主角——第 7 節檢查腳本會硬失敗。用到第 4 條（背景色）時腳本會警示，要確認那條真的只是背景。**超過就拆圖，不是換色。**

**土黃 #D9942B、綠 #3F7D5C、紫 #7A6BA8 已移出工作集。** 土黃對白底只有 2.6:1，2pt 細線會偏淡；綠與主角紅在紅綠色盲下只差 17。更重要的是：**一張圖需要第 5、6 個顏色時，該修的是圖不是色。**

**正負值不分色。** 一律靠 `zero_line` 與資料標籤表達，紅只保留給主角。理由有三：原本的 `POS`／`NEG` 是死碼（全 repo 從未被引用）；`NEG` 的值與 `ACCENT` 完全相同，真用起來紅會同時代表「重點」與「下跌」；而綠正紅負是西方慣例，與台灣的紅漲綠跌相反。

> **主角紅為什麼是 #D70C18。** 舊值 #C8102E 的註解寫著「台新紅」，但那不是台新紅——那接近 Pantone 186，是當初挑錯的。品牌紅取自 `tsholdings.com.tw` 的 logo SVG `fill`（全站計算樣式 86 處）。
>
> 另有第三個紅：**#C00000 是 PowerPoint 標準色盤的 Dark Red**，House View 月報的模板用的是那個。三者兩兩相距 ΔE 6～8，**全都落在「太近讀不出是刻意的、太遠又不像同一個顏色」的最糟區間**。
>
> **這一套與 House View 月報是同一套**（`~/houseview/HOUSEVIEW_BRIEF.md` 第 5 節）。月報會直接拿本庫的 PNG 貼進簡報、也會用本庫的 `series` 自己重畫，兩邊不同色就會在同一頁上看出破綻。**Houseview 的健康檢查會讀本檔的 `ACCENT` 比對，改這裡就會讓那邊 FAIL——那是設計成這樣的，改完要去更新那邊的登記值。**

---

## 5. JSON schema（`data/YYYY-MM-DD.json`）

```jsonc
{
  "date": "2026-08-05",
  "weekday": "週三",
  "headline": "當日總標題（20–30 字，一個判斷）",
  "standfirst": "導言 60–100 字，串起五張圖的共同線索",
  "window": { "data_asof": "2026-08-05T10:40+08:00", "note": "各序列最新日期不一致時在此說明" },
  "about": {
    "upstream": ["advisory-knowledge-hub/data/2026-08-05.json", "..."],
    "run": "本輪執行紀錄：降級、替換、待覆核事項",
    "qa_flags": [],                      // 由 render_day.py 自動寫入，不要手改
    "macro_release": []                  // `macro_release.py --check` 的原樣輸出，每天都要寫
  },
  "charts": [{
    "slug": "risk-premium-unwind",       // 檔名用，英文小寫連字號
    "slot": "當日主圖",
    "theme": "能源與原物料",              // 必須是投顧儀表板 15 個子類別之一
    "title": "...", "subtitle": "...",
    "kind": "timeseries",                // timeseries | scatter
    "y_label": "...", "y2_label": "...",
    "y_fmt": "{:,.0f}", "y2_fmt": "{:,.2f}",
    "y_log": false,                      // 利差、倍數這類「比例才有意義」的量請開 true
    "zero_line": false,
    "source": "資料來源：...（含期間）",
    "note": "計算基準、限制、取材出處",
    // ↓ 寫 series_spec，讓 build_series.py 從快取實體化 series——**不要親手打資料點**
    //   （手打一天 ~34,000 字元，而那些數字機器本來就有；量測見 2026-08-09 變更紀錄）
    "series_spec": [{ "id":"BZ=F", "name":"布蘭特原油", "t":"rebase", "since":"2026-01-02" }],
    "series_align": true,                // 跨市場比較必開：先取交易日交集再轉換
    "series": [ /* build_series.py 寫入；spec 與 series 並存，轉換方式因此可考 */ ],
    // ↓ 非折線圖型的欄位（依 kind 擇一組，見第 4 節「圖型跟著問題走」）
    "cats": ["住房","食品","能源"],      // 類別軸：waterfall／grouped_bar／stacked_bar／heatmap
    "vals": [1.62, 0.41, -0.55],         // waterfall 的各項貢獻
    "total_label": "總體年增",            // waterfall 末端合計欄；空字串＝不畫
    "groups": [{"name":"今年以來","values":[11.2,17.0]}],   // grouped/stacked，**最多四組**
    "band": [["2026-01-01", 1.2, 4.9]],  // range_area：[日期, 下緣, 上緣]
    "band_label": "近十年區間",
    "rows": ["標普500","美債"],           // heatmap 列標籤
    "matrix": [[1.0, 0.32], [0.32, 1.0]],
    "gauge": {"value":93,"lo":0,"hi":100,"ref":50,"ref_label":"中位數"},
    "markers": [{ "date":"2026-03-31", "label":"油價見頂" }],
    "pts": [], "hi_pts": [],             // kind=scatter 用
    "provenance": { "inspired_by": { "outlet":"Bloomberg", "title":"...", "url":"...", "published":"2026-08-09" } },
    // about 另有 "data_path": "direct" | "prefetch" | "browser"（見 §3.3，每期必填）
    "takeaway": "...", "reading": "...\n\n...", "so_what": "...",
    "watch": ["...", "..."], "tags": ["..."],
    "files":  {},                        // render_day.py 寫入
    "option": {}                         // render_day.py 寫入（ECharts）
  }]
}
```

**`series` 一定要把資料點寫進 JSON**（「歷史可查閱」的實作方式），但**寫入的動作交給 `build_series.py`**：執行者寫 `series_spec`（代號＋轉換），工具從 `data/series/` 快取實體化。轉換有 `raw`／`rebase`／`diff`／`yoy`／`ma:N`／`vol:N`，**`ma` 與 `vol` 會自動標 `derived`**（不會再忘）。手工 `series`（無 spec）仍合法，工具不碰。

---

## 6. 每日執行步驟

1. **查三大數據**：`python3 tools/macro_release.py --check`，輸出原樣寫進 `about.macro_release`。有 `fresh` → slot 1 就是它（第 2 節）。
2. **讀上游**：`~/advisory-knowledge-hub/data/<今天>.json`。不存在就**等 15 分鐘再讀一次**；仍無則改用前一日檔案並在 `about.run` 註明。
3. **掃圖表專欄**：Bloomberg Graphics、FT Visual & Data／LEX、Economist Graphic Detail、Reuters Graphics。**只取圖題與論點**，記下 outlet／title／url。
4. **選題**：依第 2 節五個 slot 各挑一個，檢查 `theme` 不重複。
5. **取數**：`python3 tools/fetch.py <ids...>`（進快取即可，數字用來寫判讀）。
6. **寫 JSON**：依第 5 節 schema 寫 `data/<今天>.json`——**`series` 用 `series_spec`，不要手打資料點**。
7. **實體化序列**：`python3 tools/build_series.py <今天>`。
8. **算圖 + 檢查**：`python3 tools/render_day.py <今天>`，QA 旗標依第 3.4 節處置。
9. **更新索引**：`python3 tools/rebuild_index.py`。
10. **發布前檢查**：`python3 tools/check_day.py`，全綠才發布。
11. **發布**：**直接用檔案工具寫入即可，絕對不要跑任何 git 指令（含 `git status`）。** 本機 `com.kenny.dashpush` 每 180 秒自動 add + commit + push；跑 git 會留下 `.git/index.lock` 把 daemon 擋住。
12. **驗證上線**：等 2–4 分鐘後抓 `https://gundamnboy.github.io/chart-of-the-day/data/index.json`，確認 `days[0].date` 是今天。**寫完檔不等於發布成功**——這一步不做，daemon 若沒推成功不會有任何錯誤訊息。WebFetch 常拿到 CDN 快取的舊內容，此時改用 Chrome 以 `fetch(url+'?cb='+Date.now(), {cache:'no-store'})` 重抓確認。

---

## 7. 發布前檢查腳本

**唯一權威副本在 `tools/check_day.py`**（2026-08-09 移出，讓每日執行不必讀 9,000 字元的腳本本體）。改任何檢查規則都改那個檔案，並同步第 4 節散文版與排程 prompt——**仍是三處**。下面這個殼負責執行它，維護 skill 的 regex 抽取照舊有效：

```python
import os
REPO = os.environ.get('CHART_REPO') or os.path.expanduser('~/chart-of-the-day')  # 一律絕對路徑
os.environ.setdefault('CHART_REPO', REPO)
exec(compile(open(os.path.join(REPO, 'tools', 'check_day.py'), encoding='utf-8').read(),
             'check_day', 'exec'), {'__name__': '__main__'})
```

診斷舊期：`python3 tools/check_day.py 2026-08-05`（日期當參數；`KNOWN` 歷史例外清單也在該檔）。

---

## 8. 變更紀錄

> 完整紀錄在 **[CHANGELOG.md](CHANGELOG.md)**。本節只留最新一筆（維護 skill 靠它判斷認知新舊）；**加新版本時把上一筆搬過去**，不要讓這裡長回 13,000 字元——那正是 2026-08-09 搬家的原因。

### 2026-08-14 · 第 27 版（回退機制把原始錯誤變形，外層失去判斷依據）

| | |
|---|---|
| **動到哪些檔** | `tools/fetch.py`（新增 `NotFredSeries`、`fred()` 對 400 不再退回繪圖端點、`_route_and_fetch` 改接具名例外）、`AGENT_BRIEF.md` §3.2＋§8、`MAINTENANCE.md` |
| **量測** | 第二次預抓 **32/46**：Tiingo 5 條全通（`GLD`／`XLE`／`MU`／`STX`／`CPER`，各 2,920 點末日 08-13）、台股 7 條末日 **08-14 當日**、FRED 20 條。**唯二失敗的 `SOXQ`／`FEZ` 是本版修的那個 bug**。修正後模擬驗證：400 直達 Tiingo（不經 fredgraph）、逾時仍退回 fredgraph |
| **怎麼驗的** | 以 stub 分別模擬「API 回 400」與「API 逾時」兩種情況，確認呼叫序分別為 `api→tiingo` 與 `api→graph`——**兩條路都要驗，只驗修好的那條會把退路刪掉而不自知** |
| **怎麼倒回去** | 移除 `NotFredSeries`，`fred()` 的 except 改回一律退回繪圖端點 |
| **當時已知的風險** | `SOXQ`／`FEZ` 尚未在真機驗過（本次修正後還沒重跑）。指數與期貨仍只有 Yahoo 一條路 |

**回退機制把原始錯誤變形，外層就失去了判斷依據——這是今天第三次踩到同一個形狀。**

`SOXQ`／`FEZ` 是美股 ETF 不是 FRED 序列，API 正確地回 400。但 `fred()` 把「400」也當成「API 失敗」而去打 fredgraph，fredgraph 再逾時 30 秒，**最後拋出來的是逾時錯誤**——而外層負責「400 就改試 Tiingo」的判斷是在比對錯誤訊息裡有沒有「400」，於是完全認不出來。**每一層各自的回退都合理，疊起來就把資訊磨掉了。**

- 修法是讓否定訊號**具名**：400 直接拋 `NotFredSeries`，語意是「這代號不屬於 FRED」，與「連不上」明確分開。**用字串比對錯誤訊息來判斷錯誤種類，只要中間有任何一層改寫過訊息就會失效。**
- 今天三次同型：`DAY` 是路徑卻拿去跟日期比大小（恆為 False）、`_lag` 用了同一個變數（ValueError 被自己的 except 吞掉）、以及這次。**共同點都是「錯誤被吃掉或變形，於是判斷失去依據」，而三次的表面症狀完全不同。**
- 驗證時**兩條路都要測**：只驗「400 會走到 Tiingo」而不驗「逾時仍退回 fredgraph」，就可能在修一條路時把另一條刪掉而不自知。

