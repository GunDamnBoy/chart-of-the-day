# 每日五圖 · Chart of the Day — 執行規格（AGENT_BRIEF）

> 這份文件與排程 prompt（`~/Documents/Claude/Scheduled/chart-of-the-day-daily/SKILL.md`）是**一組兩份**。
> 改任何一邊，另一邊必須同步。不同步是這類系統最常見、也最難察覺的故障。

---

## 0. 執行前提（不成立就停下來回報，不要硬跑）

1. **repo 資料夾已連線。** 開工前先用 Read 讀 `/Users/kenny/chart-of-the-day/AGENT_BRIEF.md`（也就是本檔）。讀不到代表資料夾沒掛上——**停下來回報「repo 資料夾未連線，今日無法產出」，不要嘗試其他路徑、也不要把檔案寫到別的地方。**
2. **發布靠 daemon，不靠執行者。** `com.kenny.dashpush` 每 180 秒自動推送本 repo。執行者**不跑任何 git 指令**，寫完檔後依第 6 節第 10 步抓 Pages 驗證。

---

## 1. 這套系統是什麼

一句話：**每天挑五個題目，用公開資料自己畫五張圖，每張圖配一段自己的判讀；當天封存成一個獨立 JSON，圖與資料永久保留。**

三件事必須同時成立，缺一張圖就不算數：

1. **圖是自己重製的。** 選題可以取材自 Bloomberg、FT、Economist 的圖表專欄，但**絕不轉載他們的圖**。我們讀他們的論點，然後用 FRED／Yahoo 的公開資料自己畫、自己算、自己下結論。
2. **每張圖都要有「所以呢」。** 一張沒有投資意涵的漂亮圖，對本知識庫沒有價值。`takeaway`／`so_what`／`watch` 三個欄位是必填。
3. **資料留得住。** 每天的 JSON 內含該圖用到的完整序列，`data/series/` 另存 CSV 快取。任何一天的圖，一年後都要能被 `tools/render_day.py` 原樣重畫。

### 它在整個知識庫系統中的位置

```
advisory-dashboard-daily（07:30 起跑，實測約 10:40 完成，26 家來源、15 個子類別）
              │  提供當日題材
              ▼
     chart-of-the-day（每天 11:00，本系統）
              │  提供「已經畫好、可直接引用」的圖與判讀
              ├──────────────► convergence-weekly（每週日，主題匯流訊號報）
              └──────────────► House View 月報（pptx 直接取用 PNG）
```

**方塊圖裡的 07:30 是上游的「起跑」時間，不是完成時間。** 這個區別曾經害本系統每天退回用前一日題材——原本 09:00 起跑、只等 15 分鐘，而上游實測完成時間是 08-04 09:40、08-05 10:40、08-06 10:40，**近四個交易日有三天在 09:30 時根本還沒好**。2026-08-06 決議把本系統挪到 11:00。**寫上游時間時一律寫完成時間，起跑時間對下游沒有意義。**

**下游會直接拿 `charts/<date>/*.png` 貼進簡報。** 所以圖的頁尾要自帶來源標註，離開網站也站得住。

---

## 2. 五個欄位（slot）——每天固定五張，順序固定

| # | slot | 要回答的問題 | 題材來源 |
|---|------|------------|---------|
| 1 | **當日主圖** | 今天最重要的一件事，用一張圖說清楚 | 投顧儀表板當日 headline 與深度卡 |
| 2 | **市場異動圖** | 今天市場哪裡最不尋常 | 程式化掃描：把當日變動放進歷史分布看分位 |
| 3 | **重製圖** | 國際媒體今天在講什麼、我們怎麼看得更準 | Bloomberg Graphics／FT Visual & LEX／Economist Graphic Detail／Reuters Graphics |
| 4 | **主題深掘** | 儀表板 15 個子類別中，今天最值得補一張圖的 | 投顧儀表板子類別 |
| 5 | **軌道圖** | 五大長期軌道輪值，確保跨期序列不斷線 | 固定輪盤（見下） |

**軌道輪盤（週一至週五依星期輪值，週末改為週線複查）：**

| 星期 | 軌道 |
|------|------|
| 一 | 利率與匯率 |
| 二 | 台股與資金流 |
| 三 | AI 與半導體 |
| 四 | 原物料與能源 |
| 五 | 信用與風險偏好 |
| 六、日 | **不套輪盤，改為週線複查（見下）** |

軌道圖的價值在**跨期可比**：同一條軌道每週同一天出現，序列與基期盡量沿用上一次，讀者才看得出趨勢。**不要每次換算法或換基期。**

**週末（六、日）的處理——排程一週跑七天，每個週末都會遇到，不是例外狀況。**

五條軌道綁死週一到週五，是為了「同一條軌道每週固定同一天出現」。週末若接著往下數第六、第七條，軌道與星期就會脫鉤，一輪之後全部錯位，跨期可比性直接歸零。所以週末**不推進輪盤**，改做**週線複查**：

- 從當週週一至週五已經出現過的五條軌道中挑一條，用**週線或月線尺度**重看（不是把同一張日線圖再畫一次）。
- `slot` 寫成 `軌道圖｜<軌道名>（週線複查）`。開頭仍是「軌道圖」，檢查腳本照樣通過。
- **挑選原則**：選當週那次留下未解問題、或當週之後又出現新變化的那條。**週六與週日不得挑同一條。**
- **序列與基期沿用該軌道當週那一次**，複查才看得出前後差異。
- 「五張圖 `theme` 不得重複」對週末同樣適用。

### 三條硬規則

- **五張圖不得有兩張落在同一個 `theme`。** 主題撞車就換掉排序較後的那張。
- **`slot` 1–4 必須是當天的事**，不可以拿舊題材充數。素材真的不足時，正確做法是把該 slot 改成「前瞻框架」並在 `about.run` 如實記錄，**不是硬湊**。
- **`slot` 3 的 `provenance.inspired_by` 必填**（媒體、標題、URL）。這是我們與轉載的分界線，也是日後回頭檢查「我們當初看對了沒」的依據。

---

## 3. 取數規範

### 3.1 允許的來源

| 來源 | 用途 | 取法 |
|------|------|------|
| **FRED** | 總經、利率、利差、信用、匯率、通膨預期 | `tools/fetch.py` 的 `fred()` |
| **Yahoo Finance** | 指數、個股、期貨、匯率日收盤 | `tools/fetch.py` 的 `yahoo()` |
| 世界銀行／IMF／EIA | 跨國、能源實體數據 | 官方 JSON／CSV 端點 |
| 台灣證交所、櫃買、央行、主計總處 | 台股籌碼、台灣總經 | `tools/fetch_twse.py`，`bfi82u`（三大法人）與 `margin`（融資融券）**均已實測可用**。三大法人是**六列要聚合**；融資金額單位是**仟元**（→億元除 1e5）。**日期送錯不會報錯，會安靜回最近一個交易日**，務必核對回傳 `title` |
| 公司財報、法說簡報 | 個別公司數字 | 原始文件，不引用二手轉述 |

**一律不使用付費牆內的圖或數據。** 讀了 Bloomberg／FT 的文章拿到論點沒問題，把他們的數字當成我們的資料來源不行——因為我們無法驗證，也無權轉載。

**FRED 的兩條路徑**：`fetch.py` 會自動選——讀得到 API key 就走官方 API（`api.stlouisfed.org`，文件化、120 req/min），讀不到就退回繪圖端點（`fredgraph.csv`，免認證但非文件化 API）。key 從 `FRED_API_KEY` 環境變數或 `~/.config/fred/api_key` 讀取，**不寫進程式碼、不進版控**（`.fred_key` 與 `.env` 已列入 `.gitignore`）。所有對外輸出都經過 `_redact()`，key 不會出現在 log 或錯誤訊息裡。
用 `python3 tools/fetch.py --check-key` 可自我檢查。

### 3.2 兩個實測過的限制（不要重踩）

1. **FRED 一次只能抓一條序列。** `fredgraph.csv?id=A,B` 會回傳 **zip**，不是 CSV。`fetch.py` 已對 `PK` 開頭做偵測並拋錯。
2. **ICE BofA 利差序列（`BAMLxxx`）只有近三年。** 這不是端點限制，是 FRED 的政策——序列頁面明載「Starting in April 2026, this series will only include 3 years of observations.」，資料本身受 ICE Data Indices 版權保護。**申請 FRED API key 不會解決這件事**，換 API 端點也不會。因此凡是用到 HY／IG OAS 的圖，分位數與中位數一律標明「近三年」，**不可以寫成「歷史低點」**。要更長的歷史只能改用其他來源（如 ICE 官方或 Bloomberg 授權資料）。

### 3.3 本機直連失敗時的備援（已實測可行）

**先分清楚兩種「失敗」——處置完全相反：**

| 狀況 | 特徵 | 正確處置 |
|------|------|---------|
| **網路被擋** | 連線逾時、DNS 失敗、代理拒絕 | 走下面的瀏覽器同源備援 |
| **被限流（HTTP 429）** | 對方明確回 429 | **等，不要繞。** `_get()` 會自動退避 20／40 秒 |

**429 不是障礙，是對方在說「慢一點」。** 用瀏覽器、換 host、換來源去繞過速率限制，是把別人的營運規則當成技術問題處理——我們不做這種事。若 429 持續到執行時仍未解除，正確做法是**該圖降級或改題，並在 `about.run` 如實寫明「Yahoo 限流，某某序列未取得」**。寧可誠實少一張圖，也不要繞。

公司網路或代理擋掉 Python 直連時，改由瀏覽器**同源** fetch：

- **FRED**：先 `navigate` 到 `https://fred.stlouisfed.org/series/<ID>`，再 `javascript_tool` 執行
  `await fetch('/graph/fredgraph.csv?id=<ID>&cosd=2015-01-01').then(r=>r.text())`
- **Yahoo**：先 `navigate` 到 `https://finance.yahoo.com/quote/<SYM>`，再 fetch
  `https://query1.finance.yahoo.com/v8/finance/chart/<SYM>?range=5y&interval=1d`
  （Yahoo 的 CORS 只允許 `*.yahoo.com` 來源，從 FRED 頁面抓會被擋。）

**注意兩件實測到的事：**
- **不要把整包資料當成工具回傳值往外送**——回傳會被截斷（實測單一欄位約 1,000 字元）。要嘛在本機處理完，要嘛用 `a.download` 存成檔案再讀。
- **`finance.yahoo.com` 的 CSP 會擋掉程式觸發的下載**；`fred.stlouisfed.org` 可以，但同一站台第二次自動下載會被 Chrome 擋。備援路徑只適合救急，正式流程請用本機直連。

### 3.4 資料品質檢查（強制）

`tools/render_day.py` 會對每條序列跑 `qa_series()`，抓出單日跳動超過 **5 個標準差**的點，寫進 `about.qa_flags` 並顯示在網站上。同一序列相鄰交易日的旗標會**合併成一筆事件**（記 `date_end` 與 `days`），因為 2025-04-03 與 04-04 是同一次關稅衝擊，拆成兩筆只會讓處置說明變流水帳。

**門檻為什麼是 5σ**：原本設 6σ，2026-08-05 覆核時發現它漏掉了 2026-01-30 黃金單日 **−11.37%**（z＝−5.59）。一根 11% 的棒子沒有任何機制看過一眼，就是門檻失職。改 5σ 後同一份資料由 2 筆增為 5 筆（已含合併），多出來的三筆都是已知真實事件。**多出來的不是雜訊，是本來就該被讀到的東西。**

**看到旗標不要直接忽略。** 它有三種可能，處置不同：

| 原因 | 特徵 | 處置 |
|------|------|------|
| 真實市場事件 | 有對應新聞（如 2025/04/03 關稅衝擊） | 保留，可在判讀中引用 |
| 期貨轉倉 | 出現在 `GC=F`／`BZ=F` 這類近月合約，**且幅度通常在 1% 以內** | 改用現貨或連續調整序列，或在圖註說明 |
| 來源錯價 | **隔日就跳回去**、無對應新聞 | 換來源重抓，**不要**照原樣出圖 |
| **衍生序列的窗口效應** | 滾動波動率、移動平均這類量；跳動呈**階梯狀**且與該日新聞無關 | 在 `series` 標 `"derived": true`。旗標會標記並改為**整條序列說明一次**，不必逐筆 |

**第四類是 2026-08-06 那期逼出來的**：18 筆旗標裡有 13 筆全在「黃金 ETF 60 日波動率」這一條上。它既不是市場事件、不是轉倉、也不是錯價——是某一天的極端值滾出 60 日窗口，指標就跳一階。**原本的三分法沒有這一格，執行者只能硬塞進「真實事件」或逐筆寫罐頭說明。** 標了 `derived` 之後，檢查腳本只要求該序列在 `about.run` 被提到一次。**注意這不是豁免**：完全不提仍會警示，因為讀者需要知道那些階梯是什麼。

**判斷時先看幅度再看故事。** 黃金與原油的轉倉價差來自持有成本，**正常在 1% 以內、極少超過 2%**。所以「跌 11% 是轉倉」這種解釋在數量級上就不成立——2026-01-30 那筆的正確歸類是前兩類之一，而它之後停在 4,600–4,900 沒有跳回 5,300，**排除了「來源錯價」**（錯價的特徵是隔日還原）。**不要用轉倉當成不想查的藉口。**

### 3.5 序列新鮮度（拿兩天前的收盤當「今天」講）

第 7 節檢查腳本會列出每條序列的最後日期與距當期日期的天數：

- **落後 ≥ 2 天**：警示。**凡是以該序列算出的「今天」數字，`subtitle` 或 `note` 必須寫出實際基準日。**
- **落後 ≥ 5 天**：硬失敗，不得發布。
- **`window.note` 必須與實際末日相符。** 腳本會雙向比對——note 提到卻沒有序列以該日結束、或有序列末日卻沒寫進 note，都會警示。**metadata 寫錯比不寫更糟，它讓讀者以為資料是新的。**

這條規則來自 2026-08-05 的實例：`^SOX` 序列止於 08-03，卻用來算「自高點跌幅 −21.9%」並放在 08-05 那期，而 `window.note` 還寫著「行情序列最新至 2026-08-04／08-05」——**根本沒有任何序列以 08-04 結束**。當時圖註把與媒體數字的落差歸因為「盤中／不同基準日」，但真正的第一嫌疑是**我們的資料落後了兩個交易日**。

---

## 4. 內容規範

| 欄位 | 長度 | 要求 |
|------|------|------|
| `title` | 12–30 字 | **是一個判斷，不是一個標籤。**「台股補上了費半掉下來的那一段」而不是「台股與費半走勢比較」 |
| `subtitle` | 一行 | 畫的是什麼、期間、單位、基期 |
| `takeaway` | ≤ 70 字 | 一句話結論，帶數字 |
| `reading` | 200–520 字，分 3 段 | 第 1 段講圖上看到什麼（帶數字）；第 2 段講為什麼會這樣；第 3 段講這裡面有什麼是市場還沒想清楚的 |
| `so_what` | 60–120 字 | 對投組的意涵。不准寫「須密切關注」這種空話 |
| `watch` | 1–3 條 | **可被驗證的觸發條件**，要有數字或事件名 |
| `tags` | 2–5 個 | 供跨期檢索 |

> 這張表是第 7 節檢查腳本的散文版。**腳本是唯一可執行的權威副本**，這裡的數字必須與它逐字相符。
> 改任何一個長度數字，**本節、第 7 節腳本、排程 prompt 第 5 步三處要同時改**——只改散文版不會有任何效果，只改腳本則會讓寫作者依錯的規範下筆。

### 寫作要求

- 全程**繁體中文（台灣用語）**。
- **數字一律來自我們自己算出來的序列**，不是抄新聞。若我們算出的數字與媒體引用不一致，**兩個都寫，並說明計算基準**——這正是重製的價值。
- 允許有觀點，但觀點要能被證偽。凡是寫「可能」「或許」的地方，問自己：**怎樣才算錯？** 寫不出來就刪掉那句。
- 不要用 emoji。不要用「驚人」「暴漲」這類形容詞去補數字說服力不足的地方。

### 圖表配色：四個角色，不是六個編號

**顏色回答的是「你在這張圖的論證裡是什麼角色」，不是「你是第幾條序列」。** 每張圖都有 `takeaway` 與 `so_what`，也就是每張圖都在講一句話——顏色的工作是讓人一眼看出那句話講的是哪一條線。

| 角色 | 色 | 白底對比 | 用在哪 |
|---|---|---|---|
| **主角** | **#D70C18** 品牌紅 | 5.3:1 | `takeaway` 在講的那條線／今日點。**一張圖只有一個** |
| **對照** | **#1F4E79** 深藍 | 8.7:1 | 前期、對手、共識 |
| **溢出** | **#8A8F95** 中灰 | 3.3:1 | 第三條。用到就代表該考慮拆圖 |
| **背景** | **#B8BBBE** 淺灰 | 1.9:1 | 其餘所有東西。**低對比是刻意的**，它的工作是退到後面 |

**`series[].color` 預設留空**，`chartkit` 會照上表順序給（`PALETTE[i % len]`）。**只有要指定誰是主角時才寫死**——例如 `jpy-vs-ust10` 那張刻意讓第二條（美 10 年期殖利率）當主角、第一條當對照，那就要寫。

2026-08-05 那期原本九條序列 100% 手寫了 color，其中**七條的值等於程式本來就會給的預設**（白工，且讓換配色從改一行變成逐條手改），只有 `jpy-vs-ust10` 那兩條是有語意的指定。已依此規則清空七條、保留兩條。

**序列最多四條。** 第 5 條會 `PALETTE[4 % 4]` 繞回主角紅，一張圖出現兩個主角——第 7 節檢查腳本會硬失敗。用到第 4 條（背景色）時腳本會警示，要確認那條真的只是背景。**超過就拆圖，不是換色。**

**土黃 #D9942B、綠 #3F7D5C、紫 #7A6BA8 已移出工作集。** 土黃對白底只有 2.6:1，2pt 細線會偏淡；綠與主角紅在紅綠色盲下只差 17。更重要的是：**一張圖需要第 5、6 個顏色時，該修的是圖不是色。**

**正負值不分色。** 一律靠 `zero_line` 與資料標籤表達，紅只保留給主角。理由有三：原本的 `POS`／`NEG` 是死碼（全 repo 從未被引用）；`NEG` 的值與 `ACCENT` 完全相同，真用起來紅會同時代表「重點」與「下跌」；而綠正紅負是西方慣例，與台灣的紅漲綠跌相反。

> **主角紅為什麼是 #D70C18。** 舊值 #C8102E 的註解寫著「台新紅」，但那不是台新紅——那接近 Pantone 186，是當初挑錯的。品牌紅取自 `tsholdings.com.tw` 的 logo SVG `fill`（全站計算樣式 86 處）。
>
> 另有第三個紅：**#C00000 是 PowerPoint 標準色盤的 Dark Red**，House View 月報的模板用的是那個。三者兩兩相距 ΔE 6～8，**全都落在「太近讀不出是刻意的、太遠又不像同一個顏色」的最糟區間**。
>
> **這一套與 House View 月報是同一套**（`~/houseview/HOUSEVIEW_BRIEF.md` 第 5 節）。月報會直接拿本庫的 PNG 貼進簡報、也會用本庫的 `series` 自己重畫，兩邊不同色就會在同一頁上看出破綻。**Houseview 的健康檢查會讀本檔的 `ACCENT` 比對，改這裡就會讓那邊 FAIL——那是設計成這樣的，改完要去更新那邊的登記值。**

---

## 5. JSON schema（`data/YYYY-MM-DD.json`）

```jsonc
{
  "date": "2026-08-05",
  "weekday": "週三",
  "headline": "當日總標題（20–30 字，一個判斷）",
  "standfirst": "導言 60–100 字，串起五張圖的共同線索",
  "window": { "data_asof": "2026-08-05T10:40+08:00", "note": "各序列最新日期不一致時在此說明" },
  "about": {
    "upstream": ["advisory-knowledge-hub/data/2026-08-05.json", "..."],
    "run": "本輪執行紀錄：降級、替換、待覆核事項",
    "qa_flags": []                       // 由 render_day.py 自動寫入，不要手改
  },
  "charts": [{
    "slug": "risk-premium-unwind",       // 檔名用，英文小寫連字號
    "slot": "當日主圖",
    "theme": "能源與原物料",              // 必須是投顧儀表板 15 個子類別之一
    "title": "...", "subtitle": "...",
    "kind": "timeseries",                // timeseries | scatter
    "y_label": "...", "y2_label": "...",
    "y_fmt": "{:,.0f}", "y2_fmt": "{:,.2f}",
    "y_log": false,                      // 利差、倍數這類「比例才有意義」的量請開 true
    "zero_line": false,
    "source": "資料來源：...（含期間）",
    "note": "計算基準、限制、取材出處",
    "series": [{ "name":"布蘭特原油", "dates":["2026-01-02",...], "values":[60.75,...],
                 // color 預設留空，由 PALETTE 照角色順序給；只有要指定主角時才寫
                 "color":null, "axis":"left", "style":"line", "dash":false }],
    "markers": [{ "date":"2026-03-31", "label":"油價見頂" }],
    "pts": [], "hi_pts": [],             // kind=scatter 用
    "provenance": { "inspired_by": { "outlet":"Bloomberg", "title":"...", "url":"..." } },
    "takeaway": "...", "reading": "...\n\n...", "so_what": "...",
    "watch": ["...", "..."], "tags": ["..."],
    "files":  {},                        // render_day.py 寫入
    "option": {}                         // render_day.py 寫入（ECharts）
  }]
}
```

**`series` 一定要把資料點寫進 JSON。** 這是「歷史可查閱」的實作方式——不要只存檔名或只存來源代號。

---

## 6. 每日執行步驟

1. **讀上游**：`~/advisory-knowledge-hub/data/<今天>.json`。若當天檔案不存在（投顧儀表板還沒跑完或失敗），**等 15 分鐘再讀一次**；仍無則改用前一日檔案並在 `about.run` 註明。
2. **掃圖表專欄**：Bloomberg Graphics、FT Visual & Data／LEX、Economist Graphic Detail、Reuters Graphics。**只取圖題與論點**，記下 outlet／title／url。
3. **選題**：依第 2 節五個 slot 各挑一個，檢查 `theme` 不重複。
4. **取數**：`python3 tools/fetch.py <ids...>`；跨市場比較先 `align()` 再 `rebase()`。
5. **寫 JSON**：依第 5 節 schema 寫 `data/<今天>.json`，含完整 `series`。
6. **算圖 + 檢查**：`python3 tools/render_day.py <今天>`，看 QA 旗標並依第 3.4 節處置。
7. **更新索引**：重建 `data/index.json`。
8. **跑發布前檢查腳本**（第 7 節），全綠才發布。
9. **發布**：**直接用檔案工具寫入即可，絕對不要跑任何 git 指令（含 `git status`）。** 本機 `com.kenny.dashpush` 每 180 秒自動 add + commit + push；跑 git 會留下 `.git/index.lock` 把 daemon 擋住。
10. **驗證上線**：等 2–4 分鐘後抓 `https://gundamnboy.github.io/chart-of-the-day/data/index.json`，確認 `days[0].date` 是今天。**寫完檔不等於發布成功**——這一步不做，daemon 若沒推成功不會有任何錯誤訊息。WebFetch 常拿到 CDN 快取的舊內容，此時改用 Chrome 以 `fetch(url+'?cb='+Date.now(), {cache:'no-store'})` 重抓確認。

---

## 7. 發布前檢查腳本

```python
import json, os, sys, glob, collections
REPO = os.path.expanduser('~/chart-of-the-day')          # 一律絕對路徑
# 預設檢查最新一期；發布流程用這個。維護時要診斷舊期，傳日期進來即可：
#     python3 -c "...exec(檢查腳本)..." 2026-08-05
# 沒有這個開關的話，想看舊期就得手改路徑，改完常常忘了改回來。
DAY  = (REPO + f'/data/{sys.argv[1]}.json') if len(sys.argv) > 1 else \
       sorted(g for g in glob.glob(REPO+'/data/2*.json'))[-1]
d    = json.load(open(DAY, encoding='utf-8'))

# 已知的歷史例外：不是待修的 bug，是不回頭改寫已發表分析的結果。
# **列在這裡是為了讓紅字有解釋**，否則下一個人會以為是新問題而去「修好」它。
KNOWN = {
  '2026-08-05': ['risk-premium-unwind so_what 59 字（規範 60–120）',
                 'spx-vix-corise so_what 42 字（規範 60–120）',
                 'sox-vs-twse reading 引用 8/4 費半 +6.55%，該日不在本圖序列內（已於 note 標明）'],
}
SLOTS  = ['當日主圖','市場異動圖','重製圖','主題深掘']    # 第 5 個是「軌道圖｜xxx」
THEMES = {'美股與財報','AI 與半導體','央行、利率與匯率','台灣','中國','日本',
          '能源與原物料','金融、併購與企業','地緣政治（中東與戰事）','美國政治與政策',
          '歐洲','亞太（韓國、印度、東南亞）','生技健護','信用債','黃金'}
ok = True
def bad(m):
    global ok; ok = False; print('✗', m)

print(DAY.split('/')[-1], '| 圖數', len(d['charts']))
if len(d['charts']) != 5: bad('圖數不是 5')
for f in ('headline','standfirst','window'):
    if not d.get(f): bad(f'缺 {f}')

seen_theme, seen_slot = [], []
for i, c in enumerate(d['charts'], 1):
    p = f'[{i}] {c.get("slug","?")}'
    for f in ('slug','slot','theme','title','subtitle','source','takeaway','reading','so_what'):
        if not c.get(f): bad(f'{p} 缺 {f}')
    if c.get('theme') not in THEMES: bad(f'{p} theme 不在 15 個子類別：{c.get("theme")}')
    seen_theme.append(c.get('theme')); seen_slot.append(c.get('slot',''))
    if not (12 <= len(c.get("title","")) <= 30): bad(f'{p} title 長度 {len(c.get("title",""))} 不在 12–30')
    if len(c.get("takeaway","")) > 70:           bad(f"{p} takeaway 超過 70 字")
    if not (200 <= len(c.get('reading','')) <= 520): bad(f'{p} reading 長度 {len(c.get("reading",""))} 不在 200–520')
    if len(c.get('reading','').split('\n\n')) < 3:   bad(f'{p} reading 未分 3 段')
    if not c.get('watch'):  bad(f'{p} 缺 watch')
    if not (1 <= len(c.get('watch') or []) <= 3): bad(f'{p} watch {len(c.get("watch") or [])} 條，規範 1–3')
    if not (2 <= len(c.get('tags') or []) <= 5):  bad(f'{p} tags {len(c.get("tags") or [])} 個，規範 2–5')
    if not (60 <= len(c.get('so_what','')) <= 120):
        bad(f'{p} so_what 長度 {len(c.get("so_what",""))} 不在 60–120')
    # 配色只有四個角色，第 5 條序列會 PALETTE[4 % 4] 繞回主角紅，
    # 一張圖就出現兩個主角。超過 4 條要拆圖，不是換色。
    if len(c.get('series') or []) > 4:
        bad(f'{p} 有 {len(c["series"])} 條序列，超過四個角色——第 5 條會繞回主角紅，請拆成兩張圖')
    elif len(c.get('series') or []) == 4:
        print(f'   ⚠ {p} 有 4 條序列，已用到背景色當資料序列——確認第 4 條真的只是背景')
    if not c.get('files',{}).get('png'): bad(f'{p} 未產出 PNG')
    if not c.get('option'): bad(f'{p} 未產出 ECharts option')
    if c.get('slot') == '重製圖' and not c.get('provenance',{}).get('inspired_by',{}).get('url'):
        bad(f'{p} 重製圖缺 provenance.inspired_by.url')
    if c.get('kind') != 'scatter':
        if not c.get('series'): bad(f'{p} 缺 series')
        for s in c.get('series', []):
            if len(s['dates']) != len(s['values']):
                bad(f'{p} 序列 {s["name"]} 日期({len(s["dates"])})與值({len(s["values"])})長度不符')
            if len(s['values']) < 20: bad(f'{p} 序列 {s["name"]} 只有 {len(s["values"])} 點，太短')

dup = [t for t, n in collections.Counter(seen_theme).items() if n > 1]
if dup: bad(f'theme 重複：{dup}')
for s in SLOTS:
    if s not in seen_slot: bad(f'缺 slot：{s}')
if not any(s.startswith('軌道圖') for s in seen_slot): bad('缺 slot：軌道圖')

for c in d['charts']:
    png = os.path.join(REPO, c.get('files',{}).get('png',''))
    if not os.path.exists(png): bad(f'PNG 檔不存在：{c.get("files",{}).get("png")}')
    elif os.path.getsize(png) < 20000: bad(f'PNG 過小可能沒畫出來：{png}')

# 序列新鮮度：拿兩天前的收盤當「今天」講，是最容易發生也最難察覺的錯
import datetime, re
doc_day = datetime.date.fromisoformat(d['date'])
ends = {}
for c in d['charts']:
    for s in c.get('series', []):
        ends.setdefault(s['dates'][-1], []).append(f"{c['slug']}／{s['name']}")
print('   序列新鮮度：')
for last in sorted(ends, reverse=True):
    gap = (doc_day - datetime.date.fromisoformat(last)).days
    if gap >= 5:
        bad(f'序列已過期 {gap} 天（末日 {last}）：{"、".join(ends[last])}')
    elif gap >= 2:
        print(f'   ⚠ 落後 {gap} 天（末日 {last}）：{"、".join(ends[last])}'
              '｜凡以此序列計算的「今天」數字，subtitle 或 note 必須寫出實際基準日')
    else:
        print(f'     {gap} 天（末日 {last}）：{len(ends[last])} 條序列')

# window.note 必須與實際末日相符——寫錯比不寫更糟，它會讓讀者以為資料是新的
# 一律比對 MM-DD：note 常把年份省略成「2026-08-04／08-05」。
# 前面的 (?<!\d) 與可選年份是為了不讓 '2026-08-03' 被誤切成 '26-08'。
DATE_RE = r'(?<!\d)(?:\d{4}-)?(\d{2}-\d{2})(?!\d)'
note_md = set(re.findall(DATE_RE, d.get('window', {}).get('note', '')))
ends_md = {x[-5:] for x in ends}
for nd in sorted(note_md - ends_md):
    print(f'   ⚠ window.note 提到 {nd}，但沒有任何序列以該日結束——寫錯比不寫更糟')
for ed in sorted(ends_md - note_md):
    print(f'   ⚠ 有序列末日為 {ed}，window.note 未提及')

# QA 旗標必須在 about.run 留下處置紀錄（警示級，不擋發布，但看到就要補）
# 體積守門：不會隨天數累積（每天獨立檔、前端一次只載一天），只隨序列長度成長
kb = os.path.getsize(DAY) / 1024
if kb > 600:   bad(f'單日 JSON {kb:.0f}KB 過大，前端載入會明顯卡頓')
elif kb > 250: print(f'   ⚠ 單日 JSON {kb:.0f}KB 偏大，檢查是否有圖用了過長的序列')
else:          print(f'   單日 JSON {kb:.0f}KB')

q   = d.get('about',{}).get('qa_flags',[])
run = d.get('about',{}).get('run','') or ''
plain   = [f for f in q if not f.get('derived')]
derived = [f for f in q if f.get('derived')]
print(f'   QA 旗標 {len(q)} 筆'
      + (f'（其中衍生序列 {len(derived)} 筆，整條說明一次即可）' if derived else ''))
# 一般旗標：逐筆都要在 about.run 點名
for f in plain:
    if (f.get('series') or '') not in run and (f.get('date') or '') not in run:
        print(f'   ⚠ 旗標未在 about.run 說明處置：{f.get("chart")}／{f.get("series")}／{f.get("date")}')
# 衍生序列：窗口進出造成的階梯跳動，逐筆要求只會逼出罐頭文字。
# 改成「每條序列至少要被提到一次」——說明一次，但不能完全不說。
for name in sorted({f.get('series', '') for f in derived}):
    if name and name not in run:
        n = sum(1 for f in derived if f.get('series') == name)
        print(f'   ⚠ 衍生序列「{name}」有 {n} 筆旗標，about.run 完全沒提到')

# 判讀文字不可引用「本圖序列涵蓋不到的日期」。
# 這條來自 2026-08-05 的實例：sox-vs-twse 的 reading 引用 8/4 費半 +6.55%，
# 但該圖的 ^SOX 序列止於 08-03——那個數字只能是抄來的，違反
# 「數字一律來自我們自己算出來的序列」。**抄來的數字沒有任何機制擋得住，
# 但「引用了序列涵蓋不到的日期」抓得到，而且那正是它的特徵。**
import datetime as _dt
DATE_TOK = re.compile(r'(?<![\d.])(\d{1,2})[/月](\d{1,2})(?![\d%])')
for i, c in enumerate(d['charts'], 1):
    if not c.get('series'):
        continue                                   # scatter 沒有 series，照不到
    # 取各序列末日的 min，不是 max——一張圖裡只要有一條序列沒涵蓋到那天，
    # 針對那條序列講的數字就不可能是自己算的。2026-08-05 的 sox-vs-twse 正是如此：
    # 台股到 08-05、費半只到 08-03，而文字講的是「8/4 費半 +6.55%」。
    last = min(s['dates'][-1] for s in c['series'])
    ly, lm, ld = (int(x) for x in last.split('-'))
    # **不掃 `watch`**：觸發條件本來就該指向未來日期，那是它的用途不是違規。
    text = ' '.join(str(c.get(f, '')) for f in ('title','subtitle','takeaway','reading','so_what'))
    for mm, dd in DATE_TOK.findall(text):
        mm, dd = int(mm), int(dd)
        if not (1 <= mm <= 12 and 1 <= dd <= 31):
            continue
        if (mm, dd) > (lm, ld) and abs(mm - lm) <= 2:   # 只比對同期附近，避免誤判去年的日期
            print(f'   ⚠ [{i}] {c.get("slug")} 文字提到 {mm}/{dd}，'
                  f'但本圖序列只到 {last}——該數字若非自算，違反「數字一律來自自己的序列」')

for m in KNOWN.get(d.get('date',''), []):
    print(f'   · 已知歷史例外（不必修）：{m}')

print('全部通過 ✓' if ok else '★ 有問題，不要發布')
```

---

## 8. 變更紀錄

### 2026-08-06 · 第 7 版（首次無人值守執行後：排程改 11:00、修掉兩個渲染缺陷、QA 補第四類）

第一次真正無人值守執行完成並上線。四項調整，其中兩項是**已經發布到網站上的錯誤**。

- **排程由 09:00 改為 11:00。** 08-06 那輪 09:02 查無當日上游、依規範等到 09:30 仍無，只能退回用前一日題材。上游 advisory 已擴為 26 家來源、約 10:30 才完成——**09:00 這個時間點從一開始就注定會退回**。改 11:00 後當日檔應已存在；若 11:00 還抓不到，那是上游出問題，prompt 要求在回報裡明講。
- **ECharts 的 category 軸是按位置貼資料，不是按日期——這造成線條靜默位移。** 原本 `dates = ch.series[0].dates`、每條序列直接丟 `s.values`，只要長度不同整條就偏移。而 `render_static` 用真實日期畫，完全正確——**所以兩軌不一致，網頁錯、PNG 對**，這正是第 1 版就在警告的雙軌漂移。兩個實例：08-06 圖 2 的中位數參考線只有 42 點、軸有 2,452 點，那條線被畫在最左邊 **1.7%** 的寬度裡（使用者看到的是「線不見了」）；08-05 圖 5 的台股加權有 140 點、軸有 146 點，末端被畫在 2026-07-24 的位置而非 08-05，**整條位移八個交易日**——而那張圖的判讀正是在比較台股與費半的相對走勢。改為取所有序列日期的**聯集**當軸、依日期對位、缺值補 `None` 並開 `connectNulls`，稀疏的常數參考線也會連成完整橫線。
- **前端開站顯示的永遠是最舊那天。** `idx.days.slice().sort().reverse()` —— `.sort()` 沒帶比較函式會把元素轉字串比大小，一堆 `[object Object]` 全部相等使 sort 變成 no-op，接著 `.reverse()` 把 index.json 的「新→舊」翻成「舊→新」。使用者得自己拉下拉選單才看得到今天。改為 `sort((a,b)=>b.date.localeCompare(a.date))`，**同時不再倚賴 index.json 的既有順序**。上一頁／下一頁的 disable 判斷也跟著恢復正確。
- **QA 三分法補上第四類：衍生序列的窗口效應。** 08-06 那期 18 筆旗標中有 **13 筆**全在「黃金 ETF 60 日波動率」一條上——極端值滾出 60 日窗口，指標就跳一階。它不是市場事件、不是轉倉、也不是錯價，**原本的三分法沒有這一格**，執行者只能硬塞或逐筆寫罐頭說明。新增 `Series.derived` 旗標，檢查腳本改為「整條序列說明一次即可」，但**完全不提仍會警示**。順帶修掉 `render_day.to_chart()` 沒把新欄位帶進 `Series` 的漏接——**schema 加欄位而轉換層沒接，是加欄位時最容易漏的一步**。

**關於回補既有 JSON**：前兩項渲染缺陷已影響兩期已發布的資料，因此新增 `tools/rebuild_option.py`，**只重建 `option` 欄位**並在寫檔前守門確認其餘欄位一字未動。**不可用 `render_day.py` 回補舊期**——它會順便把 `qa_flags` 用新門檻重算，而 `about.run` 的處置說明是對著當初那組旗標寫的，兩者對不上就會讓歷史紀錄從「誠實回報」變成「說明與資料不符」。**修渲染不該動到紀錄。**

### 2026-08-05 · 第 6 版（配色改為角色制，並修掉一個「註解說謊」的常數）

**這一版的起點是一個事實錯誤，不是設計偏好。** `chartkit.py` 的 `ACCENT = "#C8102E"` 註解寫著「台新紅」——但那不是台新紅。品牌紅是 **#D70C18**（`tsholdings.com.tw` 的 logo SVG `fill`，全站計算樣式 86 處）。#C8102E 接近 Pantone 186，是當初挑錯的值。**註解宣稱的東西和值本身不一致，而且沒有任何機制會發現**，因為配色規則從來沒離開過那行註解——brief 與排程 prompt 兩份文件，在這一版之前完全沒有任何一條關於顏色的規範。

- **`PALETTE` 由六色類別制改為四角色制**：主角 `#D70C18` ／對照 `#1F4E79` ／溢出 `#8A8F95` ／背景 `#B8BBBE`。土黃、綠、紫移出工作集——土黃對白底只有 2.6:1（2pt 細線會偏淡），綠與主角紅在紅綠色盲下只差 17。但真正的理由不是這兩個數字：**一張圖需要第 5、6 個顏色時，該修的是圖不是色。**
- **新增背景色 `DIM`。** 原本的調色盤沒有「退到後面」這個角色，所以做不出「其餘全灰、只有主角有顏色」的圖。每張圖都有 `takeaway`，顏色本來就該讓人一眼看出那句話講的是哪一條。
- **刪掉 `POS, NEG`。** 全 repo grep 確認**從未被任何程式碼引用**，是死碼；而且 `NEG` 的值與 `ACCENT` 完全相同（都是舊的 #C8102E），真拿去畫負值會讓紅同時代表「重點」與「下跌」。正負值改由 `zero_line` 與資料標籤表達——順帶避開綠正紅負是西方慣例、與台灣紅漲綠跌相反的問題。
- **`series[].color` 明訂預設留空。** `chartkit` 的 `s.color or PALETTE[i % len]` fallback 一直都在，但兩份文件都沒記載，結果 2026-08-05 那期九條序列 100% 手寫了 color——其中**七條的值等於程式本來就會給的預設**（白工，且讓換配色從改一行變成逐條手改），只有 `jpy-vs-ust10` 那兩條是刻意的主角互換。已清空七條、保留兩條當示範。
- **序列上限訂為四條，並加進檢查腳本。** `PALETTE` 只有四個角色，第 5 條會 `PALETTE[4 % 4]` 繞回主角紅，一張圖出現兩個主角——**加了角色制卻沒有守門，等於把規則交給運氣**。順帶把 `so_what` 60–120、`watch` 1–3、`tags` 2–5 三條補進腳本：它們原本只存在於第 4 節散文版與排程 prompt，而第 4 節自稱「腳本是唯一可執行的權威副本」，那句話在補上之前是不成立的。
- **`echarts_option()` 補上零線。** 正負值不分色之後，零線是唯一區分正負的視覺元素，但它原本只有靜態軌有（`render_static` 的 `axhline`），互動網頁完全沒畫。**規則押在一個只實作了一半的機制上**，等於在網頁上正負無從分辨。
- **破例重畫 `data/2026-08-05.json`。** 這違反「不改寫既有 `data/*.json`」，經 Kenny 同意：該期是試作首版、且是全部歷史（封存只有一天）。一天的例外，換掉一個「八月上半舊紅、下半新紅」的永久斷層——而 House View 月報每個月正是要從這批 PNG 裡挑圖貼進同一份簡報。**再過兩週不做，成本就從一天變成七十張圖。**

> **跨庫綁定**：這一套與 House View 月報是同一套（`~/houseview/HOUSEVIEW_BRIEF.md` 第 5 節）。那邊的健康檢查會讀本檔的 `ACCENT` 值比對，**本檔一改就會讓那邊 FAIL**——那是刻意的漂移偵測，改完要去更新那邊登記的值。

### 2026-08-05 · 第 5 版（實測回饋：三個「安靜地錯」的行為）

前一版的兩個模組拿到真實網路環境跑，回饋出三件事。**三件都屬於同一類：系統不報錯，只是給你錯的東西。**

- **證交所會吞掉無效的日期參數。** `dayDate=#` 不報錯、不回 stat 失敗，直接回傳最近一個交易日。要不是參數剛好被汙染，這個行為可能幾個月後才在某次「數字對不上」的爭議裡被發現。`fetch_twse.fetch()` 已在送出前擋掉非 `YYYYMMDD` 的輸入，並要求呼叫端核對回傳 `title` 裡的民國日期。
- **三大法人是六列不是三列。** 自營商拆自行買賣（−86.74 億）與避險（−107.54 億）、外資拆外資及陸資與外資自營商。**只取單一列會少算一半**——避險部位的量級與自行買賣相當。已實作聚合並與證交所自己的合計列對帳（20.05 對 20.05），列名日後被改會直接報錯而非安靜算錯。`bfi82u` 由此標為**已實測可用**；`margin` 仍待測，已改成同時支援頂層 `data` 與 `tables` 兩種回應形狀，失敗時會把頂層鍵印出來，下次一眼就能判斷資料放在哪。
- **Yahoo 429 的退避節奏原本是錯的。** 原本 1.5／3／4.5 秒對限流完全不夠，連打只會讓限流延長。改成 429 專用的 20／40 秒退避，並在 CLI 逐標的之間加 1 秒間隔——連續打同一站台正是招來限流的主因，五個標的多花 4 秒，遠比整輪重跑划算。**錯誤訊息明講「429 是限流不是壞掉」並禁止改用其他來源規避**，因為那是把速率限制當成障礙繞過去，不是我們該做的事。順帶修掉 `urllib.error` 未明確 import 的隱患（目前靠 `urllib.request` 間接載入才沒炸）。

另記一條非程式的坑：**`#` 在 zsh 互動模式預設不是註解**，貼帶行尾註解的指令會讓 `#` 與後面的文字變成參數。實測時出現的 `FAIL #` 看起來完全像取數層壞了，但取數層沒事。**偽裝成程式問題的非程式問題最浪費時間。**

### 2026-08-05 · 第 4 版（清掉四條待辦，其中兩條的原假設被推翻）

把 `MAINTENANCE.md` 第 4 節積著的四條待辦逐一查完。**兩條的原始假設是錯的**，這比查證本身更值得記。

- **黃金 2026-01-30 單日 −11.4%：判定不是轉倉，`qa_series` 門檻由 6σ 改為 5σ。** 實測 z＝−5.59，正好卡在舊門檻下方。排除轉倉的理由是數量級——黃金期貨轉倉價差來自持有成本、正常在 1% 以內，11.4% 差了十倍；排除來源錯價的理由是它之後停在 4,600–4,900 沒回到 5,300，而錯價的特徵是隔日還原。**一根 11% 的棒子沒有任何機制看過一眼，就是門檻失職。** 同時加入「同一序列連續日合併為單一事件」，因為 2025-04-03 與 04-04 是同一次關稅衝擊，拆兩筆只會讓 `about.run` 的處置說明變流水帳；合併後同一份資料是 5 筆而非 7 筆。第 3.4 節補上「先看幅度再看故事」的判斷順序——**不要用轉倉當成不想查的藉口**。
- **費半跌幅：原假設被推翻，但尚未結案。** 重算確認 −21.9% 的算術無誤（序列已 rebase，比例不受影響；高點 198.60 @ 06-22）。但發現 `^SOX` 序列**止於 08-03、比當期落後兩個交易日**，而 `window.note` 竟寫著「行情序列最新至 2026-08-04／08-05」——**沒有任何序列以 08-04 結束**。所以與媒體數字的落差，第一嫌疑不是圖註寫的「盤中／不同基準日」，而是我們的資料是舊的。缺口對得上：要達到 −18.5% 需兩日再漲 4.36%，−17% 需 6.28%，半導體在 −22% 回檔後反彈這個幅度屬合理。**還缺實際收盤價驗證**（沙箱無外網）。
- **由此新增第 3.5 節「序列新鮮度」與對應檢查**：落後 ≥2 天警示並要求 `subtitle`／`note` 寫出實際基準日、≥5 天硬失敗；`window.note` 與實際末日**雙向比對**。這是本次最重要的新增——**metadata 寫錯比不寫更糟，它讓讀者以為資料是新的**，而原本沒有任何一步會去核對它。
- **前端體積：問題框架本身是錯的。** 實測單日 105.7 KB，但**體積不隨天數累積**（每天獨立檔、前端一次只載一天），所以原待辦寫的「累積到 300KB」不成立；它只隨單張圖的序列長度成長。改為在檢查腳本加體積守門（>250KB 警示、>600KB 硬失敗），**不做過早的拆檔優化**。真要瘦身時該拉的槓桿已量測清楚：`option` 內嵌的資料點與 `series` 完全重複、佔全檔 39%。
- **台股籌碼：寫出 `tools/fetch_twse.py` 但標記為未實測。** 端點取自 advisory 產線的實測紀錄。**刻意獨立成一個檔案而不併進 `fetch.py`**——後者是每天 09:00 的關鍵路徑，把沒跑過的程式碼加進去，萬一 import 期就出錯會讓整輪產不出東西。先跑 `--selftest` 核對欄位順序，對了再併。

### 2026-08-05 · 第 3 版（發布路徑：改為 daemon，並補上驗證步驟）

首次試燒（13:13）由執行者回報一項不一致：排程 prompt 第 8 步與本文件第 6 節第 9 步都寫 `git add -A && git commit && git push`，MAINTENANCE 第 1 節卻明寫「不要跑任何 git 指令」。追查後發現真正的問題比不一致本身嚴重：

- **daemon 的設計是全自動。** `advisory-dashboard-daily` 的排程 prompt 寫明本機 `com.kenny.dashpush` 每 180 秒自動 add + commit + push，執行者只寫檔、不碰 git，寫完等 2–4 分鐘抓 Pages 驗證。姊妹系統都是這樣運作的。
- **但本 repo 當時不在它的監看清單內。** 實測證據：13:10 改完 `AGENT_BRIEF.md`，到 13:34（過了 8 個 daemon 週期）`.git/logs/HEAD` 仍只有 11:21 那次手動 initial commit，線上 Pages 抓下來也還是第 1 版。新建的 repo 不會自動被納入——11:21 那次是建置時手動推的，正是 MAINTENANCE 第 2.1 節記的 PAT 403 與 Pages 啟用那段。

**所以三份文件的矛盾，其實是「照抄了姊妹系統的正確設計，但這個 repo 的前提還沒成立」。** 若照原樣留著 git 步驟，日後 repo 一旦加進 dashpush 就會撞 `index.lock`；若直接刪掉 git 步驟而不處理 daemon，每天的產出會寫進本機、檢查全綠、然後**永遠不上線且毫無錯誤訊息**。決定走前者並把前提補實：

- 第 6 節第 9 步改為「不跑 git，靠 dashpush」，**新增第 10 步「驗證上線」**——抓 Pages 的 `data/index.json` 確認 `days[0].date` 是今天，含 CDN 快取的 `cache:'no-store'` 繞法。**這一步是這次最重要的新增**：靜默失效之所以危險，就是因為沒有任何一步會去確認「寫進本機」和「上線」是兩件事。排程 prompt 第 8 步同步改寫，並明確指示驗證失敗時**不要自己跑 git 補救**。
- **新增第 0 節「執行前提」**：repo 資料夾未連線就停下來回報，不要嘗試其他路徑。試燒時執行者是中途才把資料夾掛上的，若無人值守就會整輪失敗——`advisory-dashboard-daily` 早就有這條，本文件漏了。
- MAINTENANCE 第 1 節那條 git 禁令改寫為維護與每日執行都適用，並點名 dashpush 與「本 repo 必須在監看清單內」的前提；第 2 節已知坑新增一列，記下「檢查全綠但網站不動」的診斷與驗證法。

### 2026-08-05 · 第 2 版（首次自動執行前的規格對齊）

建立當天做的維護巡檢，在排程第一次無人值守執行（08-06 09:01）之前修掉四處問題。**四項全部是「兩份文件不同步」或「規格有洞」，沒有一項是程式錯誤**——這印證了第 1 版開頭那句話：不同步是這類系統最常見也最難察覺的故障。

- **`reading` 長度統一為 200–520 字。** 原本第 4 節散文版與排程 prompt 都寫 250–420，只有第 7 節檢查腳本寫 200–520。已經造成實害：2026-08-05 軌道圖的 `reading` 是 429 字，依散文規範超標，但腳本放行。**選擇往腳本靠而不是往散文靠**，理由是既有的 `data/*.json` 不得改寫——收緊到 250–420 會讓 08-05 那期永久紅字，等於用一條規則把自己的歷史判成不合格。同時在第 4 節表格下加註「三處必須同時改」，避免下次又只改一邊。
- **補上週末（六、日）的軌道圖規則。** 原本兩份文件都只寫「週末順延」，但沒說順延到哪裡——而排程是一週跑七天，每個週末必定遇到，第一次就是 08-08。**決定不推進輪盤，改做週線複查**：五條軌道綁死週一到週五的唯一目的是「同一條軌道每週固定同一天出現」，週末若接著往下數第六、第七條，一輪之後軌道與星期就全部錯位，跨期可比性歸零——而那正是軌道圖存在的理由。週線複查則能同時滿足「週末有東西可出」與「不動到輪盤錨點」。
- **檢查腳本加入 QA 旗標處置的警示。** 08-05 那期有 2 筆旗標（HY／IG OAS 於 2025-04-03），`about.run` 卻只記錄了費半跌幅的基準差異，兩筆旗標的處置一字未提。第 3.4 節本來就要求處置寫進 `about.run`，但沒有任何機制去驗它。新檢查逐筆比對 `about.run` 有沒有提到該筆的序列名或日期。**刻意設計成警示而非硬失敗**：漏寫處置是紀錄瑕疵不是資料錯誤，用它擋住整期發布不成比例；但印出來就沒人能假裝沒看到。實跑確認它報的正是預期那兩筆。
- **FRED API key 的雙路徑寫進排程 prompt 第 4 步。** 第 3.1 節有完整說明，prompt 卻寫「只用 FRED 與 Yahoo Finance（免 API key）」。照 prompt 走的執行者不會知道有 key 這條路，也就不會察覺自己正在非文件化的退回端點上。已補上 `--check-key` 自我檢查與「沒有 key 也能跑，但要知道自己在退回路徑上」的說明，並在禁令補一條不得把 key 寫進回報。

### 2026-08-05 · 第 1 版（建立）

- 建立本系統。定調為投顧知識庫的第四個上游庫，接在 `advisory-dashboard-daily` 之後執行。
- **五個 slot 的設計理由**：純「當日題材」會讓長期序列斷線，純「固定輪盤」會漏掉當天的大事。所以 1–4 吃當日、5 走輪盤，兩者互補。
- **雙軌產出（互動 HTML + 靜態 PNG／SVG）出自同一個 `Chart` 物件**，因為兩軌若各寫一次必然漂移。`render_day.py` 從 JSON 產圖，JSON 是唯一事實來源。
- **決定不轉載國際媒體圖表，改為重製**。除了版權，更實際的理由是：重製過程本身會抓到媒體的計算基準問題。首日試作即出現一例——費半自 6/22 高點跌幅，我們以 Yahoo `^SOX` 收盤價算得 −21.9%，而當日媒體引用為 −17%～−18.5%，已在圖註標明基準待覆核。
- 加入 `qa_series()` 單日跳動檢查（6σ）。首日抓到 2025/04/03 的 HY／IG 利差跳動，經查為關稅衝擊的真實市場事件而非資料錯誤——檢查有效。
- **已知限制**：FRED 對 ICE BofA 序列只給近三年；Yahoo 期貨為近月合約、轉倉會造成跳動。兩者都寫進第 3 節。
