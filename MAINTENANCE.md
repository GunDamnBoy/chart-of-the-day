> **這份是舊制的文件，2026-08-20 起不再是權威。**
> 每日五圖已接上 `kb-core` 的共用底盤，門檻與規則的家換了：
>
> | 要找什麼 | 現在在哪 |
> |---|---|
> | 每一個數字（篇幅、條數、門檻、時刻） | `kb-core/chart/anchors.json` |
> | 什麼算對的產出 | `kb-core/chart/BRIEF.md` |
> | 取數眉角與可用來源 | `kb-core/chart/SOURCES.md` |
> | 每天怎麼跑 | `kb-core/skills/chart/SKILL.md` |
> | 發布前檢查（原 `tools/check_day.py`） | `kb-core/checks/chart.py`＋`kb-core/tools/chart_verify.py` |
> | 程式（原 `tools/*.py`） | `kb-core/scripts/chart/` |
>
> **這裡寫的指令有一部分已經跑不動了**（`tools/` 已清空，殘骸在 `tools/_to_delete/`）。
> 這份還沒刪，是因為裡面有一些事故紀錄尚未搬完 —— 讀它可以，
> **照它做不行**：兩份規則同時存在時，改到沒在跑的那一份不會有任何徵兆。

# 維護筆記 · 每日五圖

> 每次動這套系統，把學到的東西寫回這裡。**寫「為什麼」，不要只寫「改了什麼」。**

## 1. 動手前必讀

- **同步組是三份，不是兩份**：`AGENT_BRIEF.md` ＋ 排程 prompt（`~/Documents/Claude/Scheduled/chart-of-the-day-daily/SKILL.md`）＋ **`/chart-maintain` 維護 skill**。
  只改一邊 = 製造下一次故障。2026-08-10 稽核：brief 與 prompt 天天在同步，**維護 skill 六天沒動、12/12 項與現實不符**——
  專門抓漂移的工具自己漂得最兇。skill 用 `save_skill`（`overwrite: true`）更新，直接改快取檔不會保存。
  **skill 的鐵律：不持有會變的值**（數字、時間、規則清單一律指向 repo 內文件），只記不變的原則、程序與坑——這樣它才不需要天天同步。
- **不要跑任何 git 指令（含 `git status`）——維護階段與每日執行都適用。**
  本機 `com.kenny.dashpush` 每 180 秒自動 add + commit + push，跑 git 會留下 `.git/index.lock` 把它擋住。
  要看狀態只用 `cat` / `ls` / `grep` / `tail`；**要看有沒有推上去，讀 `.git/logs/HEAD`（純讀檔，安全）或直接抓 GitHub Pages 上的檔案比對**。
  **前提：本 repo 必須在 dashpush 的監看清單內**——不在的話這條禁令會讓產出永遠留在本機。見第 2 節最後一列。
- **不要用相對路徑讀寫檔案。** `~/advisory-knowledge-hub`、`~/podcast-knowledge-digest` 與本 repo 有多個同名檔案。
- **不要改寫既有的 `data/YYYY-MM-DD.json`。** 歷史是這套系統的資產。

## 1.1 每次改動都要留下可回溯的紀錄

**git log 在這個 repo 沒有考古價值**——dashpush 每 180 秒自動 commit，訊息一律是
`chore: update <時間戳>`，而規範又禁止跑 git。**`CHANGELOG.md` 就是唯一的稽核軌跡。**

改完一版，**把新的一筆寫在 `CHANGELOG.md` 最上面**，並把 `AGENT_BRIEF.md` §8 那一行版號與日期改成新版。
每筆都要有這五欄（格式見 `CHANGELOG.md` 開頭）：

| 欄位 | 寫什麼 | 為什麼需要 |
|---|---|---|
| **動到哪些檔** | 逐檔列出，**含 repo 外的東西**（如 `~/.dashpush/`、cron、`~/houseview`） | 回溯時要知道碰哪幾個檔；跨 repo 的變更最容易漏 |
| **量測** | 改動前後的具體數字 | **沒有數字就沒有比較基礎**，下次優化無從判斷這次有沒有效 |
| **怎麼驗的** | 當時實際做的驗證 | 日後出問題，先看是不是驗證沒涵蓋到那個情境 |
| **怎麼倒回去** | 具體還原步驟，**含順序依賴** | 例如 v3 要先把 repo 移出 dashpush 清單再恢復 git 步驟，反過來會撞 `index.lock` |
| **當時已知的風險** | 明知有風險仍上線的部分 | **日後出事先查這一欄**——多半就在裡面 |

**散文的「為什麼」照舊要寫**，五欄是補上「可比較」與「可倒回」，不是取代它。

〔2026-08-17 v30 改。原制度是「brief §8 留最新一筆、加新版時把上一筆搬過去」。**那條規則自己會洩漏**：2026-08-09 才把 13,000 字元搬走，到 v29 時「最新那一筆」又長成 1,273 字元、每天被執行輪次完整讀一次而零執行用途。**留一格就會有人把那一格填滿**——改成 §8 只容得下一行版號與日期，沒有再長回去的空間。〕

### 量測值要當場量，估算值要標記

v12 的變更紀錄把每日手打 `series` 寫成「~34,000 字元」——那是**單日抽樣的估算**。v13 首輪實測回頭查前四期，實際是 37,507／53,201／58,580／132,698，**中位約 56k、上限 133k，原估算偏低約四成**。估算偏低會讓下一次優化誤判「這條沒什麼好省的」。

- **當場量得到的就當場量**，不要事後回推。
- **真的只能估，就在數字前面加 `~` 並寫清楚估法**，日後才知道那個值有多可信。
- **更正舊量測值時要留痕**（像 v12 那樣加〔更正〕註記），**不要靜默改寫**——量測表的價值在於可比較，而被偷偷改過的數字沒有比較價值。

### 稽核報告放 repo 根目錄，並從 CHANGELOG 引用

`TOKEN_AUDIT_YYYY-MM-DD.md`。它們是量測表那些數字的原始證據，**分開存是因為每份都很長、而且只有優化時才需要看**——同樣是「每日必讀 vs 維護才讀」那條分類。在 CHANGELOG 對應版本的量測欄裡引用檔名即可。

## 2. 已知坑（執行者當天用得到的）

**這一節是每日必讀。** 每一列都是執行當天可能撞到、而且撞到時要立刻做決定的事：取數限制、單位換算、判讀紀律。

**已經修進程式碼、執行者無從行動的缺陷史在 §2.2**——那一節每日執行不必讀。

| 現象 | 原因 | 處置 |
|------|------|------|
| 融資張數與融資金額方向相反 | 金額同時受股價影響，不是資料錯誤 | 2026-08-04 實測：張數 −88,687 張但金額 +63.79 億。**引用時要說清楚講的是哪一個**，媒體說「融資餘額」通常指金額 |
| Yahoo 的 `^TWOII`（櫃買指數）落後數週 | 2026-08-08 實測只到 07-17，**落後 22 天**，已達第 3.5 節硬失敗等級 | 該圖直接改題，不要硬出。證交所／櫃買端點**不允許跨源**，但**同源可用**（2026-08-11 實測，一次取得 57 個交易日）——先 `navigate` 到 `www.twse.com.tw` 再 fetch 即可 |
| **Yahoo 期貨日線整日空值，或日期標記與結算日對不上** | 2026-08-06 實測：`GC=F`／`BZ=F`／`CL=F`／`NG=F`／`HG=F`／`ZC=F` 在 08-05 全部為空值；且 `GC=F` 的 08-04 值 4,095.4 對不上該日 COMEX 結算價 4,152.6，反而接近 08-03 的水準 | 空值該日直接跳過並寫進 `window.note`。**黃金改用 `GLD`（紐約證交所收盤，無標記爭議）**，但圖註要寫明含年化約 0.4% 管理費耗損、水準不等於每盎司金價。**不要照原樣出圖再用「轉倉」搪塞**——1% 以內才是轉倉的量級 |
| **QA 對流量序列固定誤報** | 買賣超、淨流量穿越零時分母趨近 0，百分比爆炸。08-11 外資買賣超 −5.1 → +323.8 億算出 **−6449%**，序列完全正確 | `qa_series` 自動偵測「同時有正負值」改用絕對變動的 z 分數，並標 `abs_chg`。**會固定誤報的檢查比沒有檢查更糟**——它每天消耗一次判讀，然後把人訓練成忽略紅字 |
| **執行時誤用 `/tmp` 的同名舊腳本，覆寫了已發布的當期 JSON** | 撰稿用的暫存腳本命名為 `/tmp/mk.py`，與前一輪殘留的同名檔撞名；`cat >` 因權限失敗，`python3 /tmp/mk.py` 卻照樣跑了**舊的那一份**，把 `data/2026-08-12.json` 覆寫成 15KB 空殼，180 秒內被 dashpush 推上 Pages | **暫存腳本一律寫在 outputs 且檔名帶日期**（`cotd_make_YYYYMMDD.py`），並確認寫檔成功再執行。還原方式：直接讀 `.git` 鬆散物件（`zlib.decompress`，**不執行任何 git 指令**）→ 找覆寫前最後一個 commit 的 tree → 取出原始 blob 寫回。**Pages 不是備份**，它跟著 daemon 走，唯一可靠的還原點是 `.git` 物件 |

## 2.1 建置當天踩到的兩個坑（下次開新知識庫會再遇到）

- **`git push` 回 403 而不是 404。** 帳號的 personal access token 都是 **fine-grained（repository-scoped）**，
  一把只授權一個 repo。新建的 repo 不在任何一把的清單裡，所以 GitHub 認得你也認得 repo，就是不給寫。
  修法：到 `https://github.com/settings/personal-access-tokens/17743798`（`home-mac push`）
  把新 repo 加進 Repository access，並確認 **Contents: Read and write** 與 **Workflows: Read and write** 兩項都有
  ——commit 裡有 `.github/workflows/deploy.yml`，少了 workflow 權限會單獨擋掉那個檔案。
- **第一次 push 後 workflow 10 秒就失敗**，錯誤是 `Get Pages site failed`。
  這不是程式問題：repo 的 Pages 還沒啟用。到 Settings → Pages → **Source 選 `GitHub Actions`**（預設是 Deploy from a branch），
  再 Re-run jobs 即可。**順序是先開 Pages 再跑 workflow**，反過來一定失敗。

## 2.2 程式缺陷史（維護才讀）

**每日執行不必讀這一節。** 這裡每一列都已經修進 `tools/*.py`、`index.html` 或檢查腳本，
執行者讀了無從行動；讀者是下一個要改那些檔案的人——**改之前先看這裡有沒有同類的坑**。

同一類根因在這一節反覆出現，那才是它的價值：**雙軌漂移**（改了靜態軌忘了互動軌）、**提前返回的分支**、
**守門自己變成雜訊**、**否定訊號要具名**、**改一半比沒改更難發現**。

| 現象 | 原因 | 處置 |
|------|------|------|
| 圖上中文變成方框或日文字形 | matplotlib 只登記 `.ttc` 的第一個 face（日文） | `chartkit._ensure_cjk_font()` 會抽出 TC face 到 `~/.cache/`。**字型檔不要進 repo，單檔 17MB** |
| 面積填色把線壓在圖的上緣 | 面積會填到 y=0 | `chartkit` 已在雙軸時自動改為線圖 |
| 證交所某支端點回「stat=OK 但 data 為空」 | **同一個 API 家族的回應形狀不一致**：`bfi82u` 資料在頂層 `data`，`margin` 在 `tables[]` | `fetch_twse.fetch()` 兩種都吃；都不是時會印出回應頂層鍵，直接看得出資料在哪個鍵 |
| **常數的註解說謊，而且沒有任何機制會發現** | `ACCENT = "#C8102E"  # 台新紅` —— 值不是台新紅（品牌紅是 **#D70C18**，見 `tsholdings.com.tw` 的 logo SVG fill）。**根因是配色規則從來沒離開過那行註解**：brief 與排程 prompt 在第 6 版之前完全沒有任何一條顏色規範，所以沒有第二份文件可以對照 | 規則寫進 `AGENT_BRIEF.md` 第 4 節「圖表配色」。**凡是只存在於程式註解的規則，等於沒有規則**——它不會被比對、不會被檢查、也不會被下一個人讀到 |
| **死碼混在有效常數裡** | `POS, NEG = "#1F7A4D", "#C8102E"` 定義在 `chartkit.py` 卻**全 repo 從未被引用**，而且 `NEG` 的值與 `ACCENT` 完全相同 | 已刪除。危險之處在於它看起來像是「正負值分色已經實作了」——**沒實作比實作錯還隱蔽**，因為讀程式的人會以為那件事有人處理過 |
| **schema 的 fallback 沒寫進文件，等於不存在** | `chartkit` 的 `s.color or PALETTE[i % len]` 一直都能自動配色，但 brief 沒記載，2026-08-05 那期九條序列 100% 手寫 color——**七條等於預設（白工），兩條是刻意的主角互換** | brief 第 4 節已明訂「`color` 預設留空」，並已清空那七條。**沒被文件記載的便利功能不會有人用**，而手寫的那些會讓換配色從改一行變成逐條手改 |
| **只實作一半的機制被當成規則的依據** | 新訂「正負值靠 `zero_line` 與資料標籤表達」，但 `zero_line` 只有靜態軌（`render_static` 的 `axhline`）有，`echarts_option()` 從頭到尾沒提過它——**網頁上的讀者看不到零線，PNG 上看得到** | `echarts_option()` 已補 `markLine` 零線。**訂規則前先確認它依賴的機制在兩軌都存在**——這套系統是雙軌產出，「有實作」要問的是「哪一軌」 |
| **加了角色卻沒加守門** | `PALETTE` 只有四個角色，第 5 條序列會 `PALETTE[4 % 4]` 繞回主角紅，一張圖出現兩個主角，而檢查腳本原本完全沒有序列條數的限制 | 檢查腳本已加：>4 硬失敗、==4 警示。**規則沒有守門就是交給運氣**，尤其是這種「多加一條看起來沒事」的錯 |
| **網頁上某條線位置不對或整條消失，但 PNG 是對的** | **ECharts 的 category 軸按「位置」貼資料，不是按日期。** 序列長度與軸不同就整條位移；`render_static` 用真實日期畫所以正確——典型的雙軌漂移 | `echarts_option()` 已改為取所有序列日期的**聯集**當軸、依日期對位、缺值補 `None` 並開 `connectNulls`。**回補舊期用 `tools/rebuild_option.py`，不要用 `render_day.py`**（後者會重算 `qa_flags`，與 `about.run` 已寫的處置對不上） |
| 參考線（中位數、門檻）在網頁上看不到 | 同上。常數序列點數少，被擠在最左邊一小段 | 修正後只要日期涵蓋圖的頭尾就會連成完整橫線，**不必補滿整條序列長度**。日期必須是真實日期 |
| **開站看到的是最舊那天，要手動拉選單才看到今天** | `idx.days.slice().sort().reverse()` —— **`.sort()` 沒帶比較函式會把物件轉成字串**，`[object Object]` 全部相等使 sort 成為 no-op，`.reverse()` 就把「新→舊」翻成「舊→新」 | 改用 `sort((a,b)=>b.date.localeCompare(a.date))`，並**不要倚賴 index.json 的既有順序** |
| **PNG 頁尾的 `source`／`note` 被截斷，而且看不出來** | `render_static` 原本用 `fig.text()` 單行輸出，**沒有任何換行機制**，超出圖框直接被裁。既有 `2026-08-05` 有一筆 `note` 視覺寬 **224**（可容納約 120），等於砍掉快一半。**PNG 是 House View 月報直接取用的檔案，所以那是實際流出去的缺陷，不只影響網站** | `chartkit._wrap_vis()` 已依視覺寬度斷行、頁尾上限三行，超過會留**看得見的刪節號**。檢查腳本已加守門：>3 行硬失敗、==3 行警示。既有各期已用 `rebuild_option.py --png` 重繪 |
| 月頻資料畫成長條圖時柱子細得像針 | `chartkit` 的長條寬度原本寫死 `width=1.0`（＝**一天**）。日頻正確，月頻的點距是 30 天就會這樣 | 已改為取相鄰日期中位距的 0.8 倍。**任何「寬度」常數都要問清楚單位是什麼**——1.0 在時間軸上不是「一格」而是「一天」 |
| `last_updated` 解析不到，被當成「今天沒有發布」 | 無 key 時要解析 `fred.stlouisfed.org/data/<ID>` 的 HTML，**FRED 改版就會斷** | 解析失敗**大聲拋錯**，不回 `None`。**靜默的否定會讓整個偵測機制無聲失效**——這是本 repo 反覆踩到的同一類坑。長期解法是設 FRED API key 走 `fred/series` 端點 |
| **年增率／月增算出來看起來正常，但基期跨過了資料缺口** | **官方序列會缺月。** 2026-08-09 實測 `CPIAUCSL` 缺 `2025-10`。原本的 `v[i]/v[i-12]` 是**位置對應**——跨過缺口就變成 **13 個月變動**卻仍標示為年增率，受影響 8 個點、合成案例中各偏 **0.26 個百分點**。**在通膨圖上足以改變結論，而數字看起來完全正常** | `build_series._transform` 的 `yoy`／`diff` 對月頻序列改為**按日曆回推**（`_shift_months`），找不到基期就跳過或回 `None`，**不拿相鄰月份頂替**。`yoy` 對非月頻序列直接拋錯。`macro_release.py` 改為轉呼叫，**不再自己留一份** |
| **dashpush 推送成功、reflog 有新 commit，但 Pages workflow 8 秒失敗、網站不動** | **repo 裡有符號連結。** 2026-08-10 實測：`charts/2026-08-11 -> /sessions/.../charts/2026-08-09` 是維護測試留下的。`upload-pages-artifact` 打包時遇到指向樹外／不存在的目標就 **exit 1**，artifact 完全沒產生。徵狀組合是「8 秒、exit code 1、Artifacts 顯示 –」 | `check_day.py` 已加守門，全 repo 掃 `os.path.islink` 就硬失敗。**注意 `rm` 在沙箱可能回 `Operation not permitted`**，要先用 `allow_cowork_file_delete` 開權限 |
| **維護測試把東西寫進了真的 repo** | 測試時常建暫存 repo 並 `os.symlink(真實目錄, 暫存/子目錄)` 省得複製。**但之後在那個暫存子目錄裡建立任何項目，都會穿過連結寫進真的 repo。** 上面那個壞掉的連結就是這樣來的 | **暫存目錄裡只做讀取，不要建立新項目**；真要寫就整份複製而不是連結。每次維護結束前跑一次 `find . -type l -not -path './.git/*'` |
| 規範說「腳本只需要被跑、不需要被讀」，執行者卻還是讀了 | **散文版沒寫檢查的觸發條件**，執行者要避開警示就只能去讀 `check_day.py`——**規範的資訊落差會讓外移的省量無法兌現** | brief 第 4 節表格下方已補三條觸發條件（`window.note` 雙向比對／`M/D` 對最短序列／頁尾 3 行上限）。**外移一份文件之前，先確認留下的那份夠不夠用** |
| 每日執行的 token 成本悄悄膨脹 | brief 的變更紀錄與檢查腳本每天被完整讀一次（~21k 字元、零執行用途）；執行者親手打 ~34k 字元的資料點——**而那些數字快取裡本來就有** | 2026-08-09 拆分：紀錄→`CHANGELOG.md`（§8 只留最新一筆）、腳本→`tools/check_day.py`（**唯一權威副本**；brief §7 原留一個 exec 殼，v30 已刪）、資料點→`series_spec`＋`build_series.py`。**每日固定要讀／要寫的東西，每一字元都乘以 365** |
| 貼指令進 Terminal，行尾的 `#` 註解變成參數 | **zsh 互動模式預設不啟用 `interactive_comments`**，`#` 不是註解 | 給／收指令一律不要帶行尾註解，註解另起一行。這不是程式問題，但它偽裝成程式問題（實測時 `FAIL #` 看起來就像取數層壞了） |
| **檢查全綠、檔案都在，網站卻停在舊的一期，且毫無錯誤訊息** | **repo 不在 `com.kenny.dashpush` 的推送清單內。** **新建的 repo 不會自動納入推送清單。** 2026-08-05 實測：本機檔案改完 24 分鐘（8 個 daemon 週期）後 `.git/logs/HEAD` 仍只有建置時那次手動 commit，Pages 上還是舊版 | 清單在 **`~/.dashpush/repos.txt`**（一行一個 repo），`auto-push.sh:34` 讀它、`:53` 逐行載入。**本 repo 已在清單第 19 行。**〔2026-08-05 更新：原本這裡寫「清單寫死在 `auto-push.sh` 的 `set --` 那一行」，那已經過時——同日的修復已把清單抽成資料檔，`set --` 現在只是清空 argv。〕**plist 不用動、`launchctl` 不用重載**——`StartInterval` 每 180 秒重新叫起腳本，存檔後下一輪就生效。**驗證：`tail ~/.dashpush/push.log` 要出現「chart-of-the-day: 已推送 <短雜湊>」**，再抓 Pages 上的檔案比對 |
| **「每期至少一張非折線」實際上只准一種圖型** | `check_day` 只豁免 `scatter` 不需要 `series`，但六種新圖型有五種用 `cats`／`vals`／`groups`／`matrix`／`gauge`——它們一律硬失敗，**唯一過得了關的非折線就是 range_area**（它剛好也帶 series） | 已改為依 `kind` 查該有的欄位（`_NEED`）。**加新圖型時要同時告訴檢查腳本它的資料長什麼樣**，否則守門會把選擇窄化成一種，而且看起來像選題偏好不像 bug |
| **生效日守門寫成 `DAY >= '2026-08-12'` 而恆為 False** | **`DAY` 是檔案路徑不是日期字串**（`REPO + '/data/....json'`），開頭的 `/` 小於數字，比較永遠不成立。八個測試案例全數靜默不觸發、exit code 0 | 期別一律用 `d['date']`。**同一次修正裡 `_lag` 也用了 `DAY`**，`fromisoformat` 拋的 ValueError 被自己的 `except` 吞掉，八個案例又全報「格式錯誤」——**改一半比沒改更難發現** |
| **五張 PNG 都對、`check_day` 全綠，網站卻整頁只剩「載入失敗」** | **新圖型的 ECharts option 從來沒有被真的執行過。** 2026-08-12 首次使用 `gauge`：`markLine: {"data":[{"yAxis": ref}]}` 在沒有直角座標系的 gauge 上會丟 `Cannot read properties of undefined (reading 'getAxis')`，而 `index.html` 只有一層 `boot().catch`，**一張圖丟例外就整頁死掉** | gauge 的參考刻度改用第二條 gauge 疊一小段深色 `axisLine`（`z:10` 蓋在 progress 之上），並把 `splitNumber` 設 1 避免弧線上印出十個刻度數字。**首次使用任何一種新圖型的當天，一定要實載網頁看一眼**——`check_day` 檢查欄位齊不齊，不會把 option 餵進 ECharts。**仍零使用的只剩 `pct_stacked_bar`**（2026-08-17 複核） |
| **網頁上的分組柱形圖整組與 x 軸標籤錯位，PNG 卻正確** | **`barWidth` 是每一條的寬度，不是一整組的寬度。** grouped_bar 兩條 series 各給 62%，一組實佔類別帶 143%（含 barGap），溢出後被 ECharts 往旁邊推。堆疊圖不會有這問題，因為每條疊在同一個位置 | 分組圖改用 `barCategoryGap`（一整組佔類別帶多少）＋`barGap`，寬度交給 ECharts 均分；`barWidth` 只留給 stacked／pct_stacked。**這是軟失敗——不丟例外、數字也對，只有位置錯**，任何自動檢查都照不到，只能靠首用當天實載網頁目視 |
| **網頁上的瀑布圖每根都從 0 往上長，階梯不見了，跌的日子畫成漲的；PNG 正確** | **ECharts 預設把正值與負值分開堆疊**（`stackStrategy: "samesign"`）。瀑布圖靠「透明墊底＋abs() 可見長條」疊出來，墊底是負的、可見長條是正的，兩者因此各走各的堆疊。同一個坑 `_draw_stacked_bar` 的註解 2026-08-09 就記過一次，但只修了靜態軌那一邊 | 兩條 series 都加 `stackStrategy: "all"`（ECharts ≥5.3.3，本站 5.5.0），並讓資料標籤印**原始帶號值**（長條高度是 abs()，直接印會把 −6.35 顯示成 6.35）。**這是最危險的一種軟失敗——圖上的結論與 `takeaway` 相反，卻不丟任何例外** |
| **`check_day` 失敗仍 exit 0** | `bad()` 只設旗標並印 `★`，從不 `sys.exit(1)` | 已加 `sys.exit(0 if ok else 1)`。讀輸出的人看得到 ★，但用 `&&` 串接或看 `$?` 的自動化會被騙過去 |
| **工具自己產出的圖，過不了自己的檢查腳本** | `macro_release.build()` 的 2% 目標線只給頭尾 **2 點**，而 `check_day` 要求 timeseries 每條序列 ≥ **20 點** | 已改為補滿與主序列相同的日期。**凡是「工具產生 → 腳本檢查」的組合，要把工具的輸出實際餵進腳本跑一次**，不能只看兩邊各自的規格 |
| **熱力圖的中文列標籤被切掉左半邊，PNG 才看得出來** | `render_static` 的 `left=0.075` 是給 y 軸刻度**數字**用的；heatmap 的 y 軸放的是「費城半導體」這種五個全形字。互動軌的 `grid.left` 早就是 92，只有靜態軌沒跟上——又一次雙軌漂移 | `_draw_heatmap` 已自行 `subplots_adjust(left=0.155)`。**新圖型首用當天一定要逐張看 PNG**：`check_day` 檢查欄位齊不齊，看不到字有沒有被裁 |
| **兩條末值幾乎相同的線，標籤疊印成一團** | 碰撞判斷的 `frac` 用的是**每條序列自己的 min/max**，等於各自歸一化；美光 319.29（frac 0.66）與希捷 318.90（frac 0.73）差 0.07 剛好大於門檻 0.06，被判為不碰撞。註解寫的是「軸內相對位置」，實作不是 | 改為取**同一軸上所有序列的聯集範圍**再算 frac。**註解描述與實作不符時，錯的通常是實作**——但沒有任何檢查照得到它 |
| **偵測機制在最需要它的時候失效** | `macro_release.py --check` 當初的設計重點就是「不靠人、不靠會過期的行事曆」，但它依賴網路；網路一斷就只能由人重建 | 已知限制，暫無解（預抓涵蓋三大數據的序列，可緩解）。**設計不靠人的機制時，要一併問「它自己依賴什麼」**——依賴鏈上任何一環需要人，整條就需要人 |
| **純大寫的美股代號被送去 FRED，而且被備援藏了九天** | `is_yahoo = any(ch in ident for ch in "^=") or ident != ident.upper()` —— `GLD`／`XLE`／`MU`／`STX`／`2330.TW` 都是純大寫又無 `^=`，判為 FRED；FRED 回 400 後又退回 fredgraph，在發布機上那條不通，每條白等 30 秒逾時。2026-08-14 預抓首跑才發現，**因為前九天都走瀏覽器，這條主路徑從沒被執行過** | 路由改為「快取檔頭 → 啟發式（加 `.`）→ FRED 400 時改試 Yahoo」。**檔頭 `# GLD \| Yahoo (browser) \| ...` 記的是實際成功過的來源，比猜的可靠**。教訓：**長期走備援會讓主路徑無聲腐爛**，連續降級警示就是為了讓這件事被看見 |
| **快取檔頭當路由表，會把你鎖在舊來源上** | 2026-08-14 接證交所官方端點後，台股一律仍被路由到 Yahoo——因為快取檔頭寫著 `Yahoo (browser)`（先前用瀏覽器抓的），而路由以檔頭為準 | 改為**代號樣式明確時樣式優先，檔頭只拆解真正的歧義**（`GLD` 與 `DGS10` 長得一樣）。**檔頭記的是「過去從哪抓到的」，換來源時，過去正是要離開的東西** |
| **回退路徑寫在例外處理裡，被熔斷機制架空** | `GLD`／`XLE`／`MU`／`STX` 判為 Yahoo，Tiingo 寫成「Yahoo 拋 429 後」的回退；但 `prefetch` 的熔斷**在呼叫之前就攔截**，四條全被跳過，回退永遠到不了 | 改在**路由層**決定：判為 Yahoo 的美股個股與 ETF（無 `^` 無 `=`）有 Tiingo key 就直接走 Tiingo。**回退只存在於 except 裡，任何提前中止的機制都會讓它形同不存在** |
| **全新的純大寫代號第一次會白打一次 FRED** | `SOXQ`／`FEZ` 這類沒有快取的美股代號，字面上與 FRED 序列代號分不出來（`GLD` vs `DGS10`） | FRED 回 400 後自動改試 Tiingo，抓到後快取檔頭記住來源，之後直接走對。**一次多餘請求換掉一份要人維護的白名單，划算** |
| **多層回退把原始錯誤變形，外層的判斷因此失效** | `SOXQ`／`FEZ` 不是 FRED 序列，API 正確回 400；但 `fred()` 把 400 也當「API 失敗」而去打 fredgraph，fredgraph 逾時 30 秒，**最後拋出的是逾時錯誤**——外層「400 就改試 Tiingo」的字串比對完全認不出來 | 否定訊號要**具名**：400 改拋 `NotFredSeries`，與「連不上」明確分開。**用字串比對錯誤訊息判斷錯誤種類，只要中間任一層改寫過訊息就會失效。** 驗證時**兩條路都要測**，只驗修好的那條會把退路刪掉而不自知 |
| **警示範圍過寬也是雜訊** | 原本預抓有任何一條失敗就警示，但今日 12 條未取得全是本期沒用到的指數與期貨 | 改為只在**本期實際用到的序列**（`series_spec.id`）落在 `failed`／`skipped` 時才報。**盲區**：沒有 `series_spec` 的圖（scatter、純類別軸）照不到，與序列新鮮度檢查同一個既有限制 |
| **錯誤訊息裡的建議過期了，而它每天都在誤導人** | `_get()` 的 429 提示寫「稍後重跑即可」，但 Yahoo 對本機是**用戶端被擋不是限流**，重跑確定無效 | 訊息改為分辨兩種成因並給不同處置。**錯誤訊息是一種文件，它過期的方式和規格一樣，但沒有人會去稽核它** |

### 關於 dashpush：這個坑會重複發生

`~/.dashpush/auto-push.sh` 的檔頭自己記著 **2026-08-03** 的修復紀錄——`podcast-knowledge-digest` 從 8/02 18:20 起完全沒被推送，同樣是無聲的（launchd exit 0、log 沒有新行、排程照常回報成功、只有網站停在舊版）。**本 repo 在 8/05 中的是同一個 bug，相隔兩天。**

8/03 那次的修法做對了一半：加了「無變更也要寫 log」讓靜默變成可辨識的狀態。但當時根因沒解——repo 清單是寫死的，每開一個新知識庫就會再中一次，而且新 repo 剛建的頭幾天正好最沒人盯著。

因此本 repo 的 `AGENT_BRIEF.md` 第 6 節加了第 10 步「驗證上線」：**每天抓一次 Pages 的 `index.json` 確認 `days[0].date` 是今天。** 這是把「發布成功」從假設變成檢查——不管根因有沒有解，至少不會再無聲。

**已解決**（2026-08-05 同日）：清單已抽成 `~/.dashpush/repos.txt`，加 repo 就只是加一行純文字，各 `*-maintain` skill 可以直接讀它自我檢查涵蓋範圍。`auto-push.sh` 的檔頭有這次的修復紀錄。

## 3. 瀏覽器備援路徑的限制（只在本機直連失敗時用）

- 工具回傳值會被截斷（實測單一欄位約 1,000 字元），**不要試圖把整包資料當回傳值搬運**。
- `finance.yahoo.com` 的 CSP 會擋掉程式觸發的下載；`fred.stlouisfed.org` 第一次可以，第二次會被 Chrome 擋。
- Yahoo 的 CORS 只允許 `*.yahoo.com` 來源，從 FRED 頁面 fetch Yahoo 會失敗。

## 3.1 規格漂移：最容易出事的四個點（2026-08-05 巡檢實測）

首次巡檢在四處抓到不同步或規格有洞，**全部不是程式錯誤**。下次巡檢先查這四項：

| 檢查點 | 為什麼容易漂 | 現況 |
|---|---|---|
| `reading` 長度 | 散文版（brief §4 表格）、`tools/check_day.py`、排程 prompt 第 6 步**共三處**，改的人通常只改看得到的那一處 | 已統一 **200–620**（2026-08-17 複核；當初往腳本靠訂為 200–520，因為既有 JSON 不得改寫，上限後來放寬到 620）。**其餘三個長度欄位是兩處不是三處**，brief §4 表格下方已逐欄寫明 |
| 週末軌道圖 | 輪盤只定義週一到週五，排程卻一週跑七天 | 已定義為**週線複查**（不推進輪盤，避免軌道與星期錯位） |
| QA 旗標處置 | 規範要求寫進 `about.run`，但沒有機制驗證 | 檢查腳本已加**警示級**逐筆比對（不擋發布） |
| FRED API key | brief 第 3.1 節有雙路徑，prompt 曾寫「免 API key」 | 已同步進 prompt 第 4 步，含 `--check-key` |

**教訓：凡是同一個數字或規則出現在兩個以上的檔案裡，就要在文件裡明寫「共幾處、分別在哪」**，否則下一個人一定漏改。brief 第 4 節表格下方已加這樣的註記。

## 4. 待辦與觀察中

- [x] **在 Mac 上驗收台股與 Tiingo 兩條新來源（2026-08-14 完成，全數通過）。**
      台股三端點各 22 個交易日、量級正確；Tiingo key 可用（SPY 777.88）。
      預抓 13/43 → 27/43，**台股 7 條末日為當日，比 Yahoo 還新**。
      驗收同時抓出兩個路由缺口（見 §2），已修。
- [x] **安裝 launchd 讓預抓每天自動跑（2026-08-14 完成）。**
      `launchctl list` 顯示 `- 0 com.kenny.chartfetch`（已載入、未在跑、上次結束碼 0）。
      `RunAtLoad=false` 所以安裝當下不觸發，**第一次自動執行是次日 11:00**。
- [x] **驗收 launchd 是否真的觸發（2026-08-16 完成，成功）。**
      11:00:00 準時觸發、跑 8 分 53 秒、34/46，該期 `data_path = prefetch`，**全鏈路無人介入**。
      同時發現「預抓不是降級」的語意問題，見 §2 與 brief v28。
- [ ] ~~2026-08-15 驗收 launchd 是否真的觸發。~~ 看三處：`~/.chartfetch/prefetch.log`
      有沒有 11:00 那筆起訖紀錄、`check_day` 印的預抓狀態是不是「30 小時內」、
      執行輪次回報的 `about.data_path` 是不是 `prefetch`。
      **注意一個時序風險**：若 Mac 在 11:00 睡著，launchd 會延到喚醒才跑，
      **可能落在 11:31 那輪之後**——屆時該輪會用到前一日的快取。
      `_prefetch_status.json` 的時間戳與序列新鮮度檢查會讓這件事看得出來，不會靜默。
- [x] **確認 Yahoo 的 429 是暫時還是永久（2026-08-16 複測定案：永久）。**
      兩天後、單一標的 `^GSPC`、零流量，**第一個請求仍 429**。已登錄 `fetch.BLOCKED`，
      改用哨兵機制每天只試一條。**十二條缺口全部有替代品**（FRED 等價、ETF 代理、證交所、Tiingo）。
- [ ] **留意哨兵有沒有通。** `prefetch` 在 `^GSPC` 成功時會印 `★`，
      但那行**只在 stdout，不進檢查也不進回報**——每隔一段時間看一次 `~/.chartfetch/prefetch.log`。
      若通了，代表可以把幾條指數換回 Yahoo（**少一天延遲**，對「當日事件反應」的圖有差）。
- [x] **安裝並驗證本機預抓（`com.kenny.chartfetch`）——首跑已完成（2026-08-14）。**
      結果 13/36：FRED 13/13 全通（key 正常），Yahoo 全 429，9 條因路由 bug 被誤送 FRED（已修）。
      **launchd 尚未安裝**，待 Yahoo 狀況釐清後再裝。原始待辦內容：
      沙箱對外連線自 2026-08-06 起被擋且仍在，主路徑實質失效。`tools/prefetch.py`、
      `tools/prefetch-launchd.sh`、`tools/com.kenny.chartfetch.plist` 已備妥但**尚未在真機驗證過**
      （沙箱無網路，測不了）。安裝步驟見 brief §3.3；裝好後先手動跑一次
      `python3 tools/prefetch.py`，確認 `data/_prefetch_status.json` 產生且失敗數為 0。
      **在真機跑通之前，不要把它當成可依賴的路徑**——排程 prompt 目前寫的是「有就用、沒有就往下走」。
- [ ] **觀察沙箱對外連線會不會恢復。** 若某天 `check_day` 不再印連續降級警示，代表主路徑活了，
      屆時預抓仍值得留著當快取層（省去重複打外部站台），但緊急程度下降。

- [ ] **為 `echarts_option` 加一道會真的執行的守門。**
      2026-08-12 的 gauge 事故證明「JSON 欄位齊全」與「網頁跑得起來」是兩件事，而目前只檢查前者。
      可行做法是在 repo 帶一份 headless 執行環境（Node ＋ ECharts）對當日五張 `option` 各跑一次 `setOption`，
      只要丟例外就硬失敗。**在那之前，首次使用任何一種新圖型的當天必須人工實載頁面確認。**
      **零使用的只剩 `pct_stacked_bar` 一種**（2026-08-17 實測全期封存：`heatmap` 已用 2 次、`stacked_bar` 1 次、
      `waterfall` 1 次、`gauge` 1 次、`grouped_bar` 5 次、`range_area` 5 次）。**首用當天實載頁面這一條只剩它還沒兌現。**

- [ ] **2026-09 第一週：確認 DB Research Chart of the Day 是否復刊。**
      2026-08-03 那期內文明寫「This will be the last CoTD until September」（Jim Reid 休假撰寫年度長期資產報酬研究）。
      在復刊前它取不到可用題材（現存最新一期已超過 slot 3 的七天上限），**掃到沒有新東西是預期行為**。
      若九月中旬仍未復刊，就把它從 brief §2 的優先掃描清單移到「暫停中」，免得每天白掃一次。
- [ ] **觀察 `chartsofthe.day` 會不會讓 slot 3 變成翻譯。**
      它本身就是「每日一圖」產品且最好抓，**最省力的做法就是照它的第一則做**——那樣我們只是譯者。
      brief §2 已寫「取它的題目，但問題要自己重新提」，但**這是寫作紀律，沒有守門**。
      看幾期：如果連續出現我們的 slot 3 與它當期第一則同題同角度，就要考慮加規則（例如同一來源不得連續三天）。

- [x] **上游完成時間與本系統起跑時間結構性不匹配（2026-08-06 發現，同日由 Kenny 選 (a) 解決）**
      **決議：排程改為 `0 11 * * *`。** brief 第 1 節方塊圖已改成「07:30 起跑，實測約 10:40 完成」，
      並加註「**寫上游時間一律寫完成時間，起跑時間對下游沒有意義**」——那句才是這個坑的根因。
      15 分鐘等待規則保留為安全網；**若 11:00 仍讀不到當日檔，那是上游出問題**，
      排程 prompt 第 1 步已要求在回報裡明講，不要再當成常態降級。
      下游影響評估：convergence-weekly 週日 21:30、House View 為月度，**兩者都不受 11:00 影響**。

<details><summary>原始待辦與三條路的權衡（保留供對照，2026-08-06 已結案）</summary>

      brief 第 1 節的方塊圖寫著「advisory-dashboard-daily（每天 07:30）→ chart-of-the-day（每天 09:00）」，
      **但 07:30 這個數字與實際不符**。實測六期上游 `stamp`：
      07-30 **08:20**、08-02 20:40（週日）、08-03 **13:10**、08-04 **09:40**、08-05 **10:40**、08-06 **10:40**。
      本系統 09:00 起跑、依 brief 第 6 節第 1 步只等 15 分鐘就放棄，
      **近四個交易日有三天上游還沒完成**——2026-08-06 那期差了整整 70 分鐘（09:30 放棄，上游 10:40 完成）。
      結果是「退回前一日上游」從例外變成常態，而它每次都會在 `about.run` 留下一段註記，
      **看起來像偶發降級，其實是排程設計問題**。
      **三條路，都要 Kenny 選，不要自己選**：
      (a) 本系統排程往後挪到 11:00——最乾淨，但會動到下游 House View 與 convergence-weekly 的取用時點；
      (b) 等待改為「輪詢至 11:00 為止」——不動排程，但無人值守時會佔住執行時段最多兩小時；
      (c) 明訂「以前一日題材為主、當日只補市場數據」——把現況合法化，那 brief 第 2 節
          「slot 1–4 必須是當天的事」就要跟著改寫，否則規範與實務永久打架。
      **在決定之前不要改任何一份文件的 07:30**——那個數字錯了，但改成 10:40 而不動等待邏輯，
      只會讓下一個人以為問題已經處理過。

</details>

- [x] **2026-08-05 兩筆 `so_what` 長度違規（2026-08-06 結案：規範不動、文字不改）**
      `risk-premium-unwind` 59 字、`spx-vix-corise` 42 字，規範 60–120。
      **量過全樣本才決定**：10 筆裡 8 筆在範圍內，分布集中 61–77、中位數 71，
      **自然落點沒有貼著下限的（最接近是 61）——60 這個下限校準得很準**。
      這與 `reading` 那次不同：那次是腳本（200–520）與散文（250–420）**互相矛盾**，
      且既有資料落在腳本範圍內；這次兩邊一致，是資料違規。
      **把下限降到 42 只是為了兩個試作日的資料點移動球門。**
      也不回頭補寫那兩段——`option` 是渲染產物可以重建，**`so_what` 是當天實際發表的分析文字**，
      改它等於讓網站顯示一段從未在那天發表過的判讀。該期 `about.run` 自己就寫著「試作首版」。
      **改為登錄在檢查腳本的 `KNOWN` 字典**，跑 08-05 時會印「已知歷史例外（不必修）」，
      下一個人就不會把它當成新問題去「修好」。

- [x] **`sox-vs-twse` 的 reading 引用 8/4 費半 +6.55%，但 `^SOX` 序列止於 08-03（2026-08-06 結案）**
      違反「數字一律來自我們自己算出來的序列」。**沒有回頭補序列**（那是改寫既有事實），
      改為加一道**向前防止再犯**的檢查：掃 `title`／`subtitle`／`takeaway`／`reading`／`so_what`
      裡的 `M/D` 日期，與該圖**最短那條序列**的末日比對，超過就提示並點名是哪一條序列。
      三個設計決定都是實測逼出來的：
      **(1) 不掃 `watch`**——觸發條件本來就該指向未來日期，那是用途不是違規（08-06 的「8/12」）。
      **(2) 用 min 不用 max**——max 會漏掉本案（台股到 08-05 掩蓋了費半只到 08-03）。
      **(3) 降級為提示並點名短序列**——min 會誤報：08-06 `oil-vs-energy-equity` 的
      「到 8/6 的 79.65」講的是布蘭特（確實有 08-06），短的是 XLE，而 `note` 已寫明「XLE 止於 08-05」。
      **檢查無從得知文字裡的日期指哪條序列，所以它的價值是逼作者確認歸屬，不是替作者下判斷。**
      附帶未解的小不一致：`spx-vix-corise` 的 `hi_pts` 含 2026/08/04 而 `sox-vs-twse` 的標普 500 止於 08-03
      ——**同一支標的兩個末日**；scatter 沒有 `series`，新鮮度與本檢查都照不到它。

- [x] **費半自高點跌幅的基準差異（2026-08-06 結案：純粹是我們的資料落後兩天）**
      08-06 那期的 `sox-round-trip` 抓到了涵蓋 08-04／08-05 的 `^SOX`，**用我們自己的序列即可結案**，
      不必外部第二來源。以同一個高點 198.64 @ 2026-06-22 起算：

      | as-of | 水準 | 自高點 |
      |---|---|---|
      | 08-03（08-05 那期實際用的） | 155.15 | **−21.89%** |
      | 08-04 | 165.31 | **−16.78%** |
      | 08-05（該期發布當天） | 163.00 | **−17.94%** |

      **媒體當日引用的 −17% ～ −18.5%，正好對應 08-04 與 08-05 兩天的收盤。**
      基準日（6/22）與方法（收盤價）**完全相同**，差別只有 as-of 日期——
      所以原圖註寫的「盤中／不同基準日」是錯的歸因，真正原因是**我們的序列落後兩個交易日**。
      前一輪的推估也對得上：當時算「要達 −18.5% 需再漲 4.36%」，實際 +5.06%；
      「要達 −17% 需 6.28%」，實際 08-04 是 **+6.55%**。
      **順帶驗證**：08-05 那期 reading 引用的「8/4 費半 +6.55%」用我們現在的序列算出來
      **一模一樣是 +6.55%**——那個數字本身正確，問題只在於當時它不在該圖序列內、無法自證。
      **不回頭改 08-05 的圖註**（不改寫已發表分析）；防止再犯的機制已就位：
      序列新鮮度檢查（brief 第 3.5 節）＋「文字引用序列涵蓋不到的日期」檢查（第 7 節）。

- [x] **黃金 2026-01-29 → 01-30 單日 −11.4%**（2026-08-05 判定：**不是轉倉**）
      實測 z＝**−5.59σ**，落在舊門檻 6σ 之下所以漏掉。三點證據排除轉倉與錯價：
      (1) 黃金期貨轉倉價差來自持有成本，**正常在 1% 以內**，11.4% 差了一個數量級；
      (2) 錯價的特徵是隔日還原，但它之後停在 4,600–4,900，**沒有回到 5,300**；
      (3) 後續 02-03 的 +6.08% 是獨立的反彈，不是修正回補。
      **處置**：`qa_series` 門檻由 6σ 改為 5σ，並加入「同一序列連續日合併為單一事件」。
      同一份資料由 2 筆變 5 筆，多出來的都是已知真實事件（2024-08-02、2025-04-03/04、2025-05-12）。

- [x] **台股籌碼資料源**（2026-08-05 實測完成，**兩支端點均可用**）
      `tools/fetch_twse.py` 已驗證：`bfi82u` 三大法人（六列聚合、與證交所合計列對帳 20.05 對 20.05）、
      `margin` 融資融券（2026-08-04：融資餘額 5,211.35 億元、日增 63.79 億元、融券 196,970 張）。
      **仍未併進 `fetch.py`**，因為那是每天 11:00 的關鍵路徑；要併的話等它在正式流程跑順幾天再說。
      證交所不允許**跨源**，但**同源可用**：先 navigate 到 `www.twse.com.tw` 再 fetch（2026-08-11 實測）。

<details><summary>原始待辦（保留供對照，2026-08-05 已結案）</summary>

- [~] **台股籌碼資料源**（2026-08-05 首次實測，一半可用）
      `tools/fetch_twse.py` 刻意獨立於 `fetch.py`，避免未驗證的程式碼影響每天 11:00 的關鍵路徑。
      **`bfi82u`（三大法人）已實測可用**：欄位確認為 `['單位名稱','買進金額','賣出金額','買賣差額']`、
      金額單位是元。**但它是六列不是三列**——自營商拆自行買賣與避險、外資拆外資及陸資與外資自營商，
      只取單一列會少算（避險部位 −107.54 億與自行買賣 −86.74 億量級相當）。
      已實作聚合並與證交所自己的合計列對帳（實測 20.05 對 20.05）。
      **`margin`（融資融券）仍待測**：首次因日期參數無效回空，`selectType=MS` 也還沒驗證。
      **下一步**：`python3 tools/fetch_twse.py --selftest 20260804`（帶有效交易日）。
      注意證交所不允許**跨源**；**同源則可用**，先 navigate 到 `www.twse.com.tw` 再 fetch（2026-08-11 實測）。

</details>

- [x] **前端 JSON 體積**（2026-08-05 量測，**原本的問題框架是錯的**）
      單日 105.7 KB，其中 `series` 47.1 KB、`option` 41.0 KB、文字 16.4 KB。
      **體積不隨天數累積**——每天獨立檔、前端一次只載一天，所以「累積到 300KB」這個說法不成立；
      它只隨單張圖的序列長度成長（`credit-spreads` 一張就佔 49 KB，因為 3 年×2 條日資料共 1,569 點）。
      **處置**：不做過早的拆檔優化，改在檢查腳本加體積守門（>250KB 警示、>600KB 硬失敗）。
      **真要瘦身時該拉的槓桿**：`option` 內嵌的資料點與 `series` 完全重複，佔全檔 **39%**。
      讓前端從 `series` 注入資料、`option` 只留樣式，一次省掉四成——但那要同時改
      `render_day.py` 與 `index.html`，在體積成為真問題之前不值得冒這個險。

## 5. 變更紀錄

見 `AGENT_BRIEF.md` 第 8 節。
