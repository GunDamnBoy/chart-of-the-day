# Token 用量稽核 · 2026-08-09（每日五圖第一次跑 v13）

> 這份是 v13（`series_spec` + `build_series.py`、CHANGELOG 分家、check_day 外移）
> **上線後第一輪**的實測。CHANGELOG 的跨版本量測表要的就是這種當場量到的數字，
> 請把結論那節的三個值抄回去。

---

## 1. 量測方法

- 資料源：本次工作階段的完整 transcript（72 次模型呼叫）。
- **算的是「有效 token」不是原始 token**，權重：
  快取讀取 ×0.1、快取寫入 ×2、輸出 ×5、一般輸入 ×1。
  理由：同一份內容留在脈絡裡會被重讀幾十次，
  **只看原始筆數會嚴重低估「多寫一次就要重讀一輩子」的成本**。
- 原始值：輸入 1,177／快取寫入 563,121／快取讀取 12,470,110／輸出 63,095。
- 加權後 **有效總量 2,888,836**。最終脈絡長度 278,505。

分攤規則：快取寫入歸給「該回合新進脈絡的內容」，
快取讀取依當下脈絡的組成比例分攤，輸出歸給 Claude 自己產出的東西。

---

## 2. 這一輪的錢花到哪

| 分組 | 有效 token | 佔比 | 這是什麼 |
|---|---:|---:|---|
| 工具與技能清單（平台自動注入） | 851,550 | 29.5% | 每次注入的可用工具清單、MCP 說明、技能列表 |
| 系統指令與工具定義（每回合重讀） | 580,967 | 20.1% | 開場就 93,374 token 的系統提示與工具定義 |
| 沙箱指令：取數／算圖／檢查 | 580,389 | 20.1% | `bash` 的指令本體與回傳（含 python heredoc） |
| Claude 思考與判讀撰寫 | 360,078 | 12.5% | 五張圖的文字、選題推理、回報 |
| 排程指令與使用者訊息 | 194,598 | 6.7% | 排程 prompt 與系統提醒 |
| 讀規格文件與看圖 | 150,342 | 5.2% | AGENT_BRIEF、MAINTENANCE、工具原始碼、兩張 PNG 覆核 |
| 網路搜尋與抓網頁 | 113,029 | 3.9% | 四次搜尋 + 一次抓 Yahoo 專欄（該頁面 33k 字元是單筆最大） |
| 瀏覽器備援取數 | 43,643 | 1.5% | Chrome 同源 fetch，**28 條序列只花這麼多** |
| 其他 | 14,240 | 0.5% | 任務清單等 |

**第一個重點：真正的工作（取數、算圖、寫判讀）只佔約三分之一。**
前兩列合計 49.6%，是平台固定開銷——執行者改不動，
但它會把「每多讀一份文件」的邊際成本放大，因為所有東西都躺在同一個脈絡裡被重讀 72 次。

---

## 3. v13 到底省了沒：省了，約 14%

### 3.1 `series_spec` + `build_series.py` — 兌現，而且今天是重災日

| | 值 |
|---|---:|
| 今天五張圖的資料點 | 3,913 點 |
| 若手打，`series` 的 JSON 字元 | 92,193 |
| 實際手打的 `series_spec` 字元 | **986** |
| 縮減 | **98.9%** |

換算成有效 token：手打 92,193 字元 ≈ 25,609 個輸出 token，

- 輸出成本 ×5 ＝ 128,045
- 進脈絡的快取寫入 ×2 ＝ 51,218
- 之後約 35 次呼叫每次重讀 ×0.1 ＝ 89,632

**單這一項省下約 268,900 有效 token。**

對照前四期（都是手打）：08-05 為 53,201 字元、08-06 為 132,698、
08-07 為 58,580、08-08 為 37,507。v13 的變更紀錄估「每天約 34,000 字元」，
**實測區間是 37k–133k，中位數約 56k——原估算偏低。**
今天 92,193 字元屬偏高的一天，因為圖 5 用了三條近三年的日序列。

### 3.2 CHANGELOG 分家 — 兌現

`CHANGELOG.md` 現為 42,097 bytes（約 23,400 token），本輪**完全沒讀**。
若照舊內嵌在 brief 第 8 節，會被寫進脈絡並重讀約 65 次：

- 快取寫入 ×2 ＝ 46,774
- 重讀 ×0.1×65 ＝ 152,016

**省下約 198,800 有效 token。** 這一項比想像中大——
**因為它不是「少讀一次」，是「少重讀 65 次」。**

### 3.3 `check_day.py` 外移 — **這一項今天沒兌現**

腳本 12,812 bytes（約 4,900 token）。設計意圖是「只被跑、不被讀」，
但**本輪執行者主動 Read 了它一次**（為了確認長度規範與日期掃描的判定邏輯，
才不會寫出會被警示的文字）。

這不是執行者做錯，是**規範的資訊落差**：
brief 第 4 節的散文版沒有寫清楚「`window.note` 會雙向比對日期」與
「判讀文字的 `M/D` 會跟最短序列比對」這兩條的**觸發細節**，
執行者要避開就只能去讀腳本。

> **建議（不要現在改，等下一次維護一起做）**：
> 在 brief 第 4 節表格下方補三行，把這三條檢查的觸發條件寫成散文——
> (1) `window.note` 的日期集合必須等於各序列末日集合；
> (2) 判讀文字的 `M/D` 會與該圖**最短**序列末日比對；
> (3) 頁尾 `source`＋`note` 合計超過 3 行（每行 120 視覺寬）會硬失敗。
> 補完之後這 4,900 token 才會真的省下來。

---

## 4. 結論

| | 有效 token |
|---|---:|
| 本輪實際 | 2,888,836 |
| 若沒有 v13（估） | 約 3,356,500 |
| **v13 已兌現節省** | **約 467,700（13.9%）** |
| v13 尚未兌現 | 約 4,900（check_day，需補散文規範） |

**答案是：有省，約 14%，而且省的方式是對的。**
省下來的不是「少做事」，是「不再手抄機器本來就有的數字」與
「不再把只有維護才需要的紀錄放進每日脈絡」。
五張圖的內容深度沒有因此變薄——今天反而多做了一件事：
`ma:5` 週線尺度與 CCC 級序列都是新的。

**下一個該動的不是這三項，是第 2 節的前兩列。**
平台固定開銷佔 49.6%，執行者無法直接縮減，
但可以縮小它的乘數——**每一份被放進脈絡的文件都會被重讀 72 次**，
所以「這份文件是每日必讀還是維護才讀」這個分類，
是這套系統目前投報率最高的優化維度。v13 證明了這一點。

### 本輪額外發現，值得寫回 MAINTENANCE

單筆最大的非圖片項目是**抓 Yahoo 專欄那一次的網頁全文（33,227 字元）**，
裡面九成是導覽列與頁尾。重製圖只需要「圖題與論點」，
**抓全頁等於為了兩句話付整份報紙的錢。**
下次可考慮先用搜尋摘要判斷，確定要引用再抓，或抓完立刻只保留需要的段落。

---

*量測於 2026-08-09，v13 上線後第一輪。權重：快取讀 0.1x／寫 2x／輸出 5x。*
