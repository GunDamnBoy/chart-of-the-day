# 變更紀錄 · 每日五圖

> 最新一筆在 `AGENT_BRIEF.md` 第 8 節，其餘依時間新到舊收在這裡。
> **加新版本時，把 brief §8 的上一筆搬過來**，並依下方格式補齊五欄結構化資訊。

## 為什麼這份檔案要寫得這麼細

**因為 git log 在這個 repo 沒有考古價值。** `com.kenny.dashpush` 每 180 秒自動 commit，
訊息一律是 `chore: update <時間戳>`——它記錄了「什麼時候有檔案變了」，但完全不記錄
「改了什麼、為什麼、怎麼驗的、怎麼倒回去」。而維護規範又禁止跑任何 git 指令。

**所以這份檔案就是唯一的稽核軌跡。** 每一版都要能回答五個問題：

| 欄位 | 要回答的問題 |
|---|---|
| **動到哪些檔** | 回溯時要碰哪幾個檔案、有沒有動到 repo 外的東西 |
| **量測** | 改動前後的具體數字。**沒有數字就沒有比較基礎** |
| **怎麼驗的** | 當時做了什麼驗證。日後若出問題，先看是不是驗證沒涵蓋到 |
| **怎麼倒回去** | 具體的還原步驟，含**順序依賴**（例如 v3 要先移出 dashpush 清單再恢復 git） |
| **當時已知的風險** | 明知有風險仍決定上線的部分。**日後出事先查這一欄**——多半在裡面 |

## 跨版本量測（優化時的比較基準）

| 指標 | v1 建立 | v6 配色 | v11 三大數據 | v12 token 優化 | 現況 |
|---|---|---|---|---|---|
| `AGENT_BRIEF.md` 字元 | ~9,000 | ~20,000 | 35,602 | 15,852 | 見下方即時值 |
| 每日手打 `series` 字元 | ~30,000 | ~34,000 | ~34,000 | **0** | 0 |
| 排程 prompt 字元 | ~4,000 | ~8,000 | ~10,900 | ~3,600 | ~3,600 |
| `tools/*.py` 檔數 | 3 | 3 | 7 | 9 | 見下方 |
| QA 門檻 | 6σ | 6σ | 5σ＋合併＋derived | 同左 | 5σ |
| 檢查腳本硬規則數 | 0 | ~12 | ~20 | ~20 | — |

**量測方式**（重跑即可更新現況欄）：

```bash
python3 - <<'EOF'
import glob,os
b=len(open('AGENT_BRIEF.md',encoding='utf-8').read())
print(f"AGENT_BRIEF {b:,} 字元／tools {len(glob.glob('tools/*.py'))} 個檔"
      f"／check_day {len(open('tools/check_day.py',encoding='utf-8').read()):,} 字元")
EOF
```

## 版本索引

| 版 | 日期 | 一句話 | 動到已發布資料？ | 動到 cron？ |
|---|---|---|---|---|
| **12** | 08-09 | token 優化：每日成本砍六成 | 否 | 否 |
| **11.1** | 08-09 | FRED 主備關係其實是反的 | 否 | 否 |
| **11** | 08-09 | 三大月度數據發布日固定出圖 | 否 | 否 |
| **10** | 08-08 | PNG 頁尾靜默截斷；排程再挪 11:30 | **PNG 重繪**（衍生產物） | **是** `0 11`→`30 11` |
| **9** | 08-06 | 費半基準差異結案 | 否 | 否 |
| **8** | 08-06 | 兩條待辦：不動歷史、向前防呆 | 否 | 否 |
| **7** | 08-06 | 排程改 11:00；兩個渲染缺陷；QA 第四類 | **`option` 重建**（衍生產物） | **是** `0 9`→`0 11` |
| **6** | 08-05 | 配色改角色制；修掉「註解說謊」的常數 | **是（唯一一次改寫 `series`）** | 否 |
| **5** | 08-05 | 三個「安靜地錯」的行為 | 否 | 否 |
| **4** | 08-05 | 清四條待辦，兩條原假設被推翻 | 否 | 否 |
| **3** | 08-05 | 發布路徑改 daemon | 否 | 否 |
| **2** | 08-05 | 首次自動執行前的規格對齊 | 否 | 否 |
| **1** | 08-05 | 建立 | —（起點） | 建立 `0 9` |

**「動到已發布資料」那欄要分三級看**：`series`／`about` 是**事實與紀錄**，只有 v6 破例動過一次（經同意，理由是該期為試作首版且是全部歷史）；`option`／PNG 是**衍生產物**，可用 `rebuild_option.py` 重建，事實不變；其餘版本完全沒碰 `data/`。

---

### 2026-08-09 · 第 12 版（token 優化：每日執行成本砍掉約六成）

| | |
|---|---|
| **動到哪些檔** | **新增** `tools/build_series.py`、`tools/check_day.py`、`tools/rebuild_index.py`、`CHANGELOG.md`；**改** `AGENT_BRIEF.md`（§5 schema／§6 步驟／§7 改薄殼／§8 只留最新一筆）、排程 prompt |
| **量測** | brief 35,602→15,852 字元；`series` 手打 ~34,000→0；prompt ~10,900→~3,600；每日總量 −55~60% |
| **怎麼驗的** | `build_series --selftest` ＋ 假快取端到端（align／rebase／spec 保留／手工序列不碰）；`check_day` 三種呼叫方式（直接、env 抽取、舊字面量 hack）；`rebuild_index` 與現況逐欄比對 |
| **怎麼倒回去** | 三項搬移都可逆：CHANGELOG 貼回 §8、`check_day.py` 內容貼回 §7、移除 §5 的 `series_spec` 段並改回手打。**`data/*.json` 完全未受影響** |
| **當時已知的風險** | `series_spec` 改變執行者習慣，第一次實跑前無法確認會被採用 |

本系統每天跑，每一字元的常態成本都乘以 365。量測後對三個大頭動刀，**原則是把「機器早就會的事」從執行者手裡拿走，把「只給維護者看的字」從每日讀取裡拿走**：

- **`series_spec` 機制（省最多，每日 ~34,000 輸出字元）**。執行者原本親手把資料點打進 JSON——而那些數字 `fetch.py` 的快取裡本來就有。現在只寫「代號＋轉換」（`rebase`／`diff`／`yoy`／`ma:N`／`vol:N`），`build_series.py` 實體化。**spec 與 series 並存**，任何一天的圖連轉換方式都可考；`ma`／`vol` 自動標 `derived`，這個常被忘的旗標從此不會漏。手工序列（無 spec）仍合法。
- **檢查腳本移到 `tools/check_day.py`（省每日 ~8,400 輸入字元）**。腳本只需要被跑、不需要被讀。§7 留薄殼，**維護 skill 的 regex 抽取與字面量替換 hack 都實測仍有效**；`CHART_REPO` 環境變數讓沙箱維護不再需要改字串。權威副本改為 `check_day.py`，「三處同步」的第三處跟著搬。
- **變更紀錄搬到 `CHANGELOG.md`（省每日 ~12,500 輸入字元）**。§8 只留最新一筆——維護 skill 靠最上筆日期判斷認知新舊，所以不能全搬走。**加新版本時把上一筆搬過去。**
- 另補 `rebuild_index.py`：索引欄位是前端契約，原本每天由執行者重寫一段掃描碼，同一段邏輯每天重寫既花 token 又可能各天不一致。
- 排程 prompt 同步精簡（~10,900 → ~3,600 字元）：**指向 brief 的節號而不是複述內容**——執行者本來就會完整讀 brief，同一段話存在兩處是純粹的重複成本，而且正是歷次「兩份不同步」的根源。

合計每日約省 55–60% token。**沒動的**：媒體掃描與判讀寫作（那是這套系統的價值本身）、series 完整落檔（歷史可查閱不變）、所有檢查與守門。

### 2026-08-09 · 第 11.1 版（FRED 的主備關係其實是反的）

| | |
|---|---|
| **動到哪些檔** | **改** `AGENT_BRIEF.md` §3.2、`MAINTENANCE.md`（純文件） |
| **量測** | `fredgraph.csv` 逾時（Errno 60）；API `DGS10` 16,134 筆回到 1962；`BAMLH0A0HYM2` 787 筆仍近三年 |
| **怎麼驗的** | 發布機 `fetch.py --check-key` 實測 |
| **怎麼倒回去** | 純文件，直接改回敘述即可 |
| **當時已知的風險** | 無程式碼變更 |

`--check-key` 在發布機上實測：**`fredgraph.csv` 逾時（`[Errno 60]`），`api.stlouisfed.org` 正常。**

第 3.1／3.2 節原本把繪圖端點寫成「沒有 key 時的退路」，那個框架是錯的——**在這台機器上，被當成備援的那條才是壞的**。所以 FRED API key 不是可選的優化，是 FRED 這條線唯一能走的路。已改寫第 3.2 節並在 `MAINTENANCE.md` 加一列。

兩個附帶確認：官方 API 的 `DGS10` 給 **16,134 筆**（回到 1962 年），遠多於繪圖端點；ICE BofA 仍是 787 筆近三年，**第 3.2 節那條限制還成立**——`--check-key` 裡那段「若取得長於三年的歷史就更新文件」的自我檢查，這次證明它會動。

**這也讓第 11 版的 `macro_release.py` 走上好路**：`last_updated` 會用文件化的 `fred/series` 端點，而不是解析 HTML 頁面。HTML 那條備援仍保留，但實務上不會被觸發。

**教訓：主備關係要實測，不能照抄「免認證的比較通用」這種直覺。** 這裡的直覺正好相反。

### 2026-08-09 · 第 11 版（美國三大月度數據發布日固定出圖）

| | |
|---|---|
| **動到哪些檔** | **新增** `tools/macro_release.py`；**改** `chartkit.py`（長條寬度）、`AGENT_BRIEF.md`（§2 新節／§3.1 登錄／§5 加 `about.macro_release`／§7 守門）、排程 prompt |
| **量測** | `PAYEMS.last_updated` ＝ 2026-08-07 8:31 AM CDT（＝BLS 8:30 ET）；長條寬度 1 天→相鄰日期中位距×0.8 |
| **怎麼驗的** | `--selftest`（轉換＋AM/PM 邊界）；守門三情境（無發布／漏圖硬失敗／正確通過）；日頻五張圖回歸重繪 |
| **怎麼倒回去** | 移除 §2 該節與 §7 守門段落；`macro_release.py` 可留著不呼叫 |
| **當時已知的風險** | CPI／PCE 的 `--build` 尚未用真資料驗證過 |

新增能力：**非農、CPI、PCE 發布當日，`當日主圖` 強制是該數據。** 這三個數字是政策路徑的直接輸入，發布日它們本來就是當天最重要的事。

- **偵測不靠行事曆，靠資料自己的更新時間。** `tools/macro_release.py --check` 讀 FRED 序列的 `last_updated`——實測 `PAYEMS` 是 `2026-08-07 8:31 AM CDT`，正是 BLS 的 8:30 ET 發布時刻。**行事曆會過期，而且過期時是靜默的**：沒有人會發現今年的日期表還是去年那份，然後某個月就悄悄漏掉一次。資料的更新時間不會有這個問題。
- **雙路徑取 `last_updated`**：有 FRED key 走 `fred/series` 端點（欄位就叫 `last_updated`）；沒有 key 就解析 `fred.stlouisfed.org/data/<ID>` 頁面。**後者是 HTML 不是文件化 API，所以解析失敗時大聲拋錯而不是回「沒有新發布」**——靜默的否定會讓整個偵測機制無聲失效，那正是這套系統反覆踩過的坑。AM／PM 邊界（12 PM＝12:00、12 AM＝00:30）已單元測試。
- **規則有守門，不是只寫在文件裡。** 新增 `about.macro_release` 欄位存 `--check` 的原樣輸出，檢查腳本據此驗證：有 `fresh` 卻沒有對應的 slot 1 就硬失敗，欄位缺漏則警示。**這條規則一個月只觸發約三次，漏掉不會有人發現**——「規則沒有守門就是交給運氣」在這種低頻規則上代價最高。三種情境（無發布／有發布但漏圖／有發布且正確）都實測過。
- **`--build` 刻意不產文字。** 它只把序列、軸、來源備好，`title`／`takeaway`／`reading` 一律留空，另附 `_hint` 帶算好的關鍵數值。**判讀要看當天的實際數字寫，工具產罐頭句只會讓五張圖裡有一張永遠沒有觀點。**
- 附帶修掉 `chartkit` 的一個限制：**長條寬度寫死 1.0（＝一天）**，畫日頻沒事，但月頻資料的點距是 30 天，柱子會細成一根針。改成取相鄰日期中位距的 0.8 倍；日頻五張圖回歸測試通過。
- 2% 目標線用常數序列實作——**這靠的是 08-06 修好的「ECharts 依日期對位」**，在那之前常數序列會被擠在圖的最左邊 1.7%。舊修正讓新功能少寫一套機制。

### 2026-08-08 · 第 10 版（PNG 頁尾靜默截斷、排程再挪 11:30、兩條取數路徑修正）

| | |
|---|---|
| **動到哪些檔** | **改** `chartkit.py`（`_wrap_vis`／`FOOT_W`／`FOOT_MAX_LINES`）、`tools/rebuild_option.py`（加 `--png`）、`AGENT_BRIEF.md` §3.3、`MAINTENANCE.md`、cron `0 11`→`30 11` |
| **量測** | 08-05 一筆 `note` 視覺寬 224（單行容量 120）→ 斷成 2 行；四期 20 張 PNG 重繪；上游完成 10:40→11:20 |
| **怎麼驗的** | 渲染後**目視確認**三行全在框內；事實與紀錄指紋逐期比對未變 |
| **怎麼倒回去** | 移除 `_wrap_vis` 呼叫改回單行 `fig.text`；PNG 用 `rebuild_option.py --png` 再繪即可 |
| **當時已知的風險** | 頁尾三行上限是估計值，未測極端長度 |

**排程由 11:00 挪到 11:30。** 上游完成時間仍在往後漂：08-06 10:40 → 08-08 **11:20**，已經晚於起跑點，那輪是靠 15 分鐘等待才接住、安全邊際歸零。第 1 節現在保留完整的時間線而不只是最新值——**因為判斷「還要不要再挪」靠的是趨勢，不是單一數字**。同時記下：若 11:30 又接不住，先查上游來源家數是否又變（實測在 24／25／26 之間變動過），**一直往後挪只是在追症狀**。


08-08 那輪回報了兩個實測發現，其中一個是**已經流出去的缺陷**。

- **PNG 頁尾沒有換行機制，超出圖框直接被裁且不留痕跡。** `render_static` 原本用 `fig.text()` 單行輸出 `source` 與 `note`。既有 `2026-08-05` 有一筆 `note` 視覺寬 **224**（單行約容納 120），等於在已發布的圖上砍掉快一半。**嚴重性在於 PNG 正是 House View 月報直接取用的檔案**——這不是只影響網站的呈現問題，是實際交付出去的內容缺了字。新增 `_wrap_vis()` 依視覺寬度斷行（中文沒有空白可斷，所以逐字元累加而非 `textwrap`），頁尾上限三行，超過會留**看得見的刪節號**——因為這次要修掉的正是「靜默」，用另一種靜默取代它沒有意義。檢查腳本加守門：>3 行硬失敗、==3 行警示。既有四期已用 `rebuild_option.py --png` 重繪，**事實與紀錄的指紋逐期比對確認完全未變**，`files` 欄位也沿用原檔名不重新編號。
- **Chrome 下載配額的單位是分頁，不是站台。** 第 3.3 節原本寫「同一站台第二次自動下載會被擋」，實測更精確的是**每個分頁放行一次，開新分頁重新導覽後配額重置**。同時修正：`finance.yahoo.com` 的 CSP **一次都不放行**，但 `query1`／`query2.finance.yahoo.com` 可以。**單位寫錯的代價是照著做也會失敗，而且失敗是靜默的**——執行者會以為是網路問題。
- 另記兩條資料源限制進 `MAINTENANCE.md`：Yahoo 的 `^TWOII` 實測落後 22 天（達硬失敗等級，該期因此改題）；**FRED 的公債殖利率比 Yahoo 慢一個交易日**，講「當日事件反應」的圖要用 Yahoo，否則剛好漏掉那件事本身。

### 2026-08-06 · 第 9 版（費半基準差異結案：懸了兩天的問題，答案在自己的資料裡）

| | |
|---|---|
| **動到哪些檔** | **改** `MAINTENANCE.md`（待辦→結案）、`AGENT_BRIEF.md` §8。**無程式碼變更** |
| **量測** | 同一高點 198.64@06-22：08-03 −21.89%／08-04 −16.78%／08-05 −17.94%，媒體 −17~−18.5% 落在後兩者 |
| **怎麼驗的** | 用 08-06 那期自己的 `^SOX` 序列重算 |
| **怎麼倒回去** | 純文件 |
| **當時已知的風險** | 無 |

`MAINTENANCE.md` 唯一剩下的未結案項目。08-06 那期的 `sox-round-trip` 抓到了涵蓋 08-04／08-05 的 `^SOX`，**用我們自己的序列就能結案，根本不需要外部第二來源**。

以同一個高點 198.64 @ 2026-06-22 起算：08-03 收 155.15（−21.89%）、08-04 收 165.31（−16.78%）、08-05 收 163.00（−17.94%）。**媒體當日引用的 −17% ～ −18.5% 正好對應 08-04 與 08-05 兩天的收盤。** 基準日與方法完全相同，差別只有 as-of 日期。

**所以原圖註寫的「盤中／不同基準日」是錯的歸因**——真正原因單純是我們的序列落後兩個交易日。08-05 那輪的推估也對得上：當時算「要達 −18.5% 需再漲 4.36%」，實際 +5.06%；「要達 −17% 需 6.28%」，實際 08-04 是 +6.55%。順帶驗證了 08-05 那期 reading 引用的「8/4 費半 +6.55%」——用現在的序列算出來一模一樣，**那個數字本身正確，問題只在於它當時不在該圖序列內、無法自證**。

**這條懸案的教訓不是「要找第二來源」，而是「先確認自己的資料是不是最新的」。** 兩天前把它歸因為計算基準差異，是因為沒有人檢查序列的末日；第 3.5 節的序列新鮮度檢查正是為此而加，而它一加上去就立刻指出了真正的原因。**不回頭改 08-05 的圖註**，該期的計算基準說明維持原狀。

### 2026-08-06 · 第 8 版（清掉兩條積欠的待辦，兩條都選擇「不動歷史、向前防呆」）

| | |
|---|---|
| **動到哪些檔** | **改** 檢查腳本（`KNOWN` 字典／日期參數／`DATE_TOK` 引用日期檢查）、`AGENT_BRIEF.md` §4、`MAINTENANCE.md` |
| **量測** | `so_what` 全樣本 10 筆中 8 筆合規、分布 61–77、中位 71 → 判定 60 下限校準正確 |
| **怎麼驗的** | 對 08-05（真違規）與 08-06（誤報案例）分別驗證報得對不對 |
| **怎麼倒回去** | 移除 `DATE_TOK` 與 `KNOWN` 區塊 |
| **當時已知的風險** | 日期檢查是提示級，無法區分文字指哪條序列 |

`MAINTENANCE.md` 第 4 節積了兩條前輪標為「需 Kenny 決定」的待辦，同日一併結案。**兩條的結論都是不回頭改已發表的內容，改為補上防止再犯的機制。**

- **`so_what` 長度：規範不動，文字也不改。** 08-05 有兩筆 59 字與 42 字（規範 60–120）。**量過全樣本才決定**：10 筆裡 8 筆在範圍內、分布集中 61–77、中位數 71，**自然落點沒有貼著下限的，最接近是 61——60 校準得很準**。這與 `reading` 那次不同：那次是腳本與散文**互相矛盾**且既有資料落在腳本範圍內，這次兩邊一致、是資料違規。**把下限降到 42 只是為了兩個試作日的資料點移動球門。** 也不補寫那兩段——`option` 是渲染產物可以重建，`so_what` 是當天實際發表的分析文字，改它等於讓網站顯示一段從未在那天發表過的判讀。改為登錄進檢查腳本新增的 `KNOWN` 字典，跑該期時印「已知歷史例外（不必修）」，**讓紅字有解釋**，否則下一個人會以為是新問題而去修它。
- **新增「文字引用了序列涵蓋不到的日期」檢查。** 起因是 08-05 的 `sox-vs-twse` 引用「8/4 費半 +6.55%」而 `^SOX` 只到 08-03——那個數字只能是抄來的。**沒有回頭補序列**（那是改寫既有事實），改為向前防呆。三個設計決定全是實測逼出來的：**不掃 `watch`**（觸發條件本來就該指向未來，08-06 的「8/12」是正當用法）；**用 min 不用 max**（max 會被較長的序列掩蓋，正是本案漏抓的原因）；**降級為提示並點名最短那條序列**——因為 min 會誤報：08-06 `oil-vs-energy-equity` 的「到 8/6 的 79.65」講的是布蘭特（確實有 08-06），短的是 XLE，而 `note` 已寫明「XLE 止於 08-05」。**檢查無從得知文字裡的日期指的是哪一條序列，所以它的價值是逼作者確認歸屬，不是替作者下判斷。**
- **檢查腳本支援指定日期**（`sys.argv[1]`）。原本寫死檢查最新一期，想診斷舊期就得手改路徑、改完常忘了改回來。**把維護時的常見操作變成支援的用法，而不是要人去改腳本。**

### 2026-08-06 · 第 7 版（首次無人值守執行後：排程改 11:00、修掉兩個渲染缺陷、QA 補第四類）

| | |
|---|---|
| **動到哪些檔** | **新增** `tools/rebuild_option.py`；**改** `chartkit.py`（聯集軸／`connectNulls`／`Series.derived`）、`render_day.py`（`to_chart` 帶 `derived`）、`index.html`（`sort` 比較函式）、`AGENT_BRIEF.md`（§2 週末／§3.4 第四類／§3.5）、排程 prompt、cron `0 9`→`0 11` |
| **量測** | 08-06 圖2 中位數線佔寬 1.7%→100%；08-05 台股線位移 8 交易日→0；18 筆旗標中 13 筆歸入 derived |
| **怎麼驗的** | 兩期 `option` 重建後首末日逐條比對；`rebuild_option` 守門確認 `series`／`qa_flags`／`about.run` 指紋未變 |
| **怎麼倒回去** | `chartkit` 改回 `series[0].dates`；`option` 用 `rebuild_option.py` 再建 |
| **當時已知的風險** | 聯集軸會讓 x 軸點數變多（08-05 圖5 146→150） |

第一次真正無人值守執行完成並上線。四項調整，其中兩項是**已經發布到網站上的錯誤**。

- **排程由 09:00 改為 11:00。** 08-06 那輪 09:02 查無當日上游、依規範等到 09:30 仍無，只能退回用前一日題材。上游 advisory 已擴為 26 家來源、約 10:30 才完成——**09:00 這個時間點從一開始就注定會退回**。改 11:00 後當日檔應已存在；若 11:00 還抓不到，那是上游出問題，prompt 要求在回報裡明講。
- **ECharts 的 category 軸是按位置貼資料，不是按日期——這造成線條靜默位移。** 原本 `dates = ch.series[0].dates`、每條序列直接丟 `s.values`，只要長度不同整條就偏移。而 `render_static` 用真實日期畫，完全正確——**所以兩軌不一致，網頁錯、PNG 對**，這正是第 1 版就在警告的雙軌漂移。兩個實例：08-06 圖 2 的中位數參考線只有 42 點、軸有 2,452 點，那條線被畫在最左邊 **1.7%** 的寬度裡（使用者看到的是「線不見了」）；08-05 圖 5 的台股加權有 140 點、軸有 146 點，末端被畫在 2026-07-24 的位置而非 08-05，**整條位移八個交易日**——而那張圖的判讀正是在比較台股與費半的相對走勢。改為取所有序列日期的**聯集**當軸、依日期對位、缺值補 `None` 並開 `connectNulls`，稀疏的常數參考線也會連成完整橫線。
- **前端開站顯示的永遠是最舊那天。** `idx.days.slice().sort().reverse()` —— `.sort()` 沒帶比較函式會把元素轉字串比大小，一堆 `[object Object]` 全部相等使 sort 變成 no-op，接著 `.reverse()` 把 index.json 的「新→舊」翻成「舊→新」。使用者得自己拉下拉選單才看得到今天。改為 `sort((a,b)=>b.date.localeCompare(a.date))`，**同時不再倚賴 index.json 的既有順序**。上一頁／下一頁的 disable 判斷也跟著恢復正確。
- **QA 三分法補上第四類：衍生序列的窗口效應。** 08-06 那期 18 筆旗標中有 **13 筆**全在「黃金 ETF 60 日波動率」一條上——極端值滾出 60 日窗口，指標就跳一階。它不是市場事件、不是轉倉、也不是錯價，**原本的三分法沒有這一格**，執行者只能硬塞或逐筆寫罐頭說明。新增 `Series.derived` 旗標，檢查腳本改為「整條序列說明一次即可」，但**完全不提仍會警示**。順帶修掉 `render_day.to_chart()` 沒把新欄位帶進 `Series` 的漏接——**schema 加欄位而轉換層沒接，是加欄位時最容易漏的一步**。

**關於回補既有 JSON**：前兩項渲染缺陷已影響兩期已發布的資料，因此新增 `tools/rebuild_option.py`，**只重建 `option` 欄位**並在寫檔前守門確認其餘欄位一字未動。**不可用 `render_day.py` 回補舊期**——它會順便把 `qa_flags` 用新門檻重算，而 `about.run` 的處置說明是對著當初那組旗標寫的，兩者對不上就會讓歷史紀錄從「誠實回報」變成「說明與資料不符」。**修渲染不該動到紀錄。**

### 2026-08-05 · 第 6 版（配色改為角色制，並修掉一個「註解說謊」的常數）

| | |
|---|---|
| **動到哪些檔** | **改** `chartkit.py`（`PALETTE` 六色→四角色、新增 `DIM`、刪 `POS`/`NEG`、`echarts_option` 補零線）、`AGENT_BRIEF.md` §4 新增配色節、檢查腳本（序列≤4／`so_what`／`watch`／`tags`）、**破例重畫** `data/2026-08-05.json` |
| **量測** | 主角紅 #C8102E→#D70C18；九條手寫 color 清空七條；檢查腳本新增 4 條規則 |
| **怎麼驗的** | 重畫後全期 PNG 目視；grep 確認 `POS`/`NEG` 全 repo 無引用 |
| **怎麼倒回去** | **改回 #C8102E 會讓 `~/houseview` 的健康檢查 FAIL**——那是刻意的跨庫漂移偵測，回溯時要同步更新那邊登記值 |
| **當時已知的風險** | **唯一一次改寫既有 `data/*.json`**（經 Kenny 同意，理由：該期是試作首版且為全部歷史） |

**這一版的起點是一個事實錯誤，不是設計偏好。** `chartkit.py` 的 `ACCENT = "#C8102E"` 註解寫著「台新紅」——但那不是台新紅。品牌紅是 **#D70C18**（`tsholdings.com.tw` 的 logo SVG `fill`，全站計算樣式 86 處）。#C8102E 接近 Pantone 186，是當初挑錯的值。**註解宣稱的東西和值本身不一致，而且沒有任何機制會發現**，因為配色規則從來沒離開過那行註解——brief 與排程 prompt 兩份文件，在這一版之前完全沒有任何一條關於顏色的規範。

- **`PALETTE` 由六色類別制改為四角色制**：主角 `#D70C18` ／對照 `#1F4E79` ／溢出 `#8A8F95` ／背景 `#B8BBBE`。土黃、綠、紫移出工作集——土黃對白底只有 2.6:1（2pt 細線會偏淡），綠與主角紅在紅綠色盲下只差 17。但真正的理由不是這兩個數字：**一張圖需要第 5、6 個顏色時，該修的是圖不是色。**
- **新增背景色 `DIM`。** 原本的調色盤沒有「退到後面」這個角色，所以做不出「其餘全灰、只有主角有顏色」的圖。每張圖都有 `takeaway`，顏色本來就該讓人一眼看出那句話講的是哪一條。
- **刪掉 `POS, NEG`。** 全 repo grep 確認**從未被任何程式碼引用**，是死碼；而且 `NEG` 的值與 `ACCENT` 完全相同（都是舊的 #C8102E），真拿去畫負值會讓紅同時代表「重點」與「下跌」。正負值改由 `zero_line` 與資料標籤表達——順帶避開綠正紅負是西方慣例、與台灣紅漲綠跌相反的問題。
- **`series[].color` 明訂預設留空。** `chartkit` 的 `s.color or PALETTE[i % len]` fallback 一直都在，但兩份文件都沒記載，結果 2026-08-05 那期九條序列 100% 手寫了 color——其中**七條的值等於程式本來就會給的預設**（白工，且讓換配色從改一行變成逐條手改），只有 `jpy-vs-ust10` 那兩條是刻意的主角互換。已清空七條、保留兩條當示範。
- **序列上限訂為四條，並加進檢查腳本。** `PALETTE` 只有四個角色，第 5 條會 `PALETTE[4 % 4]` 繞回主角紅，一張圖出現兩個主角——**加了角色制卻沒有守門，等於把規則交給運氣**。順帶把 `so_what` 60–120、`watch` 1–3、`tags` 2–5 三條補進腳本：它們原本只存在於第 4 節散文版與排程 prompt，而第 4 節自稱「腳本是唯一可執行的權威副本」，那句話在補上之前是不成立的。
- **`echarts_option()` 補上零線。** 正負值不分色之後，零線是唯一區分正負的視覺元素，但它原本只有靜態軌有（`render_static` 的 `axhline`），互動網頁完全沒畫。**規則押在一個只實作了一半的機制上**，等於在網頁上正負無從分辨。
- **破例重畫 `data/2026-08-05.json`。** 這違反「不改寫既有 `data/*.json`」，經 Kenny 同意：該期是試作首版、且是全部歷史（封存只有一天）。一天的例外，換掉一個「八月上半舊紅、下半新紅」的永久斷層——而 House View 月報每個月正是要從這批 PNG 裡挑圖貼進同一份簡報。**再過兩週不做，成本就從一天變成七十張圖。**

> **跨庫綁定**：這一套與 House View 月報是同一套（`~/houseview/HOUSEVIEW_BRIEF.md` 第 5 節）。那邊的健康檢查會讀本檔的 `ACCENT` 值比對，**本檔一改就會讓那邊 FAIL**——那是刻意的漂移偵測，改完要去更新那邊登記的值。

### 2026-08-05 · 第 5 版（實測回饋：三個「安靜地錯」的行為）

| | |
|---|---|
| **動到哪些檔** | **改** `tools/fetch_twse.py`（日期格式守門／六列聚合／`tables` 形狀／成功但空）、`tools/fetch.py`（429 退避、補 `urllib.error` import）、`MAINTENANCE.md` |
| **量測** | 429 退避 1.5/3/4.5s→20/40s ＋ 標的間隔 1s；三大法人聚合與證交所合計列對帳 20.05 vs 20.05 |
| **怎麼驗的** | mock 三種回應形狀；退避節奏攔截 `sleep` 驗證；真實資料驗聚合 |
| **怎麼倒回去** | 移除 429 分支恢復統一退避 |
| **當時已知的風險** | `urllib.error` 原本靠 `urllib.request` 間接載入，是既有隱患不是新增 |

前一版的兩個模組拿到真實網路環境跑，回饋出三件事。**三件都屬於同一類：系統不報錯，只是給你錯的東西。**

- **證交所會吞掉無效的日期參數。** `dayDate=#` 不報錯、不回 stat 失敗，直接回傳最近一個交易日。要不是參數剛好被汙染，這個行為可能幾個月後才在某次「數字對不上」的爭議裡被發現。`fetch_twse.fetch()` 已在送出前擋掉非 `YYYYMMDD` 的輸入，並要求呼叫端核對回傳 `title` 裡的民國日期。
- **三大法人是六列不是三列。** 自營商拆自行買賣（−86.74 億）與避險（−107.54 億）、外資拆外資及陸資與外資自營商。**只取單一列會少算一半**——避險部位的量級與自行買賣相當。已實作聚合並與證交所自己的合計列對帳（20.05 對 20.05），列名日後被改會直接報錯而非安靜算錯。`bfi82u` 由此標為**已實測可用**；`margin` 仍待測，已改成同時支援頂層 `data` 與 `tables` 兩種回應形狀，失敗時會把頂層鍵印出來，下次一眼就能判斷資料放在哪。
- **Yahoo 429 的退避節奏原本是錯的。** 原本 1.5／3／4.5 秒對限流完全不夠，連打只會讓限流延長。改成 429 專用的 20／40 秒退避，並在 CLI 逐標的之間加 1 秒間隔——連續打同一站台正是招來限流的主因，五個標的多花 4 秒，遠比整輪重跑划算。**錯誤訊息明講「429 是限流不是壞掉」並禁止改用其他來源規避**，因為那是把速率限制當成障礙繞過去，不是我們該做的事。順帶修掉 `urllib.error` 未明確 import 的隱患（目前靠 `urllib.request` 間接載入才沒炸）。

另記一條非程式的坑：**`#` 在 zsh 互動模式預設不是註解**，貼帶行尾註解的指令會讓 `#` 與後面的文字變成參數。實測時出現的 `FAIL #` 看起來完全像取數層壞了，但取數層沒事。**偽裝成程式問題的非程式問題最浪費時間。**

### 2026-08-05 · 第 4 版（清掉四條待辦，其中兩條的原假設被推翻）

| | |
|---|---|
| **動到哪些檔** | **新增** `tools/fetch_twse.py`；**改** `chartkit.qa_series`（6σ→5σ＋連續日合併）、`AGENT_BRIEF.md`（§3.4／新增 §3.5 序列新鮮度／§7 三項新檢查）、`MAINTENANCE.md` |
| **量測** | 旗標 2→7→合併後 5；黃金 2026-01-30 z=−5.59（舊門檻漏抓）；單日 JSON 105.7KB＝series 47.1＋option 41.0＋文字 16.4 |
| **怎麼驗的** | 三種門檻（6/5/4.5σ）對同一份資料比對；新鮮度與 `window.note` 雙向比對報得對不對 |
| **怎麼倒回去** | `qa_series(z=6.0)` 改回；移除 §3.5 與對應檢查 |
| **當時已知的風險** | 5σ 讓信用利差圖常態多出數筆旗標，增加 `about.run` 撰寫負擔 |

把 `MAINTENANCE.md` 第 4 節積著的四條待辦逐一查完。**兩條的原始假設是錯的**，這比查證本身更值得記。

- **黃金 2026-01-30 單日 −11.4%：判定不是轉倉，`qa_series` 門檻由 6σ 改為 5σ。** 實測 z＝−5.59，正好卡在舊門檻下方。排除轉倉的理由是數量級——黃金期貨轉倉價差來自持有成本、正常在 1% 以內，11.4% 差了十倍；排除來源錯價的理由是它之後停在 4,600–4,900 沒回到 5,300，而錯價的特徵是隔日還原。**一根 11% 的棒子沒有任何機制看過一眼，就是門檻失職。** 同時加入「同一序列連續日合併為單一事件」，因為 2025-04-03 與 04-04 是同一次關稅衝擊，拆兩筆只會讓 `about.run` 的處置說明變流水帳；合併後同一份資料是 5 筆而非 7 筆。第 3.4 節補上「先看幅度再看故事」的判斷順序——**不要用轉倉當成不想查的藉口**。
- **費半跌幅：原假設被推翻，但尚未結案。** 重算確認 −21.9% 的算術無誤（序列已 rebase，比例不受影響；高點 198.60 @ 06-22）。但發現 `^SOX` 序列**止於 08-03、比當期落後兩個交易日**，而 `window.note` 竟寫著「行情序列最新至 2026-08-04／08-05」——**沒有任何序列以 08-04 結束**。所以與媒體數字的落差，第一嫌疑不是圖註寫的「盤中／不同基準日」，而是我們的資料是舊的。缺口對得上：要達到 −18.5% 需兩日再漲 4.36%，−17% 需 6.28%，半導體在 −22% 回檔後反彈這個幅度屬合理。**還缺實際收盤價驗證**（沙箱無外網）。
- **由此新增第 3.5 節「序列新鮮度」與對應檢查**：落後 ≥2 天警示並要求 `subtitle`／`note` 寫出實際基準日、≥5 天硬失敗；`window.note` 與實際末日**雙向比對**。這是本次最重要的新增——**metadata 寫錯比不寫更糟，它讓讀者以為資料是新的**，而原本沒有任何一步會去核對它。
- **前端體積：問題框架本身是錯的。** 實測單日 105.7 KB，但**體積不隨天數累積**（每天獨立檔、前端一次只載一天），所以原待辦寫的「累積到 300KB」不成立；它只隨單張圖的序列長度成長。改為在檢查腳本加體積守門（>250KB 警示、>600KB 硬失敗），**不做過早的拆檔優化**。真要瘦身時該拉的槓桿已量測清楚：`option` 內嵌的資料點與 `series` 完全重複、佔全檔 39%。
- **台股籌碼：寫出 `tools/fetch_twse.py` 但標記為未實測。** 端點取自 advisory 產線的實測紀錄。**刻意獨立成一個檔案而不併進 `fetch.py`**——後者是每天 09:00 的關鍵路徑，把沒跑過的程式碼加進去，萬一 import 期就出錯會讓整輪產不出東西。先跑 `--selftest` 核對欄位順序，對了再併。

### 2026-08-05 · 第 3 版（發布路徑：改為 daemon，並補上驗證步驟）

| | |
|---|---|
| **動到哪些檔** | **改** `AGENT_BRIEF.md`（新增 §0 執行前提、§6 第 9–10 步）、排程 prompt、`MAINTENANCE.md`；**repo 外**：`~/.dashpush/auto-push.sh` 改為讀 `~/.dashpush/repos.txt` |
| **量測** | 發布延遲 ≤180 秒；本 repo 加入清單後首次自動推送成功（log 出現「本輪涵蓋 4 個 repo」） |
| **怎麼驗的** | reflog 出現新 commit ＋ Pages 抓檔比對 |
| **怎麼倒回去** | **要先把 repo 移出 dashpush 清單再恢復 git 步驟**，否則兩邊同時動會撞 `index.lock` |
| **當時已知的風險** | dashpush 是 repo 外的共用元件，動它會影響另外三個知識庫 |

首次試燒（13:13）由執行者回報一項不一致：排程 prompt 第 8 步與本文件第 6 節第 9 步都寫 `git add -A && git commit && git push`，MAINTENANCE 第 1 節卻明寫「不要跑任何 git 指令」。追查後發現真正的問題比不一致本身嚴重：

- **daemon 的設計是全自動。** `advisory-dashboard-daily` 的排程 prompt 寫明本機 `com.kenny.dashpush` 每 180 秒自動 add + commit + push，執行者只寫檔、不碰 git，寫完等 2–4 分鐘抓 Pages 驗證。姊妹系統都是這樣運作的。
- **但本 repo 當時不在它的監看清單內。** 實測證據：13:10 改完 `AGENT_BRIEF.md`，到 13:34（過了 8 個 daemon 週期）`.git/logs/HEAD` 仍只有 11:21 那次手動 initial commit，線上 Pages 抓下來也還是第 1 版。新建的 repo 不會自動被納入——11:21 那次是建置時手動推的，正是 MAINTENANCE 第 2.1 節記的 PAT 403 與 Pages 啟用那段。

**所以三份文件的矛盾，其實是「照抄了姊妹系統的正確設計，但這個 repo 的前提還沒成立」。** 若照原樣留著 git 步驟，日後 repo 一旦加進 dashpush 就會撞 `index.lock`；若直接刪掉 git 步驟而不處理 daemon，每天的產出會寫進本機、檢查全綠、然後**永遠不上線且毫無錯誤訊息**。決定走前者並把前提補實：

- 第 6 節第 9 步改為「不跑 git，靠 dashpush」，**新增第 10 步「驗證上線」**——抓 Pages 的 `data/index.json` 確認 `days[0].date` 是今天，含 CDN 快取的 `cache:'no-store'` 繞法。**這一步是這次最重要的新增**：靜默失效之所以危險，就是因為沒有任何一步會去確認「寫進本機」和「上線」是兩件事。排程 prompt 第 8 步同步改寫，並明確指示驗證失敗時**不要自己跑 git 補救**。
- **新增第 0 節「執行前提」**：repo 資料夾未連線就停下來回報，不要嘗試其他路徑。試燒時執行者是中途才把資料夾掛上的，若無人值守就會整輪失敗——`advisory-dashboard-daily` 早就有這條，本文件漏了。
- MAINTENANCE 第 1 節那條 git 禁令改寫為維護與每日執行都適用，並點名 dashpush 與「本 repo 必須在監看清單內」的前提；第 2 節已知坑新增一列，記下「檢查全綠但網站不動」的診斷與驗證法。

### 2026-08-05 · 第 2 版（首次自動執行前的規格對齊）

| | |
|---|---|
| **動到哪些檔** | **改** `AGENT_BRIEF.md`（§2 週末規則／§4 長度／§7 腳本）、排程 prompt、`MAINTENANCE.md` |
| **量測** | `reading` 三處統一 200–520；檢查腳本新增 QA 旗標處置警示 |
| **怎麼驗的** | 抽出腳本對 08-05 實跑，確認警示報的是預期那兩筆 |
| **怎麼倒回去** | 三處長度數字改回 250–420；移除週末段落與旗標警示 |
| **當時已知的風險** | 週末週線複查規則當時尚未實際跑過 |

建立當天做的維護巡檢，在排程第一次無人值守執行（08-06 09:01）之前修掉四處問題。**四項全部是「兩份文件不同步」或「規格有洞」，沒有一項是程式錯誤**——這印證了第 1 版開頭那句話：不同步是這類系統最常見也最難察覺的故障。

- **`reading` 長度統一為 200–520 字。** 原本第 4 節散文版與排程 prompt 都寫 250–420，只有第 7 節檢查腳本寫 200–520。已經造成實害：2026-08-05 軌道圖的 `reading` 是 429 字，依散文規範超標，但腳本放行。**選擇往腳本靠而不是往散文靠**，理由是既有的 `data/*.json` 不得改寫——收緊到 250–420 會讓 08-05 那期永久紅字，等於用一條規則把自己的歷史判成不合格。同時在第 4 節表格下加註「三處必須同時改」，避免下次又只改一邊。
- **補上週末（六、日）的軌道圖規則。** 原本兩份文件都只寫「週末順延」，但沒說順延到哪裡——而排程是一週跑七天，每個週末必定遇到，第一次就是 08-08。**決定不推進輪盤，改做週線複查**：五條軌道綁死週一到週五的唯一目的是「同一條軌道每週固定同一天出現」，週末若接著往下數第六、第七條，一輪之後軌道與星期就全部錯位，跨期可比性歸零——而那正是軌道圖存在的理由。週線複查則能同時滿足「週末有東西可出」與「不動到輪盤錨點」。
- **檢查腳本加入 QA 旗標處置的警示。** 08-05 那期有 2 筆旗標（HY／IG OAS 於 2025-04-03），`about.run` 卻只記錄了費半跌幅的基準差異，兩筆旗標的處置一字未提。第 3.4 節本來就要求處置寫進 `about.run`，但沒有任何機制去驗它。新檢查逐筆比對 `about.run` 有沒有提到該筆的序列名或日期。**刻意設計成警示而非硬失敗**：漏寫處置是紀錄瑕疵不是資料錯誤，用它擋住整期發布不成比例；但印出來就沒人能假裝沒看到。實跑確認它報的正是預期那兩筆。
- **FRED API key 的雙路徑寫進排程 prompt 第 4 步。** 第 3.1 節有完整說明，prompt 卻寫「只用 FRED 與 Yahoo Finance（免 API key）」。照 prompt 走的執行者不會知道有 key 這條路，也就不會察覺自己正在非文件化的退回端點上。已補上 `--check-key` 自我檢查與「沒有 key 也能跑，但要知道自己在退回路徑上」的說明，並在禁令補一條不得把 key 寫進回報。

### 2026-08-05 · 第 1 版（建立）

| | |
|---|---|
| **動到哪些檔** | **新建** `AGENT_BRIEF.md`、`README.md`、`index.html`、`tools/fetch.py`、`tools/chartkit.py`、`tools/render_day.py`、`data/2026-08-05.json`、`.github/workflows/deploy.yml` |
| **量測** | 首期五張圖；`qa_series` 門檻 6σ；FRED＋Yahoo 兩個來源 |
| **怎麼驗的** | 首日試作即抓到費半跌幅與媒體不一致（−21.9% vs −17~−18.5%），已於圖註標明待覆核 |
| **怎麼倒回去** | —（起點） |
| **當時已知的風險** | ICE BofA 近三年、Yahoo 期貨轉倉兩條限制已知並寫入 §3 |

- 建立本系統。定調為投顧知識庫的第四個上游庫，接在 `advisory-dashboard-daily` 之後執行。
- **五個 slot 的設計理由**：純「當日題材」會讓長期序列斷線，純「固定輪盤」會漏掉當天的大事。所以 1–4 吃當日、5 走輪盤，兩者互補。
- **雙軌產出（互動 HTML + 靜態 PNG／SVG）出自同一個 `Chart` 物件**，因為兩軌若各寫一次必然漂移。`render_day.py` 從 JSON 產圖，JSON 是唯一事實來源。
- **決定不轉載國際媒體圖表，改為重製**。除了版權，更實際的理由是：重製過程本身會抓到媒體的計算基準問題。首日試作即出現一例——費半自 6/22 高點跌幅，我們以 Yahoo `^SOX` 收盤價算得 −21.9%，而當日媒體引用為 −17%～−18.5%，已在圖註標明基準待覆核。
- 加入 `qa_series()` 單日跳動檢查（6σ）。首日抓到 2025/04/03 的 HY／IG 利差跳動，經查為關稅衝擊的真實市場事件而非資料錯誤——檢查有效。
- **已知限制**：FRED 對 ICE BofA 序列只給近三年；Yahoo 期貨為近月合約、轉倉會造成跳動。兩者都寫進第 3 節。
